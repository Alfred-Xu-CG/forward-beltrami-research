# Phase VI worklog

Start: 2026-09-22T18:59:43Z. The `START_TIME.json` file records the goal creation time. No Phase V clock is reused.

## Route A — fixed-grid monotone construction

Image identifiability card (cross-route, 2026-09-23). Question: Does the high32 image pair contain information about its 32-cycle displacement coefficient, or does the A2/A4/CF2 image-only neural failure reflect a weak or poorly optimized encoder? Exact diagnostic: for the declared synthetic three-coefficient deformation family, linearize moving-image sampling around the identity and fit its three coefficient fields from observed moving/fixed images through a batched three-column least-squares system. Compare estimated coefficients to generated held-out coefficients only after fitting. If the linearization is too inaccurate, initialize a three-variable exact differentiable image-sampling refinement with the linear estimate. Assumptions: the known synthetic basis is used for this **identifiability diagnostic only**, not as an acceptable general-purpose registration layer; image gradients are measured in physical coordinates, Gram conditioning is reported, and the held-out split is unchanged. Falsifiers: poor coefficient estimation despite low image residual, nearly singular Gram systems, or any use of target coefficients during fitting. Smallest test: eight held-out high32 pairs at 512², comparing coefficient RMSE, image MSE and map RMSE with learned A4. Prior work: linearized optical-flow brightness constancy; this does not imply topological safety for an unconstrained optical-flow map.

Exact SPD neural-backend card (Route C). Question: Can the measured PARDISO factorization benefit be incorporated in the all-edge positive-conductance layer with an exact implicit VJP, rather than remaining an isolated matrix benchmark? Exact claim: for every finite logit vector, positive conductances form the same SPD Dirichlet matrix as the existing all-edge layer; one symmetric factorization solves the two-coordinate primal and its transposed (identical) adjoint system, and the edge-logit VJP is the negative primal/adjoint edge-difference inner product times the softplus derivative. The layer must free native factor memory after backward or inference and independently reject nonfinite/folded represented outputs. Assumptions: optional PyPardiso availability, CPU float64 input, correct upper-triangle storage and edge ordering, and identical boundary right-hand side. Falsifiers: full-map or VJP disagreement with the separate Schur/SuperLU reference, gradient finite-difference mismatch, factor memory growth across steps, or no actual training-time advantage. Smallest test: 17² and 65² independent map/VJP checks, then same 257² image-to-latent one-pair protocol for 50–300 steps with forward/VJP/RSS and face geometry. Prior work: symmetric sparse direct factorization and implicit differentiation; backend replacement is an engineering speed improvement, not a new homeomorphism theorem.

Photometric local-hint card (Route A follow-up). Question: Can a generic local photometric correction, computed from the fixed/moving images **after** the trained coarse/fine A2 base map, activate A4's proven full-resolution latent freedom on held-out high32 images without hardcoding the target's Fourier basis? Exact construction to test: evaluate the moving image and its physical-coordinate spatial gradient at each base-mapped control vertex; compare with the fixed image at that original source vertex. Within a small local window, form the two-by-two brightness-constancy normal matrix \(G=\sum gg^\mathsf T+\lambda I\) and right-hand side \(b=\sum g(I_f-I_m\circ F_{\rm base})\), solve \(G\delta=b\), bound \(\delta\) into finite A4 radial logits, and let A4 certify the resulting original-grid P1 map. The image gradient is a photometric *input feature*, not a loss-descent direction \( -\nabla_F L\), and the radial safety map still defines the latent-to-homeomorphism contract. Assumptions: same held-out split, fixed pretrained image encoder, correct physical gradient units and sampling coordinates, window/regularization selected on train data, and no target-map use in selection. Falsifiers: worse held-out image/map geometry, near-degenerate faces, gross local flow aperture errors, or cost erasing the layer advantage. Smallest test: 17² coordinate/gradient finite-difference sanity, then 257² train-split parameter choice and independent held-out evaluation versus the unchanged A4 checkpoint. Prior work: Lucas-Kanade local optical flow and the A4 radial safety theorem; this test is a diagnostic refinement, not a general registration guarantee.

Repeated safe-hint card (Route A follow-up). Question: Can two to four image-conditioned local normal-equation passes recover fine deformation that a single smoothed pass misses, while retaining the **same original-grid P1** output and an acceptable area/Beltrami margin? Exact claim: after the learned A2+ base and first learned-plus-photometric A4 pass, recompute the local image residual/gradient on the current represented P1 vertex map and pass each subsequent bounded latent through the same four-color radial safety construction. Each pass preserves positive areas and fixed boundary, so their sequential **vertex-state updates** produce one valid P1 map on the original triangulation; this is not PL function composition and requires no dynamic query locator. Assumptions: image gradient and residual computed from the current map, finite ridge/logits, no target maps in hyperparameter choice, and explicit all-face certificates after every pass. Falsifiers: train-selected improvement fails on held-out pairs, minimum area or maximum Beltrami distortion becomes unacceptable, or backpropagation/memory cost scales poorly with pass count. Smallest test: freeze the 1,024-parameter high32 A4 encoder, use train-selected window 5/ridge 10/gain 1, evaluate 1–4 passes on the same 32/8 split, select count by train image loss subject to area threshold, then held-out QC and timing. Prior work: iterative Lucas-Kanade refinement and A4's fixed-grid safety theorem, not a promise of convergence.

Absolute base-relative area-floor card (Route A). Question: The four-pass photometric experiment violated float32 orientation only after areas were driven to roundoff scale; can the latent mapping maintain a declared positive **global** face-area floor through arbitrarily many local passes? Exact claim: for a positive base P1 grid, compute its minimum signed double area \(A_{\min}^{(0)}>0\) and select \(m=\rho A_{\min}^{(0)}\), \(0<\rho<1\). During each colored vertex motion with proposed \(d_v\) and adverse area changes \(g_t\), restrict its scalar to \(s_v\le\min_{t:g_t>0}\min\{\gamma A_t,A_t-m\}/g_t\), also \(s_v\le1\). If current faces satisfy \(A_t\ge m\), every post-pass face satisfies \(A_t^{new}\ge\max\{(1-\gamma)A_t,m\}>0\), so by induction the represented original-grid P1 map retains the same base-relative floor for any finite pass count in exact arithmetic. The differentiable global minimum contributes a real gradient; it must not be detached. Assumptions: exact face indexing, finite inputs, base orientation, and finite-precision checks performed separately; this is a latent-range constraint, not post-hoc fold repair. Falsifiers: small-mesh independent area/VJP mismatch, a four-pass 257² float32 folded face, margin collapse below rounding safety, or unacceptable image/Beltrami fit from the restricted family. Smallest test: 9² random large-logit and VJP check, then replay the failed four-pass 257² case with \(\rho\) selected on training split, measuring held-out topology, image/map/QC and forward/VJP cost. Prior work: elementary per-face linear area bounds and barrier-like safety constraints; no claim of an exact finite-precision theorem.

Sine-spectral flow-hint card (Route A image inference). Question: Is the local brightness-constancy correction noisy in incoherent spatial modes while the true fine displacement occupies a small number of coherent modes, such that a differentiable fixed-boundary spectral filter improves **both** mapping and QC error? Exact test: apply the discrete sine transform of type I separately along both control-grid axes to the interior two-vector photometric proposal; this corresponds to odd extension of a zero-boundary field, not a periodic deformation assumption on the square. Inspect the rank/energy of the 32-cycle target mode and the residual spectral distribution on the training split; then keep a declared number \(K\) of largest-magnitude coefficients (or use a continuous shrinkage) and invert the transform before A4 radial safety. Select \(K\), photometric window/ridge and gain by training image MSE under a positive face-area threshold, then evaluate held-out image, coordinate-map and Beltrami metrics. Assumptions: regular rectangular control grid, correct DST scaling/inverse and gradient, no target coefficient used in selection, spectral filtering of a *latent proposal* not of the final certified map, and finite areas after radial safety. Falsifiers: target mode has low signal-to-noise, filtering suppresses fine amplitude or worsens QC, or FFT overhead/VJP memory exceeds the saved cost. Smallest test: 9² round-trip and VJP, one 257² spectral ranking, then at most a short train-split parameter sweep with held-out evaluation. Prior work: discrete sine basis for Dirichlet-zero fields and spectral shrinkage; this is not a Beurling-transform/Beltrami equation solver and does not assume the deformation itself is periodic.

Intrinsic QC regularization card (A6 follow-up). Question: A6 improves image, coordinate and mean Beltrami errors, but its maximum predicted face distortion exceeds the target; can an image-only objective with a target-independent local QC penalty reduce this tail without losing the 257² performance advantage? Exact proposed loss: image MSE plus \(\lambda\) times the mean over all original P1 triangles of \(\max(0,|\mu_t|-\kappa)^2\), where \(\mu_t=F_{\bar z}/F_z\) is computed directly from each oriented affine face Jacobian and \(\kappa\) is a declared allowable distortion bound. No target map, target \(\mu\), or evaluation landmark enters training. The A6 radial safety theorem is unchanged; this penalty controls *quality*, not topology. Assumptions: consistent lower/upper triangle Jacobian indexing, finite denominator, fixed warm-start checkpoint and split, prior image-only loss scales measured before choosing \(\lambda\), and fresh-seed evaluation after selecting candidates on train/validation. Falsifiers: tiny or zero gradients at high-distortion faces, loss of image/map gains, higher Beltrami error elsewhere, or prohibitive VJP memory/time. Smallest test: 9² analytic affine Jacobian check and directional VJP, inspect 257² train-set penalty magnitudes, then 300–1000-step GPU runs at one or two \(\lambda\) values with all-face and new-seed evaluation. Prior work: quasiconformal distortion penalties in registration; unlike post-hoc repair, the decoder remains topology-safe for every iterate.

Question: Can a dense rectangular P1 map be generated from latent logits with no global linear solve, while guaranteeing a homeomorphism on the supplied fine triangulation?

Exact claim: On the standard lower-left to upper-right diagonal triangulation, define vertex images \(F(i,j)=(X_i,V_{ij})\), where \(X_0=0<X_1<\cdots<X_{N-1}=1\) and, for every column \(i\), \(V_{i0}=0<V_{i1}<\cdots<V_{i,N-1}=1\). The P1 extension maps the unit square homeomorphically onto itself. A row-wise analogue follows by swapping coordinates.

Assumptions: Complete rectangular triangulation, consistent face orientation, strictly realized coordinate inequalities, finite coordinates, shared boundary values on adjacent faces. The implementation uses normalized positive increments with a uniform floor and checks inequalities in the represented dtype.

Proof sketch: Within either triangle of a cell, the first output coordinate is affine in source \(x\) alone with positive derivative. The second output coordinate has positive source-\(y\) derivative: in the lower triangle it is the positive increment in the right column, and in the upper triangle the positive increment in the left column. Thus every affine face has positive determinant. More directly, \(X(x)\) increases from 0 to 1; for each fixed \(x\), \(y\mapsto F_2(x,y)\) is continuous, piecewise affine, strictly increasing from 0 to 1. Therefore every target \((u,v)\) has one preimage, found first from \(X(x)=u\), then from \(F_2(x,y)=v\). Compact-to-Hausdorff continuous bijectivity gives a homeomorphism.

What falsifies it: A face with zero/negative represented determinant, noninjective vertical fiber, failed boundary coverage, or a finite-precision collapse at intended resolutions.

Smallest decisive test: Random and extreme float32 logits on 17² and 257² grids; independent signed face-area check; first-order directional finite difference for VJP; then time a 257² forward and backward.

Prior work: Phase V's fixed-grid positive Tutte decoder and exact composition implementation provide comparators. This explicit triangular family is deliberately more restricted than unrestricted Tutte weights; expression on cross-coupled targets must be measured, not assumed.

Baseline: Local Windows Anaconda torch 2.5.1+cpu. The initial baseline pytest invocation imported Torch before NumPy LAPACK and aborted in `numpy.linalg.eigvalsh`; importing NumPy and executing a tiny eigvalsh before pytest made the existing symmetric/composition selection pass (47 passed, 2 CUDA-unavailable skips). This is a known runtime load-order issue documented in Phase V, not a Phase VI algorithm failure. New Route A focused tests: 3 passed.

First dense CPU measurement: `tools/phase6_benchmark_monotone.py --side 257 --batch 1 --repeat 3 --device cpu --dtype float32`, with `OMP_NUM_THREADS=4`, on local Intel64 Family 6 Model 60 CPU. The control mesh has 66,049 vertices and 131,072 faces. The three complete decode+map-loss forward times were 0.9605/0.9378/0.9473 ms; backward times were 0.6108/0.6020/0.6170 ms. Minimum signed face-area ratio was 0.29679. The untrained random-logit map had target RMSE 0.02127, which is only a workload value, not fitted accuracy. These timings exclude image warping and neural prediction; no CPU process peak was measured. A GPU and actual-training comparison remain necessary.

Remote checkout `e4c4939` was installed from a 3.77 MB incremental Git bundle into new `phase6_dense_20260923` directories on Element and AI; existing Phase V checkouts were not changed. Tracked files matched the commit; untracked transfer bundles were present during this run and were removed after fetching, without changing tracked code. SSH calls used `ClearAllForwardings=yes`, and fresh GPU inventory showed Element L40 GPU 1 and AI A6000 GPU 2 at 14/11 MiB used and zero utilization just before the runs. The same batch-1 float32 257² forward/VJP workload ran ten repeats on both. Element GPU 1 median forward/backward was approximately 0.532/0.660 ms; AI GPU 2 approximately 0.69/0.79 ms (some earlier samples slower). Both reported a minimum signed area ratio 0.33159, map RMSE 0.02192 for the random untrained map, and 5,003,264 bytes of peak CUDA allocated memory. These are decoder-plus-map-loss microbenchmarks on different shared hosts, not a ranking of GPUs or complete registration layers.

The decoder now also accepts multiple latent resolution levels. Each level's logits are interpolated to the 257² latent grid and summed before one monotone decode on the original fine triangulation; interpolation does not resample a map. A 17² two-level gradient/topology test passes. The image-to-latent experiment uses an encoder with coarse and fine logit heads; a 9² control / 16² image / two-step CPU smoke passed and reduced image MSE from 0.01937 to 0.01755. The planned main-scale run will use 257² control vertices, 512² image queries and an image-only loss.

Main-scale Route A training: Two independent float32 CUDA runs with tracked code at commit `e508e4e`, batch 2, 257² control vertices / 131,072 faces, 512² image queries, latent sides 33 and 257, Adam rate 0.003, 300 image-only updates. Untracked bundle transfer files were still present in the checkouts during the runs but did not enter imports; they were deleted after fetching. Element L40 GPU 1 improved image MSE 0.0297863→0.00859470; AI A6000 GPU 2 gave 0.0297862→0.00871670. Final map RMSE was 0.01187/0.01190 against the independently generated analytic target; minimum signed area ratio 0.36545/0.34844. Median post-first-step complete encoder+decoder+warp+loss forward was 1.00/2.36 ms, backward 1.83/3.90 ms, and peak allocated CUDA memory 91.17/90.14 MB. These host-specific timings include GPU synchronization but exclude one-time query table construction. The target's horizontal displacement depends on source y, outside a single vertical layer's exact representation family, so nonzero final error is expected. Full per-step receipts are in `raw_results/routeA_train257_512_*_300.json`.

## Route B — local patch maps and exact composition

Question: Can many independent small homeomorphisms supply dense-grid details while avoiding a global matrix solve and preserving a certified PL composition?

Exact claim: Partition selected nonoverlapping square patches of the fixed grid. In each patch, fix its boundary pointwise and map each interior vertical column by strictly increasing y values with x unchanged, or each interior horizontal row by strictly increasing x values with y unchanged. Outside the patches use identity. The P1 map of each layer is a homeomorphism of the full square. Compose vertical and horizontal layers on shifted patch partitions; the exact continuous composition remains a PL homeomorphism, generally on a refinement rather than the original triangulation.

Assumptions: Patch side divides the full-grid cell count at offset zero; other offsets select only complete nonoverlapping patches and leave uncovered strips unchanged. Interior updates use normalized positive increments with a represented-dtype floor, patch boundaries remain exactly equal to identity, and all layers share the same oriented source triangulation. The source for a later map is still the unit square; query points are dynamically located in that map's triangles.

Proof sketch: A vertical patch map holds x fixed and gives each vertical fiber a strictly increasing continuous P1 y map with endpoints fixed. It is therefore a homeomorphism of its patch, equal to identity on its boundary. Horizontal patches are the coordinate-swapped case. Disjoint patch maps glue to an identity map outside patches, yielding a global homeomorphism. Composition preserves homeomorphism; finitely many PL maps have a finite common-refinement partition on which the composition is affine.

What falsifies it: A collapsed represented increment, mismatch along patch boundary, negative face determinant in an output layer, incorrect dynamic point location, or an unbounded practical cost when queries pass through many layers. Sampling the composition at original vertices and reinterpolating is not covered by this claim.

Smallest decisive test: Two shifted 17² patch layers on a 33² mesh, one vertical and one horizontal, with random and extreme logits; direct face-area and boundary checks; finite-difference VJP; compare exact coordinate composition with direct per-layer evaluation. Then 257² control/512² query forward and backward, and image registration on the same target as Route A.

Prior work: Reuse Phase V's `StructuredDenseQueryTable.interpolate_points` and `compose_control_maps` for exact dynamic query evaluation. This route's latent-to-map construction is local and solve-free; batched local positive-Tutte solves remain a separate variant to test if the explicit patches lack expressive power.

Implementation state: `LocalPatchMonotoneLayer` uses per-patch positive increments in only interior rows/columns and leaves each patch boundary pointwise fixed. Offset-zero and half-patch-offset layers can be composed with `LocalPatchComposition`, which uses the existing dynamic query locator. Three focused tests pass, including random 33² patch layers, two-layer VJP through exact composition, and extreme finite float32 logits on a 257² grid with 16-cell patches. A 17² control / 32² query / two-layer CPU smoke ran successfully.

Main-scale Route B measurements on AI RTX A6000 GPU 3 (freshly 11 MiB used, zero utilization before the jobs), float32, 257² control / 512² queries: two layers, batch 1, 115,440 latent scalars per sample gave median forward 2.70 ms and VJP 2.34 ms, peak CUDA allocated 85.8 MB. Four layers, batch 2, 230,880 latent scalars per sample gave median forward 6.93 ms and VJP 5.87 ms, peak 238.0 MB. These include patch decoding, dynamic point location and a coordinate map loss, not image encoder/warp. Minimum signed-area ratios per layer exceeded 0.23; no original-grid P1 certificate is implied for the composition. Raw repeat arrays are in `raw_results/routeB_patch257_512_ai_gpu3_*.json`.

Image-only training on the same synthetic pair and analytic target as Route A used a small convolutional encoder, 257² patch layers, 512² images, batch 2, Adam 0.003. Two layers over 300 steps improved image MSE 0.0294455→0.0128848, final query-map RMSE 0.0201273, peak CUDA allocation 177.1 MB. Four layers over 500 steps improved image MSE 0.0294226→0.00911557, map RMSE 0.0194510, peak 258.0 MB. Every individual layer had a positive observed minimum face-area ratio; the exact composition theorem supplies continuous topology conditional on represented layer validity. The four-layer image objective is near Route A's result, but its map RMSE is substantially worse: registration texture ambiguity and patch-fixed lines matter. Raw step records are in `raw_results/routeB_patch_train257_512_ai_gpu3_*.json`. This is one synthetic pair family, not a general accuracy ranking.

## Route C — fine-grid positive-conductance structure

Question: Can a shared fine-grid positive-conductance baseline be updated from hierarchical latent variables and solved with exact algebraic structure fast enough for forward/VJP, without storing the dense inverse or turning a coarse interpolation into a topology claim?

Exact claim for the first implementation: Let the full fine-grid Dirichlet conductance matrix be (A_0\succ0), let (f_0) be its exact solution at a fixed ordered square boundary, and select (k) full-mesh edges. The (r)-th selected edge has an interior incidence column (u_r\in\mathbb R^{n_I}), full-edge baseline difference (d_r=f_0(p_r)-f_0(q_r)\in\mathbb R^2), and positive increment (\delta_r>0). Put (U=[u_1,\ldots,u_k]), (D=\operatorname{diag}(\delta)), (W=A_0^{-1}U), (G=U^\mathsf{T}W). Then the exact updated interior map is

\[
f_I=f_{0,I}-W(D^{-1}+G)^{-1}d,
\]

where the rows of (d) are the full-edge differences (d_r). This includes boundary-adjacent selected edges because the full-edge differences account for the changed Dirichlet right-hand side. It is algebraically equivalent to solving ((A_0+UDU^\mathsf{T})f_I=b_0+\Delta b); every updated edge remains positive. The same (W,G) are reused for all latent batches. No full (A_0^{-1}) is stored, only (n_I k) columns and a (k\times k) Gram matrix. Autograd through the small SPD solve and matrix product supplies a first-order VJP; compare it against an independent full sparse solve and finite differences.

Assumptions: (A_0) is the Dirichlet graph Laplacian on the **same** complete 257² triangular mesh; all baseline active conductances are positive; the square boundary stays fixed and satisfies the existing Tutte geometric hypotheses; selected edge incidences and signs use the same indexing as the assembled matrix; the positive increment and baseline solution are represented accurately enough to preserve the observed face-area margin. The exact-arithmetic Tutte conclusion is not automatically a floating-point certificate.

What falsifies it: A sign/right-hand-side error on an interior-boundary edge, disagreement with a freshly assembled sparse solve, VJP mismatch, dense W storage or k³ small solve overwhelming the baseline, or a negative represented face on the returned fine-grid P1 map. Large smooth conductance changes will generally update many edges, raising rank; this must be measured instead of hidden.

Smallest decisive test: On a 9² grid, include both interior-interior and interior-boundary selected edges, compare to independent SciPy sparse solve with residual and map error, finite-difference the latent gradient, and check every face. Then measure k=16/64/256 on 129² and 257², including shared precomputation and reuse, forward/VJP and memory. Separately test a coarse latent affecting a block of fine edges and report its actual algebraic rank. If low-rank is inadequate, implement and compare an exact block/Schur option rather than stopping at a rank objection.

Prior work: Phase V's `MatrixFreeSymmetricTutteLayer` provides the full positive-conductance CG comparator and implicit VJP. Its old `woodbury_row_update` applies to directed row changes and is not this symmetric edge-increment construction. The matrix identity is classical Sherman–Morrison–Woodbury; the research question here is whether a reusable fine-grid baseline and physically useful update family give an actual neural-layer advantage at required resolution.

Second Route C research card — exact block elimination. Question: When one coarse latent changes conductances on many fine edges and Woodbury rank is therefore large, can the full fine-grid positive system be solved and differentiated by exact patch-interior elimination at useful cost? Exact claim: Partition interior vertices into (P=\bigsqcup_b P_b), where (P_b) are vertices strictly inside disjoint (p\times p) cell patches, and interface vertices (Q). No graph edge joins distinct (P_b). Consequently (A_{PP}) is block diagonal and

\[
S=A_{QQ}-\sum_b A_{QP_b}A_{P_bP_b}^{-1}A_{P_bQ}\succ0,\qquad
S f_Q=b_Q-\sum_b A_{QP_b}A_{P_bP_b}^{-1}b_{P_b},
\]

followed by (f_{P_b}=A_{P_bP_b}^{-1}(b_{P_b}-A_{P_bQ}f_Q)), is algebraically the original full fine-grid Dirichlet solution, not an approximate coarse replacement. Reusing the same factors to solve (A\lambda=\partial L/\partial f_I) yields the implicit VJP (\partial L/\partial c_{ab}=-(f_a-f_b)\cdot(\lambda_a-\lambda_b)), with zero adjoint at fixed boundary vertices; chain through positive conductance logits. Assumptions: one consistent complete triangulation and edge ordering, positive conductances, fixed ordered convex boundary, exact block partition including diagonal edges, no dropped Schur fill, represented residual and face-area checks. Falsifiers: a cross-block edge, discrepancy with an independent complete sparse solve or finite-difference VJP, excessive Schur fill/memory, or folded represented output. Smallest test: 17² p4, random all-edge positive weights, compare complete sparse direct and directional finite difference; then 65², 129² and 257² p16 with build/forward/VJP/memory timings. Prior work: standard Schur complement/domain decomposition identity; this is an exact direct solve, explicitly not an AMG stopping scheme. The application-specific question is whether its interface/fill and factor reuse help the neural layer.

Measured low-rank state: The SparseEdgeWoodburyTutteLayer includes both interior-interior and interior-boundary edge increments; three focused algebra, topology, and directional-gradient tests pass. On 257² control vertices, AI RTX A6000 GPU 2, float32, fixed baseline factorization takes 5.65/5.95/7.34 s at ranks 16/64/256. The corresponding repeated median forward times are approximately 1.18/1.09/2.88 ms and backward 1.42/7.79/7.89 ms; batch is 1/2/2. Peak CUDA allocation is 37.2/59.0/108.3 MB. An independent freshly assembled full sparse solve at every rank has relative residual about 4.5e-15 and differs from the GPU output by at most about 3e-8 in coordinates. Minimum face-area ratio in these untrained maps is positive. These times exclude one-time setup, and the update family only alters 16–256 selected edges out of 196,094 active edges, so it has weak global expressivity. Detailed records are in raw_results/routeC_woodbury257_*.json.

Existing all-edge matrix-free CG comparator: At 129² control, float32 tolerance 1e-5, the adjoint stalled near 1.93e-5 after both 1000 and 3000 iterations and raised rather than returning an invalid gradient. At float64 tolerance 1e-8 it converged after 1047 forward and 1163 adjoint iterations, with 4.2–4.4 s forward and 0.85–0.88 s backward on AI A6000; the forward includes CPU face validation. At 257² float64 tolerance 1e-8, the adjoint stalled at 1.48e-7 after 4000 iterations. At relaxed tolerance 1e-6 it converged after 2335 forward and 3389 adjoint iterations, taking 15.92 s forward and 2.43 s backward; minimum face-area ratio 0.8466. This does not make the relaxed solve equivalent to the Woodbury oracle accuracy. The late-CG issue motivates exact structure and/or a stronger preconditioner with explicit residual/topology checks, not a claim that CG is impossible.

Exact block-Schur prototype: A new CPU float64 module factors every patch interior, assembles all induced interface fill, factors the full interface Schur complement, reconstructs the complete original fine-grid map, and reuses factors for implicit VJP. Three tests pass: random all-edge weights agree with a separate complete sparse direct factorization to <1e-13; directional gradient agrees with central differences to <1e-8; batched gradients are finite. A 65² control / 16-cell patch local benchmark gave 72 ms forward and 10 ms backward, versus 28 ms for a separate complete sparse direct forward; interface has 369 vertices and 30,617 structural nonzeros. Its first measured point is slower, not a speed win. Larger scale will decide whether the interface growth changes this conclusion.

Route A/B crossover card — alternating global monotone maps. Question: Can two or four full-resolution solve-free monotone layers, alternating vertical and horizontal directions, remove the single-layer x-only-by-column restriction and the local-patch fixed-line restriction while retaining fast exact PL composition? Exact claim: Each layer is the previously proved original-grid P1 homeomorphism; their function composition is a continuous PL homeomorphism. Dynamic point location in each subsequent layer evaluates the exact composition at requested image coordinates, except for ordinary floating-point error; it does not certify P1 reinterpolation on the original grid. Assumptions: every layer's represented increments remain strictly positive and coordinates stay in the square, query locator uses the same oriented mesh, and only the final image is resampled. Falsifiers: layer-face collapse, incorrect coordinate composition or VJP, excessive query-location memory/time, and failure to fit the cross-coupled target despite full fine-grid latent freedom. Smallest decisive test: 17² direct sequential query evaluation and VJP finite difference, then 257² control/512² query forward/VJP plus image-only training against the same synthetic pair as Routes A and B. Prior work: Phase V exact Tutte composition query code and current Route A monotone decoder; this is a new comparison of solve-free full-resolution alternating layers against patch-boundary-local layers.

Route C image-latent card — all-edge hierarchy versus sparse-edge restriction. Question: Does a coarse-plus-fine image encoder coupled to positive conductances on every edge recover meaningful image deformations at 257², and what is the full training cost when the exact Schur forward/adjoint is included? Exact claim: Bilinearly sampling coarse and fine scalar latent fields at every fine-grid edge midpoint produces finite edge logits; softplus plus a positive floor yields positive conductances on the entire original fine graph. The exact block-Schur solve recovers the original fine-grid P1 map; its implicit gradient to every edge logit composes with the differentiable midpoint sampling and convolutional encoder. The fixed convex boundary and represented face check provide the same conditional topology contract as the solver. Assumptions: fixed edge ordering and midpoint coordinates, CPU float64 Schur solve, no detached bridge between encoder and solver, and a common image-only loss with the explicit routes. Falsifiers: gradient disappearing at either latent scale, disagreement with a tiny finite difference, training instability/folds, or total time and memory making the method uncompetitive. Smallest test: 17²/32² two-step image smoke with nonzero gradients to both scales; then 257²/512² batch-one short run on idle remote CPU, with per-step forward/VJP and RSS. A parallel low-rank Woodbury image encoder is useful as a contrast, but its narrow edge-update family must not be mistaken for the all-edge model.

Route A/B held-out generalization card. Question: Does the image-conditioned fine-grid layer learn a deformation inference rule rather than simply memorize one fixed/moving pair? Exact claim to test, not to assume: after image-only training over a deterministic family of distinct textures and analytic diffeomorphisms, the encoder plus certified decoder should reduce held-out registration error and recover held-out maps without target-map supervision. Assumptions: all generated maps obey a stated global Lipschitz perturbation bound below one and fix the square boundary; training and held-out seeds are disjoint; both methods see identical samples, optimization schedule, device, batch, and query grid; the true map enters data synthesis and evaluation only. Falsifiers: no held-out gain, much worse held-out map error than training error, failure of represented layer inequalities, or unreasonable forward/VJP/memory at 257². Smallest decisive test: several training and held-out amplitudes/textures at 65²/128², then 257² control / 512² image, one and two layers, with several hundred steps and a fixed held-out set. Prior work: the Phase VI one-pair image-only runs establish differentiability and fitting but not statistical generalization; no generalization claim should be made from them.

Explicit-layer scaling card. Question: Does the solve-free layer remain useful when control vertices grow past the required 257² scale to 513² and 1025², with fixed 512² image queries? Exact claim to test: for a fixed number of explicit P1 factors, decoding is \(O(LN^2)\), dense query interpolation/composition is \(O(LQ)\) for structured-grid arithmetic point location, and activation storage is \(O(LN^2+LQ)\) up to implementation constants; neither forward nor first-order VJP requires a sparse matrix factorization or Krylov trace. Assumptions: each factor's represented increments remain positive; query count \(Q\) and batch are recorded separately; one-time mesh/query-table setup is excluded and reported; CPU and GPU resources are not conflated. Falsifiers: superlinear observed cost after accounting for initialization, pathological dynamic point-location memory, or represented float32 area collapse at \(N=1025\). Smallest decisive test: 129² and 257² first, then 513² and 1025² with batch one and 512² queries, random controlled logits, repeated forward/VJP, process RSS, and independent per-factor area checks. Prior work: the 257² Phase VI benchmark established the mechanism; this is a scaling measurement, not a new topology theorem.

Route A2 research card — nested convex-quadrilateral subdivision. Question: Can multiscale latent variables generate a cross-coupled \(257^2\)-vertex map that is P1 on the **original fine regular triangulation**, globally homeomorphic by construction, and solve-free? Exact claim: Start with the identity map on one convex square cell. At every dyadic level, split each *shared* horizontal and vertical cell edge at a strictly interior point determined by its latent fraction (boundary-edge fractions fixed at \(1/2\)). In each convex parent quadrilateral, connect its bottom/top edge points and its left/right edge points by straight chords. They meet uniquely at an interior center. The four child quadrilaterals are intersections of the convex parent with four chord-defined halfplanes, hence convex with disjoint interiors and their union the parent. Assign their corners, edge points and center to the corresponding \(2n-1\) by \(2n-1\) source grid vertices, triangulate each child by the fixed southwest-to-northeast diagonal, and repeat. After eight levels, the P1 map on the canonical \(257^2\) grid is a boundary-identity homeomorphism. Unlike the earlier single-axis map, both output coordinates may depend on both input coordinates. Assumptions: finite strictly convex parent quadrilaterals, strictly interior edge fractions, one shared edge point for neighboring cells, consistent node/face orientation, and an intersection determinant bounded away from represented zero in numerical execution. Boundary fractions equal \(1/2\) to fix boundary vertices pointwise. The exact-arithmetic theorem is parameterization-wide; finite float32/64 outputs require a positive-face and boundary check. Falsifiers: a convex parent producing a nonconvex/overlapping child, an intersection outside either chord, a finite-precision zero face at 257², a failed directional VJP, or an impractical backward time/memory despite no global solve. Smallest decisive test: explicit one-cell geometry with asymmetric edge fractions and orientation checks; recursive 5² and 17² random/extreme latent tests; finite-difference VJP; then 257²/512² forward/VJP and direct-latent fit to the independent high-frequency map. Prior work: geometric subdivision of convex cells and multiresolution structured grids are classical; this particular neural parameterization is a candidate, not an asserted novel theorem. It is distinct from an unrestricted vertex displacement followed by line search, because its latent coordinates determine edge splits and cell centers before a map exists and every exact subdivision remains a homeomorphism.

Route A2+ research card — free center within the inner edge-point quadrilateral. Question: Does the chord-intersection center impose unnecessary expressivity loss, and can the center become a separately learnable latent without sacrificing the fixed-grid P1 theorem? Exact claim: For a convex parent with cyclic edge points \(B,R,T,L\), their inner polygon \(K=[B,R,T,L]\) is strictly convex. If the center \(C\) lies strictly inside \(K\), then all four child quads \([q_{00},B,C,L]\), \([B,q_{10},R,C]\), \([C,R,q_{11},T]\), and \([L,C,T,q_{01}]\) are strictly convex and tile the parent. A bilinear positive mixture of \(B,R,T,L\), using fractions \(u,v\in(0,1)\), lies in \(\operatorname{int}K\). Therefore two center logits per cell, including the root square, may move the center while preserving every prior topological conclusion. Assumptions: same shared strictly interior edge fractions and fixed boundary as A2; center fractions realized strictly inside (0,1); no degenerate represented faces. Falsifiers: an explicit convex parent/center in \(K\) with a nonconvex child, a failed represented 257² face check, bad VJP, or no improvement in map fit despite extra latents. Smallest test: geometry and VJP at 5², random/extreme 17² and 257², then the same 257² direct-latent high-frequency target and 512² image training comparison as A2. Prior work: convex-cell subdivision and positive barycentric combinations are classical; no novelty claim follows. This is an architectural extension of the already tested chord-center variant, not a post-hoc deformation repair.

A2 geometry-regularization card. Question: The image-trained A2 map has excellent held-out image MSE but an observed maximum Beltrami modulus near 0.91 against a target maximum near 0.32; can a target-independent geometric prior reduce this disparity without erasing registration accuracy? Exact claim to test: adding \(\lambda\) times the mean squared deviation of fine horizontal/vertical edge gradients from the identity edge gradients to the **image-only** objective keeps the decoder's hard topology construction unchanged and supplies a differentiable first-order smoothness prior. This term uses only the predicted control map and source grid, never the analytic target map or target Beltrami coefficient. Assumptions: same 257²/512² data split, encoder, optimizer and GPU as the unregularized A2 run; separately report image MSE, map RMSE, source-area-weighted face Beltrami RMSE, maximum \(|\mu|\), minimum face area, timing and memory. Falsifiers: no QC improvement, serious loss of image alignment, greater near-folding despite the prior, or the metric changing only because evaluation grids differ. Smallest test: \(\lambda\in\{10^{-3},10^{-2}\}\) at 257² with 1000 steps, load each saved state into the same independent face-level evaluator. Prior work: smoothness penalties are standard in registration; this is not a topology guarantee or novel regularizer, only a controlled accuracy tradeoff within a hard-homeomorphic decoder.

Route B representation-cost card. Question: How much does exact composition refine the P1 triangulation even when each factor is on the same 257² grid? Exact claim: Let \(T\) be a source triangle, let its image under the first affine factor have vertical extent \([y_{\min},y_{\max}]\), and let the second factor's canonical grid have horizontal lines at \(j/(N-1)\). Every open horizontal grid strip intersecting the interior of the image of \(T\) contributes at least one positive-area cell to the geometric overlay partition needed to evaluate the exact composition facewise. Its strip count is \(\lceil (N-1)y_{\max}\rceil-\lfloor (N-1)y_{\min}\rfloor\), with lines exactly touching a vertex handled by a numerical tolerance. Summing over source triangles is a lower bound on **overlay incidences**, not on the minimum number of distinct affine formulas: adjacent regions can coalesce if the second-factor gradients happen to agree. Assumptions: first factor is nondegenerate P1, second factor uses the fixed grid, and counted intersections have positive area. Falsifiers: an open intersected strip with no positive-area preimage in \(T\), or an implementation reporting count below one for a positive source face. Smallest test: identity, controlled random first factor, and the saved 257² two-factor direct-map oracle; count total strips, fraction of source faces crossing a second-layer horizontal line, and maximum strips per face. This is a conservative representation-cost diagnostic, not a full triangle-overlay construction. Prior work: classical PL-map composition via common refinement; no novelty claim.

Route C coherent-cut Woodbury card. Question: Does selecting a spatially coherent set of 256 fine edges at the same Woodbury rank produce a more useful deformation family than one isolated diagonal edge in each of 256 coarse cells? Exact claim to test: choose the southwest-to-northeast diagonal in every cell of one horizontal cell row, so all selected active edges cross one horizontal cut. Positive increments on these 256 edges preserve all original graph weights and the exact Woodbury formula/VJP; the update remains on the original 257² mesh. This is a different rank-256 *family*, not a proof of high expressivity or faster factorization. Assumptions: canonical diagonal and edge ordering match the existing symmetric Tutte operator; selected edges are distinct and active; same batch, precision, device, optimizer and image target as scattered-edge comparison. Falsifiers: mismatch with independent full sparse solve, nonpositive face, instability of the small SPD system, or no measurable representational gain in an image-to-latent or direct-map fit. Smallest test: exact sparse oracle and topology at 17²/257², then compare scattered versus coherent-cut 257² rank-256 fixed-pair image training and forward/VJP under the same protocol. Prior work: classical low-rank updates and graph cutsets. The cut is chosen to probe spatial support; an arbitrary regional conductance field generally changes far more than 256 edges, so this does not resolve the all-edge scaling problem.

A2 encoder-sharing ablation card. Question: Is the 257² held-out accuracy gain caused mainly by the convex-cell decoder, or by granting it independently learned prediction heads at seven dyadic scales? Exact claim to test: tying the horizontal, vertical and center 1×1 convolution heads across all seven levels retains the **same** A2 decoder and hard homeomorphism theorem but cuts encoder parameters from 1006 to about 790, near AB2's 772. At each level, the common heads evaluate that level's downsampled image feature, then adjacent vertex logits are averaged into edge/cell logits exactly as before. This does not equalize all architecture details, but removes most of the extra head capacity while leaving decoder geometry unchanged. Assumptions: identical 32/8 held-out images and maps, 257² control/512² query, batch 2, optimizer, seed, device and 1000 updates; compare both registration and map metrics, topology, wall-time and memory. Falsifier: tied heads lose most of A2's image/map advantage, showing that the original comparison was materially confounded by head freedom. Smallest test: shape/gradient smoke at 17², then main-scale held-out training. Prior work: standard parameter-sharing ablation; this is causal architecture analysis, not a new deformation construction.

Fine-detail stress-test card. Question: Do apparent 257² gains survive a target with 32 spatial oscillations per side, only eight control cells per wavelength, rather than the original eight oscillations? Exact claim for independent target generation: let \(F(x,y)=(x,y)+a\,b(x,y)+(a_f,a_f)h(x,y)\), where \(a=(a_x,a_y)\), \(b=\sin(2\pi x)\sin(2\pi y)\), \(h=\sin(64\pi x)\sin(64\pi y)\), \(a_x\in[0.005,0.015]\), \(a_y\in[0.010,0.025]\), and \(a_f\in[-0.002,0.0025]\). Since \(\|\nabla b\|\le2\pi\) and \(\|\nabla h\|\le64\pi\), the displacement Lipschitz constant is at most \(2\pi\sqrt{0.015^2+0.025^2}+64\pi\sqrt2(0.0025)<0.895<1\). Therefore this continuous boundary-fixed target is a homeomorphism. The sampled P1 target on the 257² grid needs a **separate** face check; the continuum fact does not imply it. Assumptions: same texture generator, fixed disjoint train/test seeds, 32/8 sample counts, image-only training, 512² image queries, and equivalent methods/schedule. Falsifiers: analytic Lipschitz calculation or sampled-P1 orientation fails, aliasing or image ambiguity swamps the comparison, or the candidate's target accuracy collapses. Smallest test: independent sampled-P1 area check and 17² smoke, then 257² held-out A2/AB2/A and at least a short C run. Prior work: existing 16\(\pi\) fine term is an easier representational test; this variant isolates genuine near-control-scale detail.

Fine-detail identifiability card. Question: The high32 image-trained A2 model appears to recover essentially zero amplitude in the target's 32-cycle Fourier mode; is that due to the decoder, amortized encoder, or weak image-only objective? Exact diagnostic: project the predicted fine-grid displacement onto \(h(x,y)=\sin(64\pi x)\sin(64\pi y)\) by \(\hat a=\langle F-\mathrm{id},h\rangle/\langle h,h\rangle\) separately for output x/y. The synthetic target's coefficient \(a_f\) is known only for **evaluation**. Compare (i) the image MSE obtained by the known low-frequency part with \(a_f=0\), (ii) a direct-latent oracle fitted to target vertex coordinates, and (iii) image-only direct-latent optimization on one pair. If (iii) succeeds but the encoder fails, amortization/local feature prediction is implicated; if even (iii) fails while (ii) succeeds, the image objective/optimization is implicated. Assumptions: same 257² P1 decoder, 512² image synthesis, source-coordinate convention and boundary padding; no target map enters image-only optimization. Falsifiers: the projection does not discriminate actual fine recovery, the low-only image loss is far above the trained loss, or changing initializations reverses the conclusion. Smallest test: eight-heldout low-only image loss, one fixed pair at 257², 1000 image-only latent updates and oracle QC evaluation. Prior work: amplitude-projection diagnostics are elementary Fourier analysis; this is a failure-mode attribution, not a method claim.

Route C reusable-interface Schur card. Question: Can a positive-conductance family gain useful forward/VJP speed from an *exact* fine-grid Schur solve when latent changes are confined to edges whose endpoints lie on a sparse patch interface, so every patch-interior factor is shared across neural calls? Exact claim: Partition interior vertices into strict patch interiors \(P\) and interface \(Q\) as in Section 4 of `03_conductance_structure.md`. Fix all conductances on edges touching \(P\), and allow positive latent-dependent conductances only on active edges with no endpoint in \(P\). Then \(A_{PP}\), \(A_{PQ}\) and its exact patch factors and inverse coupling are independent of the latent. The reduced system is \(S(c)=S_0+U_Q\operatorname{diag}(c-c^0)U_Q^\mathsf T\) with the corresponding boundary right-hand-side update, and exact recovery produces the original 257² P1 solution. Because every full-graph conductance stays positive and the fixed square boundary satisfies the same Floater hypotheses, the exact map remains a homeomorphism; the represented result still needs a face/boundary/residual check. The same fixed patch factors and current interface factor solve the adjoint; the edge VJP remains \(-\Delta Y_e\cdot\Delta\lambda_e\), chained through softplus. Assumptions: changed edges truly avoid \(P\), no omitted Schur fill, consistent edge orientation/boundary values, CPU float64 reference implementation first. Falsifiers: discrepancy with a freshly assembled full sparse solve or finite-difference VJP, a nonpositive represented face, large interface factor time, or inability to express the common image targets due to interface-only control. Smallest test: 17² p4 full-oracle and gradient; then 257² p16/32/64 setup, forward/VJP/memory and same image pair short training. Prior work: static condensation/domain decomposition; the method is exact but its expressivity-versus-interface-size tradeoff is the research question. It must not be described as a universal all-edge fast solver.

Route B sampled-export card. Question: Can the exact two-factor PL composition be safely exported to a single P1 field on the original grid by sampling its vertices, or does this break the topology theorem in a concrete realizable case? Exact distinction: every vertical/horizontal factor is a P1 homeomorphism and their exact composition is a PL homeomorphism on its common-refinement partition. Sampling \(F_2\circ F_1\) at original grid vertices and linearly extending on the original triangles defines a *different* map; factor validity alone gives no theorem for its face orientations. Assumptions: factor logits are finite and certified positive on a 17² canonical mesh, query evaluation uses exact dynamic point location, all returned coordinates are finite. Falsifiers: no negative resampled face in a bounded random/extreme search (which would not prove general validity), or an independently evaluated factor itself is invalid. Smallest test: seeded random two-factor latents at 17² with growing scale, independently record each factor's minimum face-area ratio and the sampled-export minimum; if negative, store the exact latent state and a minimal reproduction. Prior work: standard PL common refinement; the user specifically identified repeated interpolation and fixed-grid export as a possible topology pitfall.

A2 image-only direct-latent geometry-prior card. Question: Can a target-independent deformation-gradient penalty prevent the high32 direct image optimizer from obtaining very low image error with large Beltrami distortion, while preserving a measurable 32-cycle component? Exact claim to test: minimize image MSE plus \(\lambda\) times the mean squared deviation of horizontal and vertical fine-grid edge vectors from their identity values, using the existing hard-homeomorphic A2 decoder and no target-map values in the loss. This is an empirical accuracy tradeoff, not a new guarantee; the decoder's topology theorem is unchanged. Assumptions: same held-out pair, 257² control grid, 512² image queries, optimizer initialization and step budget, with only \(\lambda\) varied. Falsifiers: QC error and maximum \(|\mu|\) do not improve, image alignment or high-frequency amplitude degrades too far, or numerical face certification fails. Smallest test: pair index one, 1000 updates each at \(\lambda=10^{-4},10^{-3},10^{-2}\), recording image MSE, map error, Fourier projection, area and face Beltrami metrics. Prior work: standard edge-gradient/smoothness priors in image registration; this test isolates a decoder/objective failure mode rather than claiming novel regularization.

A2 image-encoder resolution card. Question: Is the near-zero predicted high32 component an unavoidable 257² control-grid representational limit, or does doubling the control resolution while keeping the **same 512² images** improve amortized inference? Exact claim to test: rerun the unchanged high32 image-only train/test protocol with a 513² A2+ decoder, compare image/map error and projected 32-cycle amplitude against the existing 257² run, and report the doubled-resolution forward/VJP and memory separately. More control vertices cannot by itself prove encoder identifiability, and 512² query resolution is unchanged. Assumptions: same 32/8 deterministic split, 1000 Adam steps, batch two, encoder width, head mode, precision and CPU host; only control-side changes. Falsifiers: no improvement in projected amplitude, topology certificate fails, or cost becomes impractical. Smallest test: one 513² run plus the existing same-host 257² baseline, evaluate both with the same face-\(\mu\) tool and note that the sampled-P1 target floor differs by control resolution. Prior work: standard resolution ablation; no universal resolution claim.

A2 convex-cell obstruction card. Question: Does the high32 target even belong to the A2+ hierarchical convex-cell family on the fixed grid? Exact necessary condition: every mapped dyadic source cell is a strictly convex quadrilateral, because the A2+ construction maintains a convex image for each parent and child. A target sampled P1 map can be globally homeomorphic while a union of its two triangles in one source grid cell forms a concave quadrilateral; one such target cell excludes **exact** A2+ vertex matching, independent of optimizer and encoder. Assumptions: source cell corners are ordered consistently southwest, southeast, northeast, northwest; signed orientation uses the same source convention; assess all dyadic scales, not only the finest. Falsifier: all target cells strictly convex at every scale, in which case this particular necessary-condition objection vanishes but sufficiency remains unproved. Smallest test: evaluate analytic base and high32 held-out target samples at 257², enumerate signed cross products at four corners of every dyadic cell, report counts and minimum normalized corner cross products; check P1 face orientations separately. Prior work: elementary convex-polygon geometry and the proved A2+ construction; this is an architecture coverage audit, not a new deformation method.

A2 straight-edge obstruction card. Question: Even when all target dyadic cells are convex, are target grid vertices forced off the A2+ manifold because this decoder places each new edge vertex **on the straight mapped parent edge**? Exact necessary condition: for every dyadic parent edge with mapped endpoint images \(a,b\), its child midpoint image \(m\) lies in the segment \([a,b]\). If an analytic target has \(\delta=\operatorname{dist}(m,[a,b])>0\), no A2+ latent yields its exact sampled vertices. More quantitatively, if a candidate A2+ map has at most \(\varepsilon\) Euclidean vertex error at these three points, then \(\delta\le 2\varepsilon\): write the candidate midpoint as \((1-t)a'+tb'\), \(0<t<1\), and apply the triangle inequality. Assumptions: same recursive edge-split geometry; evaluate actual held-out analytic coefficients on a 257² grid in float64, all horizontal/vertical parent edges at each level, and distinguish this necessary obstruction from a global RMSE lower bound. Falsifier: every midpoint collinear and between its target endpoints within numerical tolerance. Smallest test: record per-level maximum and RMS midpoint-to-target-segment deviation for base/high32 held-out samples, then compare maximum deviation to direct-fit maximum vertex error. Prior work: elementary segment geometry, no novel theorem claim.

A2 amortized-optimization duration card. Question: Is the near-zero high32 Fourier projection merely a 1000-step optimization artifact? Exact test: warm-start the saved 257² high32 image encoder, reset Adam at rate 0.001 and run another 3000 image-only updates on the identical 32/8 split, then reevaluate the held-out maps and projection with the same face-level tool. A lower image MSE without recovered fine amplitude would implicate architecture, image ambiguity, or optimizer landscape more than the original short schedule; it cannot by itself distinguish these alternatives. Assumptions: no target-map loss, checkpoint loaded exactly, control and image resolution unchanged, same CPU host. Falsifier: substantial recovered amplitude and improved Beltrami geometry after longer training. Smallest test: the single 3000-step continuation; compare with the tracked 1000-step state and target-P1 face-error floor. Prior work: standard training-duration ablation.

Route B exact-composition geometry card. Question: Does the high32 two-layer AB2 coordinate-fit oracle preserve reasonable quasiconformal geometry **between** the fitted original-grid vertices? Exact claim: for a point away from triangle-switch boundaries, \(D(F_2\circ F_1)(x)=DF_2(F_1(x))DF_1(x)\), and each factor has positive determinant; therefore its exact composition has positive determinant almost everywhere, but vertex-map RMSE alone does not control its Beltrami coefficient. Assumptions: saved 257² two-layer direct-latent checkpoint, independent source-face-centroid queries, target analytic derivative at those points, and a separate target-P1 discretization comparison. Falsifier: large sampled face-\(\mu\) RMSE or extreme maximum \(|\mu|\) despite the excellent coordinate fit. Smallest test: decode saved AB2 factors, verify their own face-area margins, evaluate chain Jacobians in face batches, report source-centroid map/Beltrami RMSE and maximum \(|\mu|\); do not call this exact integral over the composition overlay. Prior work: classical chain rule for PL maps and exact dynamic query evaluation.

A2 contextual-encoder card. Question: The existing two-convolution A2 image encoder sees only a five-control-pixel neighborhood, possibly too small to infer displacements of several image pixels in the high32 family; can a modest multiscale context body recover a nonzero 32-cycle component without changing the hard-homeomorphic decoder? Exact test: add a body with three pooled context scales and skip fusion, retain the exact same A2+ control decoder and image-only objective, and train on the same 32/8 high32 split at 257² controls / 512² images for 1000 steps. Report its extra parameters and forward/VJP/memory, compare image/map/face-\(\mu\)/Fourier metrics against the original encoder, and keep it only if gains justify cost. Assumptions: identical initial output map (zero-initialized heads), batch two, Adam rate 0.003, float32, same deterministic split and same CPU host. Falsifiers: fine projection remains near zero or costs overwhelm the benefit. Smallest test: 17² shape and VJP smoke, then the full 257² held-out comparison. Prior work: standard image pyramids and convolutional context, not a novel deformation theorem; decoder topology is unchanged.

Route C fixed-patch harmonic obstruction card. Question: How much expressivity is lost when reusable-interface Schur restricts all changed conductances to edges with no strict patch-interior endpoint? Exact necessary condition: every represented map satisfies \(\sum_{j\sim i}(Y_i-Y_j)=0\) at each strict patch-interior vertex \(i\), since all conductances there remain exactly one, regardless of variable interface values. For a sampled target \(F_\star\), define \(r_i=\sum_{j\sim i}(F_{\star,i}-F_{\star,j})\). Any represented map whose Euclidean coordinate error is at most \(\varepsilon\) at every vertex must have \(\|r_i\|_2\le 2d_i\varepsilon\), where \(d_i\) is graph degree; thus \(\varepsilon\ge\max_{i\in P}\|r_i\|_2/(2d_i)\). Positive target residual is a proof of non-exact representability, not a global RMSE lower bound or a claim about arbitrary all-edge positive conductance. Assumptions: canonical full triangulation, unit fixed patch-interior edge conductances, strict patch-interior set at p16/p64, target sampled on the 257² vertices. Falsifiers: implementation has a changed edge touching \(P\), or a supposedly represented map violates the fixed-interior harmonic equation. Smallest test: independently form all mesh edges and residuals for analytic base/high32 targets; report strict interior count and max-error lower bound at p16/p64 and check a decoded map's \(P\)-residual. Prior work: elementary discrete harmonicity and triangle inequality; this is a representation diagnostic.

A2 supervised-encoder diagnosis card. Question: The direct A2+ latent oracle contains high32 detail but every image-only encoder so far predicts nearly zero fine amplitude; can the existing local encoder *represent and learn* the target family if given direct coordinate supervision during training? Exact test: retain the same encoder, A2+ decoder, 32/8 split, 257² control and 512² queries, but minimize query-map MSE alone for 1000 Adam steps, evaluating on held-out maps and images. This is explicitly an **oracle diagnosis**, not a valid image-to-latent registration result, because target maps enter its training loss. Assumptions: same deterministic samples, batch and initialization, target map used only for the diagnostic supervised variant; evaluate Fourier projection and face geometry. Falsifier: substantial held-out 32-cycle recovery with low map error, which would shift blame toward the image-only objective rather than the encoder capacity. Smallest test: add an optional map-only loss switch and run the local encoder once; compare with its already saved image-only state. Prior work: standard supervised-ablation diagnosis, not a new method.

Route B coarse-plus-fine A2 composition card. Question: Can a 17² coarse convex-cell factor followed by a 257² fine convex-cell factor remove the single-A2 straight-edge representational obstruction while retaining an exact PL-homeomorphism and avoiding a fine global linear solve? Exact claim: each factor is a boundary-identity P1 homeomorphism on its own complete structured triangulation; evaluating \(F_{257}\circ F_{17}\) at arbitrary queries by dynamic triangle location represents their exact continuous PL composition, hence a homeomorphism on their common refinement. It is generally not P1 on the original 257² triangulation. A coarse factor may absorb broad curvature so the fine residual needs less bending along its own hierarchy, but this is an empirical expressivity hypothesis, not a theorem. Assumptions: factor latent ranges and represented face checks as for A2+, one coarse and one fine map, original 257² target-coordinate oracle for a first decisive comparison, no image or target map interpolation between factors. Falsifiers: no error/geometry improvement over one fine A2+, high dynamic-query or VJP overhead, or a factor face failure. Smallest test: shape/identity/gradient and factor-topology at 17²+33², then 257² high32 direct-map oracle at equal 1000/3000 step budgets and separate exact-chain Beltrami evaluation. Prior work: classical composition of PL homeomorphisms and multilevel residual parameterizations; this is an explicit test of the user's coarse-to-fine cascade idea, not an inverse-multigrid solve.

Route B staged coarse-fine fit card. Question: Simultaneously fitting two redundant homeomorphism factors underperforms a single A2+ on high32; does a coarse-then-fine schedule improve the direct-map oracle at equal 3000 update calls? Exact test: with the same 17²+257² exact composition and analytic high32 target, first optimize only coarse factor for 1000 steps, then hold it fixed and optimize only fine factor for 1000, then jointly optimize both for 1000. The first stage can fit only broad structure; the second sees the exact composed output, not a pre-resampled target. Compare with simultaneous 3000 steps and single A2+ 3000 steps, reporting map and chain-Beltrami metrics. Assumptions: fixed factor order, initial identity, float32, Adam, same target and query grid; no inverse solve or target residual construction. Falsifier: staged schedule remains worse or produces near-folding factors. Smallest test: add factor selection to the existing direct-oracle runner, checkpoint at each stage, and evaluate the final exact composition. Prior work: conventional coarse-to-fine continuation; this is an optimization test, not a new topology theorem.

Route B coarse-fine image-layer card. Question: The 17²+257² exact convex-cell cascade improved direct-map fit on the base target, but does it improve *image-only held-out training* enough to justify a second factor? Exact test: use separate small image encoders to predict coarse and fine A2+ latents, decode both on their actual grids, evaluate their exact composition at 512² queries and train from image MSE on the same base 32/8 split, batch two, 1000 Adam updates. Keep original A2+ and AB2 held-out results as comparators but report the increased parameter count, factor triangles, dynamic query cost, topology per factor, wall/memory and map/Beltrami metrics. Assumptions: coarse side17, fine side257, float32, same images and seeds, no target map in training loss; this is not parameter-matched to A2+. Falsifiers: no held-out gain, large geometry distortion, or unacceptable forward/VJP memory/time. Smallest test: 17²+33² smoke with image MSE gradient, then main 257²/512² train and independent exact-chain face evaluation. Prior work: elementary cascaded deformation networks and PL composition; the differentiating question is the hard-homeomorphic dense-grid realization with measured cost.

Route C distributed-positive-update rank card. Question: Does a small number of coarse latent parameters imply a small Woodbury rank when each parameter changes many positive fine-mesh conductances? Exact claim: for a set of edges with strictly positive conductance increments, the full update \(\Delta L=U D U^\mathsf T\) has rank equal to the incidence rank of its positive-edge support graph, namely the number of incident vertices minus its connected-component count. After fixing boundary vertices, \(\Delta A=U_I D U_I^\mathsf T\) has rank equal to the number of interior vertices in each support component that touches a fixed boundary, or that count minus one in an entirely interior component. Therefore a single coarse parameter changing a connected swath of fine edges generally induces rank proportional to the number of affected vertices, even though its latent dimension is one. A rank-one positive-semidefinite graph-Laplacian update with nonpositive off-diagonal entries and zero row sums can change only one edge: writing it as \(uu^\mathsf T\) forces at most one positive and one negative entry of \(u\). Assumptions: strictly positive *increments* \(D\), not merely positive final conductances with mixed-sign increments; complete incidence including fixed boundary. Falsifiers: small graph violating the incidence rank formula, or a rank-one positive graph Laplacian supported on more than one edge. Smallest test: enumerate one-scalar updates on 1D/2D small graphs and check numeric rank against the graph formula; use the result to delimit, not dismiss, the implemented Woodbury and exact Schur families. Prior work: standard incidence-matrix rank theorem and graph Laplacian structure; no novelty claim.

CF2 parameter-capacity ablation card. Question: The 17²+257² image cascade has 1868 encoder parameters versus single A2+'s 1006; is its base-family held-out gain merely due to a larger predictor? Exact test: increase single A2+ convolution width from 8 to 12, giving approximately matched encoder parameters without changing its decoder, and rerun the same Element CPU 32/8 base image-only protocol for 1000 steps. Compare image/query-map errors, independent face-\(\mu\), maximum distortion, forward/VJP wall and RSS to CF2 and width-8 A2+. Assumptions: same data/seed/batch/optimizer, no target map in loss, only width changed; wider A2+ retains fixed-grid P1 topology. Falsifier: wider A2+ matches or exceeds CF2's accuracy and geometry, making the cascade gain less compelling. Smallest test: width-12 smoke and one 257²/512² held-out run. Prior work: standard capacity-matching ablation.

CF2 scaling card. Question: Does the exact 17²+fine-grid PL composition retain a practical first-order forward/VJP and memory profile beyond 257² fine controls? Exact claim to test: with fixed 512² image queries, its two decoders use \(O(N^2)\) arithmetic and activation storage in the fine side \(N\), while exact dynamic query composition uses \(O(Q)\) work/storage per factor; measured constants may still make it unusable. Assumptions: one batch, float32, fixed 17² coarse factor, identical small random latent distribution and image warp/loss, warm timing, four CPU threads; include full first-order VJP and report factor certificates separately. Falsifier: steep superlinear measured scaling, memory exhaustion, nonfinite gradients, or nonpositive represented areas at 513² or 1025². Smallest test: local 33² smoke, then same-host 257²/513²/1025² with 512² queries and three timed repeats, compared with the existing single-A2 scaling runner. Prior work: standard operation counting and Phase VI explicit scaling; no theorem about training quality at larger control scales.

Two fine convex-cell factors card. Question: The 17²+257² cascade helps the base target but hurts high32 direct-oracle accuracy; can two full 257² convex-cell factors remove the first factor's coarse bottleneck and bend a dyadic target edge that either single factor cannot bend? Exact claim: two identical-grid A2+ factors remain PL homeomorphisms under exact dynamic composition, but the composition is generally not P1 on the original grid and is not constrained to put a source dyadic edge midpoint on the segment joining its mapped endpoints. This is a possible expressivity gain, not an accuracy or speed theorem. Assumptions: full 257² factors, same analytic high32 target, same original-vertex coordinate loss and optimizer as the saved single-A2 and 17²+257² oracles, separately certified factor faces; distinguish doubled latent count/cost. Falsifier: no direct-fit improvement or pathological chain Beltrami geometry at a fixed update budget. Smallest test: 17²+17² tiny identity/VJP smoke and noncollinear midpoint witness, then 257²+257² 1000/3000-step direct fit with chain-Jacobian evaluation. Prior work: classical PL composition and our measured single-factor straight-edge obstruction.

Positive-conductance inverse-feasibility card. Question: Even with unlimited positive conductance degrees of freedom, can the sampled analytic base/high32 deformation be exactly represented as a symmetric weighted Tutte equilibrium on this specific structured triangular graph? Exact criterion: for fixed target vertex positions \(Y\), every interior vertex must satisfy \(\sum_{j\sim i}c_{ij}(Y_i-Y_j)=0\) simultaneously in both coordinates, with one symmetric edge value \(c_{ij}>0\); this is a sparse linear feasibility problem in \(c\), not a nonlinear PDE solve. For a declared bounded range \(c\in[c_{\min},c_{\max}]\), feasibility certifies a bounded positive representation; infeasibility only excludes that bounded range unless a dual separating certificate is extracted. Assumptions: the same 2D triangulation and edge ordering as Route C, fixed convex boundary, analytic target sampled on control vertices, float64 LP tolerance checked independently by residual. Falsifiers: solver reports success but weighted equilibrium residual is non-small, or supposedly infeasible instance becomes feasible under the same bounds by another solver. Smallest test: 17²/33² base/high32 with scipy HiGHS linear programming, then expand only if informative. Prior work: linear feasibility and Tutte's positive barycentric equilibrium theorem; this is a route-specific representability diagnosis, not a new theorem.

High-frequency image-loss card. Question: Two full fine factors can represent the 32-cycle target under coordinate supervision, yet image-only training suppresses that component; can a directly observable image-gradient discrepancy supply enough high-frequency signal without consulting target maps? Exact test: add \(\lambda\) times the mean squared horizontal and vertical one-pixel differences of the warped-minus-fixed image residual to the existing pixel MSE, while keeping the image-to-latent encoder, exact two-factor decoder, 32/8 split, 257² controls, 512² queries, 1000-step optimizer schedule and no map supervision fixed. The derivative term is computed in pixel units and its weight is recorded. Assumptions: same deterministic textures/maps, no target geometry in objective; output remains an exact PL homeomorphism because only training loss changes. Falsifiers: held-out 32-cycle projection remains near zero, geometry worsens, or gains are only from severe overfitting. Smallest test: a 33²/64² gradient smoke, then one same-host high32 run at weight 10 and independent held-out face/Fourier evaluation; adjust weight only if measured balance supports it. Prior work: Sobolev/gradient image matching and aperture ambiguity; no claim of geometric identifiability from one image channel.

Two-fine direct image optimization card. Question: If the two-fine amortized encoder fails to emit the 32-cycle component, is that because the image-only loss is uninformative even with freely optimized decoder latents, or because the encoder/optimization sharing across images fails? Exact test: on one predeclared held-out high32 pair, optimize all 257²+257² factor latents directly from identity using the same differentiable image resampling and image MSE, never adding target-map or coefficient loss. Evaluate coefficients, map and Beltrami only after fitting. Assumptions: one-pair diagnostic, not a held-out neural prediction result; exact PL factor certificates and source-face chain Jacobian are checked independently. Falsifiers: recovered fine amplitude and lower map error would implicate amortized inference; equally suppressed amplitude would leave image ambiguity/optimization unresolved. Smallest test: 17²+17² smoke, then 1000 direct steps at 257² with sample index 1 and a fixed learning rate, compare with saved A2 one-pair direct result. Prior work: standard per-instance optimization diagnostic; it is too slow to qualify as the desired fast layer.

Route A3 colored safety-radius latent card. Question: Can we remove A2+'s straight-parent-edge obstruction while retaining an **original-grid P1 homeomorphism** and no sparse solve or exact-composition query cost? Exact claim: start with a certified oriented A2+ P1 map and freeze the square boundary. Four-color the interior structured-grid vertices by the parity pair of their row and column indices; no two vertices of one color occur in the same mesh edge or triangle. For each color, and each vertex \(v\) in that color, compute \(r_v\) as the minimum signed altitude from its current image \(Y_v\) to the oriented opposite edge of each incident triangle. If all prior triangles have positive signed area, \(r_v>0\). A latent two-vector \(z_v\) moves it by \(\Delta Y_v=\gamma r_v\tanh(z_v)/\sqrt2\) for fixed \(0<\gamma<1\); hence \(\|\Delta Y_v\|<\gamma r_v<r_v\). The signed area of every incident triangle remains strictly positive because the point cannot cross any opposite-edge line. Same-color updates have disjoint affected faces; four sequential color passes preserve every face. With a fixed injective boundary map, an orientation-preserving nondegenerate PL map of this triangulated disk is globally a homeomorphism by planar degree/covering, not merely by a local Jacobian sample. Assumptions: complete fixed triangulation, correct cyclic opposite-edge ordering, exact arithmetic theorem, float32 represented positive-face check, strictly interior vertices only, finite latent, and the same face set used in both radius and final P1 output. Falsifiers: one color contains adjacent vertices, a computed radius overestimates a true face altitude, a finite latent yields a nonpositive face, boundary moves, gradient fails a directional finite difference, or 257² cost overwhelms A2. Smallest test: 9² random/larger logits with independent all-face certificate and finite-difference VJP; exhibit an A3 parent-edge midpoint off the A2 line; then 257² direct high32 oracle, forward/VJP/RSS and held-out image training if direct fit justifies it. Prior work: local vertex-kernel geometry and elementary finite-element orientation/degree theory; this is a latent-defined decoder, **not** an optimizer step \(F+t(-\nabla L)\) with post-hoc fold repair.

Directional radial safety card. Question: A3's Euclidean safe disk may be much smaller than the true intersection of its six face-positive halfplanes in a relevant displacement direction; can a closed-form radial boundary parameterization improve high32 fixed-grid P1 fit without losing its theorem or speed? Exact claim: for current vertex \(v\), incident oriented opposite edges \(e_t=Y_r-Y_q\) and signed double areas \(A_t>0\), let raw latent displacement \(d_v=\alpha h\tanh(z_v)\), with source spacing \(h=1/(N-1)\), declared scale \(\alpha>0\), and adverse signed area change \(g_t=(-\det(e_t,d_v))_+\). Set \(\tau_v=\min_t A_t/g_t\), interpreting zero \(g_t\) as \(+\infty\), and \(s_v=\min(1,\gamma\tau_v)\) with \(0<\gamma<1\). Moving by \(s_vd_v\) leaves every incident area at least \((1-\gamma)A_t>0\) when restricted and strictly positive when unrestricted. Four independent color passes and fixed boundary give the same original-grid P1 homeomorphism theorem as A3. This is a latent-defined radial map into a local feasible polygon, not a descent-gradient line search. Assumptions: same exact mesh incidence as A3, finite latent, positive input areas, and stable finite-precision division/clamping. Falsifiers: all-face orientation or boundary failure under large logits, VJP mismatch away from a min-switch, no direct-oracle improvement over A3 disk at equal 257²/1000/3000 schedules, or uncompetitive forward/VJP. Smallest test: 9² large-logit and directional VJP tests; then same-host 257² high32 direct oracle and fixed-query benchmark, followed by image training only if the direct comparison justifies it. Prior work: elementary line-segment/halfplane intersection; not a general polygon-kernel enumeration or arbitrary-map representation theorem.

Route C PARDISO baseline card. Question: Is the observed 0.44–0.57 s all-edge 257² direct-solve bottleneck primarily the chosen CPU sparse factorization, and can a symmetric sparse direct implementation with reused factors make Route C competitive without changing the positive-conductance family or topology theorem? Exact test: assemble the **same** 257² SPD Dirichlet matrix and two-coordinate right-hand side from the existing all-edge Schur layer; compare SciPy SuperLU to the installed Intel MKL PARDISO wrapper on fixed deterministic conductance logits. Separately time assembly, analysis/numerical factorization, two-RHS forward substitution, and a same-factor adjoint solve; verify true residual, map difference and all original face areas. Repeat with varied logits to reveal whether symbolic analysis is reused. Assumptions: float64 CPU, same matrix/weights/boundary, four threads, no editing global environment or terminating jobs; treat PARDISO as an optional dependency and distinguish library speed from a novel mathematical solver. Falsifiers: import/ABI issues, incorrect symmetric-storage convention, residual/topology disagreement, no repeated-call gain, or excessive RSS. Smallest test: 65² correctness, then 257² same-host three repeats with explicit setup and VJP solve timings. Prior work: Intel oneMKL PARDISO phases and the installed PyPardiso wrapper; its public `factorize` may rerun analysis for every changed matrix, so symbolic-reuse claims require direct verification.
# Route C bounded-conductance sine-preconditioner card. Question: Can an all-edge positive-weight Tutte layer on the fixed southwest-to-northeast structured triangulation avoid a new sparse factorization at every latent while retaining accurate first-order VJP and certified returned P1 topology? Exact claim: write the Dirichlet energy as horizontal plus vertical plus northeast-diagonal squared differences with conductances in [c_min,c_max]. Let P be the unit-weight horizontal+vertical five-point Dirichlet Laplacian. Since the diagonal difference is the sum of one horizontal and one vertical difference, its squared energy is at most twice the corresponding five-point energy; on this fixed diagonal orientation, the chosen paths use every horizontal/vertical edge at most once. Hence c_min P <= A(c) <= 3 c_max P in Loewner order, so preconditioned CG has condition number at most 3 c_max/c_min independently of mesh side. P^{-1} is evaluated by two zero-Dirichlet DST-I transforms, not a periodic torus FFT. The *exact* equilibrium with convex fixed boundary and positive edge weights is a Tutte homeomorphism. A finite-tolerance PCG output is not automatically that exact map; the implementation must independently check all original faces and fixed boundary, true residual, and directional VJP. Assumptions: complete structured triangulation, bounded strictly positive symmetric edge weights, float64 for stringent solves, and a PCG stop rule based on true residual. Falsifiers: a small random positive-weight matrix violating the spectral bound, DST inverse disagreeing with sparse direct, poor 257² convergence/time, negative returned face or nonfinite VJP, or a gradient mismatch beyond the measured solve tolerance. Smallest decisive test: 9² compare P inverse, matrix action, and exact sparse solve; then 257² CPU/GPU forward/VJP and condition-number/iteration measurements with separately reported topology and residual. Prior work: standard Dirichlet sine diagonalization and spectral equivalence of graph energies; novelty, if any, is the certified neural-layer combination, not PCG itself.
# Route C sine-PCG bounded-weight expressivity card. Question: After removing the all-edge solve bottleneck, can bounded positive symmetric conductances on the 257² fixed triangulation represent the high32 synthetic target to accuracy competitive with the explicit decoder? Exact test: optimize three complete edge-logit arrays directly against the analytic target's control-vertex coordinates, with (c_e=1+15\sigma(z_e)\in(1,16)), and solve each exact equilibrium to true relative residual <=1e-10 using the sine-PCG layer. This is target-coordinate oracle supervision and must not be presented as image-to-latent training. Assumptions: same fixed convex boundary, full edge family, float64, original-grid P1 face certificate, fixed first target item, declared Adam steps and learning rate. Falsifiers: map RMSE plateaus above the A4/A6 target scale, the returned P1 map folds, solver or gradient fails with the wider weight range, or the residual stops meeting tolerance. Smallest decisive test: 129² short fit, then 257² 300/1000 steps if stable; compare with sampled-target P1 geometry. Prior work: the earlier 97² bounded conductance LP demonstrates feasibility for another sample/range, while the 257² LP timed out without a conclusion; gradient fitting is a practical representation diagnostic, not a feasibility proof.
# Route C local conductance-ratio obstruction card. Question: Is the high32 item29 failure under all-edge (c\in[1,16]) due to a provable local weight-ratio obstruction rather than optimizer failure? Exact necessary condition: at each interior vertex with mapped neighbor vectors (d_j=Y_j-Y_i), equilibrium is (sum_j c_jd_j=0). For any unit direction (p), let (P=\sum_{p\cdot d_j>0}p\cdot d_j) and (N=-\sum_{p\cdot d_j<0}p\cdot d_j). If every (c_j\in[1,R]), then (P\le RN) and (N\le RP), so (R\ge\max(P/N,N/P)). A single direction/vertex with lower bound >16 rigorously excludes the target from the *bounded* symmetric family, regardless of solver or optimizer; a bound <=16 is inconclusive because two coordinate equations and symmetric edge sharing remain. Assumptions: exact same six-neighbor graph and sampled target coordinates; positive P,N; finite angle sampling only gives a lower bound on the supremum. Falsifier: re-evaluation with independently formed neighbor vectors does not reproduce a claimed >16 witness. Smallest test: check 360 declared directions on all 255² strict interior vertices for train32 item0 and item29, compare with achieved PCG oracle geometry and target P1 orientation. Prior work: scalar projection of positive barycentric equilibrium and elementary interval bounds; not a complete conductance feasibility test.
# Route C high-frequency oracle-loss card. Question: Does the all-edge bounded-conductance oracle miss the 32-cycle component because plain coordinate MSE underweights its derivative-scale error, or because the positive-weight equilibrium family cannot realize it? Exact test: retain the same item29, [1,16] conductance parameterization, sine-PCG solver and Adam update count, but add a declared target-dependent horizontal/vertical finite-difference Jacobian loss weighted by beta to the coordinate MSE. This is an *oracle diagnosis* only; no target geometry may enter the subsequent image neural-layer training. Assumptions: original 257² control grid, true residual and face check on every returned iterate, same initialization and optimizer settings; report both the optimization objective and the unweighted coordinate RMSE plus projected fine amplitude. Falsifier: fine amplitude and derivative agreement remain poor despite a stable, sufficiently weighted derivative objective, motivating a genuine global conductance-feasibility investigation rather than more plain-MSE updates. Smallest test: beta=0.001 and 0.01 at 500 updates each, compared with the saved beta=0 result. Prior work: standard Sobolev supervision and the measured local positive-weight ratio bound; this does not prove symmetric global feasibility.
# Route C sine-PCG image-to-latent card. Question: Can the fast all-edge bounded-weight equilibrium be used as a practical 257² image-conditioned neural layer, and does an image-only residual-gradient objective recover any high32 detail without analytic target geometry? Exact test: reuse the two-scale edge-midpoint CNN and deterministic high32 fixed/moving image generator, replace only its full-edge solve with the float64 sine-PCG layer and bounded weights [1,16], then train 300/1000 Adam steps on the declared first training pair. Predict logits for the three edge directions, including inert boundary-boundary positions, and query the returned original-grid P1 map at 512² image pixels. Compare pixel MSE-only to pixel MSE plus a predeclared weight on one-pixel finite differences of the warped-minus-fixed image residual; analytic target maps and derivatives are *evaluation only*. Assumptions: GPU2 idle, batch one, same initial CNN, forward/adjoint residual <=1e-10, represented faces checked on every solve, and all wall/memory terms recorded. Falsifiers: ineffective image loss reduction or high32 amplitude, disproportionate FFT/warp memory, nonzero residual/face failures, or unstable image-network gradients. Smallest test: 17²/64² network smoke then 257²/512² one-pair training; if one-pair cannot fit, do not imply held-out generalization. Prior work: existing all-edge Schur/PARDISO one-pair baseline and the target-coordinate Sobolev oracle; this test isolates the new solver's practical training cost, not a new guarantee from image data.
# Route C direct image-latent diagnostic card. Question: The bounded all-edge sine-PCG CNN produces almost no 32-cycle amplitude even with a residual-gradient image loss; is that a limitation of the image objective or of amortized CNN inference? Exact test: on the same declared first high32 training pair, initialize every fine-edge logit at zero, then optimize those logits directly from the fixed/moving images via the same 257² equilibrium, one final 512² image warp, pixel MSE with or without the same residual-gradient term, and Adam. Target coefficients/maps remain unavailable to the optimizer and enter only a post-fit metric. This is per-instance latent fitting, not fast image-to-latent inference or held-out prediction. Assumptions: conductances stay in [1,16], solver true residual <=1e-10, all original faces positive, equal image samples and query resolution, predeclared learning rate/steps. Falsifier: fine amplitude remains small and map error high even when direct latents substantially reduce image error; that leaves image ambiguity or restricted conductance geometry rather than only the CNN architecture. Smallest test: 17²/64² smoke and one 257²/512² 500-step direct run with residual-gradient weight 100, contrasted with the 300-step CNN run and the target-coordinate oracle. Prior work: standard per-instance versus amortized optimization diagnosis; it is not a deployable neural layer.
# Route C contextual edge encoder card. Question: Direct image-only fine-edge optimization recovers much of the high32 target but the shared two-convolution encoder does not; does a modest multiscale context body remove that amortization bottleneck at 257² without changing the fast positive-conductance decoder? Exact test: replace only the existing 5-pixel-receptive-field CNN body by three pooled context scales with skip fusion, retain the same coarse/fine edge-midpoint heads, bounded [1,16] sine-PCG decoder, image pair, 512² query table, optimizer and 300/1000-step image-only loss. Report extra parameters and full forward/VJP/memory. Assumptions: same one-pair train32 item0, identical image-only loss, no target-map input, no checkpoint warmstart, same device. Falsifiers: projected 32-cycle amplitude stays near zero or image/map/QC gains are outweighed by cost. Smallest test: 17²/64² shape/VJP smoke, then 257²/512² 300 steps; continue to 1000 only if evidence suggests optimization has not plateaued. Prior work: standard multiscale CNN context and saved direct-latent image diagnosis; this is a model-capacity ablation, not a new topology theorem.
# Route C displacement-to-conductance forward-synthesis card. Question: Can a desired dense displacement be converted to positive all-edge conductances in O(N²) local/cumulative work, avoiding hundreds of per-instance inverse-optimization steps while retaining the exact Tutte topology theorem and fast sine-PCG solve? Exact first-order construction: for unit-square source X and uniform base c0, let desired Y=X+u with u zero on boundary and let r=L(c0)Y=L(c0)u on interior. Keep diagonal conductances fixed. For each interior row, set a horizontal increment field by δc_h(i,j)-δc_h(i,j-1)=r_x(i,j)/h; for each interior column set δc_v(i,j)-δc_v(i-1,j)=r_y(i,j)/h, with arbitrary row/column additive constants chosen to center the increments. Then L(δc)X=-r exactly on interior, so L(c0+δc)Y=L(δc)u: the remaining error is the nonlinear product of conductance and displacement changes. Clip bounded conductances to [c_min,c_max] only as a declared positive parameterization; this adds an independently measured residual. Recompute r=L(c)Y and repeat the same increment correction a few times as a fixed-point heuristic, not a guaranteed convergence theorem. Assumptions: structured three-direction graph, original-grid P1 desired map with fixed boundary, side N, positive post-clamp edge weights, and sine-PCG final equilibrium face/residual checks. Falsifiers: a direct exact-arithmetic 9² check of L(δc)X fails, clipping is severe at 257², iteration diverges, returned map misses high32 detail, or VJP through cumsums/PCG fails. Smallest test: 9² algebra test and VJP; 257² target-coordinate oracle synthesis for train32 item0/item29, comparing no-correction and 1–3 correction passes to direct-latent optimization. Prior work: discrete divergence/flux integration and linearization of weighted harmonic coordinates; this is a proposed explicit inverse-conductance initializer, not a general exact inverse of Tutte.
# Route C row-gauge shift card. Question: Does the arbitrary constant in each integrated horizontal row and vertical column cause avoidable positive-weight clipping in the displacement-to-conductance synthesis? Exact claim: adding a constant to every horizontal edge conductance in one interior row does not change (L(\delta c)X) at its strict interior vertices because (X_x) has equal and opposite differences to left/right; analogously a constant in a vertical column does not change (L(\delta c)X) for (X_y). For a raw row range [m,M] and permitted interval [c_min,c_max], choose the shift nearest zero inside [c_min-m,c_max-M] when feasible; otherwise use the midpoint of that interval to minimize symmetric overflow. This preserves the first-order cancellation before clipping and minimizes clipping from the additive gauge, but it need not minimize the nonlinear (L(\delta c)u) residual. Assumptions: fixed boundary coordinates and the same NE-diagonal graph, only interior rows/columns shifted. Falsifiers: algebra check fails, clipping does not decrease on item0/item29, or mapped geometry worsens enough that avoiding clipping is not useful. Smallest test: 9² cancellation/VJP regression and 257² item0/item29 one-to-three-pass oracle comparison against the centered-gauge records. Prior work: freedom in one-dimensional antiderivative and bounded interval projection; no general exact inverse-weight theorem.
# Route A-to-C image-conditioned bridge card. Question: If a trained A6 image layer provides a dense, boundary-fixed P1 displacement, can one forward conductance-synthesis pass and a single sine-PCG equilibrium preserve its high32 image/map/QC quality while producing a distinct positive-weight Tutte output with manageable full VJP? Exact test: load the fixed A6 image checkpoint, compute its original-grid 257² map from fixed/moving images, apply one explicit [1,16] conductance-synthesis pass with the range gauge, solve the complete fine-grid weighted equilibrium, and evaluate both A6 and resulting C map on the same 512² image queries and original face-centroid Beltrami metrics. Then differentiate the C image loss through synthesis, PCG and the A6 encoder on a batch, measuring forward/VJP and peak memory. The intermediate A6 prediction is used as an image-derived candidate, not as a target map or post-hoc fold repair; the final topology guarantee comes from the positive-weight C solve plus face check. Assumptions: frozen pretrained A6 encoder for initial comparison, same heldout8 and 32/8 protocol, image-only loss for any subsequent fine-tuning, zero-boundary synthesis condition and true solver residual. Falsifiers: severe quality loss or clipping, nonfinite/zero gradients, or end-to-end cost exceeding the desired layer budget. Smallest test: 17² synthetic bridge VJP, then 257² heldout8 no-training comparison and one batch full VJP; only fine-tune if the bridge is viable. Prior work: A6 spectral photometric hint and the new linearized conductance synthesis; this is a hybrid diagnostic, not an independent Beltrami solver.
# Route A-to-C spectral-cleanup card. Question: The direct A6-to-C bridge clips 6–45% of weights and loses image quality because row-integrated conductances react to noisy high-frequency components of the A6 map; can filtering the **whole image-derived displacement** in a bounded zero-boundary sine basis retain the target's 32-cycle mode while reducing conductance range? Exact test: before synthesis, take the A6 map minus identity on 255² inner vertices, keep the top K vector DST-I coefficients for fixed K in {16,32,64,128}, insert zero boundary, and feed this declared candidate to the same one-pass range-gauge [1,16] conductance synthesis and PCG. Compare A6, direct bridge, and filtered bridge on identical heldout8 image/map/face-μ metrics, clipping and full VJP; do not choose K on the heldout set and then call that same set independent validation. Assumptions: no target geometry in filter selection, fixed pretrained encoder, Fourier ranking only by image-derived displacement energy, final C topology independently certified. Falsifiers: clipping persists or filtered candidate/returned map lose the 32-cycle component and image accuracy. Smallest test: four K values on train32 item0/first small training batch to select one, then one heldout8 evaluation and a fresh seed if a candidate appears promising. Prior work: zero-boundary spectral processing from A6; this is a targeted repair to the measured bridge failure, not a generic smoothness prescription.
# Route A7 spectral residual-feedback card. Question: Can one or two additional image-conditioned, zero-boundary sine-filtered photometric proposals recover the missing 32-cycle component of A6 on the **same original 257² P1 grid**, without global linear solves, repeated image resampling, or near-folding? Exact construction: start from the saved image-trained A6 P1 map F0. At each feedback pass k, evaluate fixed/moving image residual and gradient at current Fk, solve the local ridge 2x2 photometric normal equation, keep the top K joint DST-I vector modes, multiply the resulting finite logits by gain eta, and apply the same four-color radial safe vertex decoder to Fk. Hold a shared per-sample face-area floor equal to a declared fraction of the initial F0 minimum double area, so every pass must retain positive represented faces in exact arithmetic and observed float32. This is a recurrent image-to-latent forward layer, not a descent-gradient line search; the final map remains P1 on the *original* mesh. Assumptions: trained A6 checkpoint fixed for first test, K and gain selected only on train32, full face and boundary check at each pass, no target map in updates or selection loss except after-the-fact metrics, and measure complete forward/VJP and peak memory. Falsifiers: no fine-amplitude or image gain, Beltrami/maximum |mu| worsen, area margin collapses, or extra-pass cost exceeds benefit. Smallest test: side9 shape/VJP and four-color positivity, then train32 sweep of 1/2 extra passes, gain {0.25,0.5,1}, K16 with floor fraction0.2; choose one candidate under area/|mu| constraints and evaluate heldout8 plus fresh16. Prior work: A5 unfiltered repeated feedback produced near-folding and worse QC; A6's 16-mode hint improved single-pass alignment, so spectral feedback is a targeted difference.
# Route A7 end-to-end feedback-training card. Question: Frozen one-pass spectral feedback improves high32 image/map error on two fresh seeds but can increase maximum |mu|; can full first-order training through the feedback pass and an original-face QC tail penalty improve the accuracy/distortion tradeoff while retaining an original-grid P1 hard-topology decoder? Exact test: create one reusable decoder wrapping the saved A6 base, initial K16 photometric hint and exactly one extra K16 spectral residual hint with gain0.25 and an initial-map area floor fraction0.2. Warm-start its image encoder from the predeclared A6 QC045 checkpoint, then train the same 32 image pairs for 1000 Adam steps using pixel image MSE plus (0.5\operatorname{mean}(|\mu|-0.45)_+^2), with no analytic target map in the objective. Evaluate heldout8 and new untouched seed16 only after training, reporting face checks, map/μ error, high-frequency projection and full forward/VJP/peak memory. Assumptions: fixed hyperparameters inherited from train-only A7 selection and earlier QC ablation, batch2, 257² controls, 512² image queries, float32, true image resampling only once at final map. Falsifiers: no heldout/fresh benefit, maximum |mu| remains extreme, area margin shrinks, or backprop through spectral top-K/colored safety becomes unstable. Smallest test: side9 decoder shape/VJP/face check, then 257² 1000-step run; compare to frozen A7 and A6 QC045 rather than a different dataset. Prior work: A6 and its moderate QC-tail penalty, plus frozen A7 feedback measurements; this is neural-layer optimization, not target-coordinate oracle fitting.
# A8 research card（2026-09-23）：面 Beltrami 上界下的局部谱反馈

- Question：A7 增大反馈 gain 会改善图像误差，但恶化最大 Beltrami 模长；能否用原网格 P1 面 Jacobian 的解析分解，在每个四着色局部顶点更新时直接给出安全步长，使更强建议仍保留逐面 QC 上界？
- Exact claim：若输入 P1 map 固定凸矩形边界、所有面正向，且每面 \(|\mu_t|<\kappa<1\)，对一个顶点建议位移 \(d_v\)，以 \(J_t(s)=J_t(0)+s\,d_v\otimes\nabla\phi_v\) 定义 \(\alpha_t(s)\) 和 \(\beta_t(s)\)。设 \(m_t=\kappa|\alpha_t(0)|-|\beta_t(0)|>0\)，\(h_t=\kappa|\alpha'_t|+|\beta'_t|\)。令 \(s_v=\min(1,\eta\min_{t\ni v}m_t/h_t)\)，\(0<\eta<1\)，则所有被更新面满足 \(|\mu_t(s_v)|<\kappa\) 且正向；四着色后整个原网格 P1 homeomorphism 保留此 QC cap。
- Assumptions：源网格是指定 SW-NE 规则三角剖分；同色内点不共享三角形；边界固定；输入已经满足 cap；有限实数和精确算术。若输入只满足正向而不满足统一 cap，不能声称该步骤能补救或保持固定 cap。
- What would falsify it：某个满足条件的 5²/9² 输入或随机方向，经该步后存在 \(|\mu|\ge\kappa\) 或有向面积非正；解析 VJP 与非切换点中心差分不符；在 257² 上收益因保守界几乎消失。
- Smallest decisive test：先以单三角形验证 \(\alpha,\beta\) 与有限差分的 Jacobian；再在 9² 随机已认证映射上强 logits 测每面 \(|\mu|\)；最后用固定 A6 QC checkpoint 在 257²、128 fresh 高细节样本比较 gain=0.25/0.5/1、同一 κ 和端到端 VJP。
- Prior work：quasiconformal 的复导数分解 \(\alpha=f_z,\beta=f_{\bar z}\) 与 \(\mu=\beta/\alpha\) 是标准公式；A7 四着色安全面更新与局部光度反馈已实现。本卡主张的是本代码路径上的可计算保守界与实测，不声称新数学定理。
# A8 scaling research card（2026-09-23）

- Question：两次 QC-safe 谱反馈虽然在 257² 准确，但它的面/颜色中间张量是否使 513² 或 1025² 控制网格的完整神经层 forward/VJP 显存难以接受？重算中间量的 checkpoint 能否在不改输出/梯度的条件下降低峰值内存？
- Exact claim：普通 autograd 的前向/VJP 算法时间与内存随控制顶点数至少线性增长；可按颜色或 pass 重算局部非线性中间量，以额外 forward 代价换低峰值，但必须比较完全相同输出与一阶 VJP。首先实测未经优化的真规模，不凭小网格外推。
- Assumptions：AI 主机 GPU 2 空闲；图像固定 512²，控制侧分别为 257²、513²、1025²；同一个 untrained CNN/decoder 结构只测系统规模，不将不同控制侧的误差差异解释成训练效果。
- What would falsify it：1025² 的 batch1 OOM；时间/内存远超顶点数比例且无可解释工作区；checkpoint 改变输出或参数梯度；峰值实际由常驻数据而非 autograd 图主导。
- Smallest decisive test：batch1/2 257² 与 batch1 513²/1025² 的热身后同步 GPU 全 forward/VJP；准确记录是否包含图像 query 与 warp、峰值 allocated/reserved；再只对瓶颈处试局部重算。
- Prior work：A4 1025² 原网格安全更新和 C 正弦-PCG 1025² 已分别测过；A8 新增两次光度 normal-equation + DST + QC 面门控，不能直接沿用 A4 的内存数字。
# C multi-sample research card（2026-09-23）

- Question：有界全边正 conductance + 正弦-PCG 已在 257² 完成 fwd/VJP 和单图像对训练，是否能在与 A8 同一 high32 的 32 训练/8 保留协议下学习图像到 conductance，而不是仅对单例优化？
- Exact claim：SinePreconditionedTutteLayer 在输出正权重与指定凸边界时给数学上的 Tutte P1 homeomorphism；有限精度训练要逐次控制真实残差并在最终所有面检查。由 edge-midpoint CNN 产生全部横、纵、对角边 logits，图像 MSE 经解码和一次 image warp 反传到 CNN。实验检验泛化及计算成本，不预设精度能追上 A8。
- Assumptions：规则 SW-NE triangulation，控制侧 257²，图像512²，正 conductance 在 [1,16]，float64 PCG，batch2，训练32/测试8，同一高32生成器、图像损失、1000 Adam 更新。网络参数量与 A8 不同，比较应注明。
- What would falsify it：VJP 非有限、残差超过容差、面翻转、held-out image MSE 接近初始不下降、细频振幅接近零、或耗时/内存失去可训练性。即使失败也不否定正权重表示本身，因为已有直接边 latent oracle 能拟合目标。
- Smallest decisive test：先 2 步远程 smoke 与真实残差/面积，接着 300 步看下降趋势，再 1000 步与 fresh 128 评估；如果 300 步完全无信号，检查梯度头范数和训练图像的对比度后再决定是否继续。
- Prior work：09_sine_pcg_and_conductance_synthesis.md 已给 257² direct solve/VJP、单例 image-to-latent、目标坐标 oracle 和失败的 A6 位移到正 conductance 桥接；此处只补多样本泛化，不重复证明。
# C teacher-distillation research card（2026-09-23）

- Question：多样本 C 的细频振幅几乎为零；如果把已训练 A8 从相同图像产生的安全 P1 map 当作训练期教师，C 的 edge CNN 能否 amortized 地学到细频正 conductance，还是其表达/架构依然阻断？
- Exact claim：保持 C 正 conductance 区间、正弦-PCG、图像输入和推理路径不变；仅在训练 32 例的损失增加教师 P1 坐标在水平/竖直细边上的差分 MSE，教师不用于测试前向。正权重保证仍来自 C 解码器而非蒸馏。如果细频振幅仍为零，这排除“纯像素损失没有显式梯度信号”这一单独解释，但不证明所有边权编码器都不可能。
- Assumptions：A8 教师检查点固定、只从训练图像预计算顶点图；不使用解析目标或测试图像标签；λ_teacher_grad 先固定 0.01，与此前 C 目标坐标 oracle 的差分 MSE 量纲一致。评价同 8 保留和 fresh128。
- What would falsify it：教师监督使 C 细频振幅恢复显著且图像/μ 不恶化过多；反之若目标梯度损失下降但细频仍零，要检查 teacher edge-gradient 数据、CNN 输出谱和实际 C 映射误差，而非直接宣布结构不可能。
- Smallest decisive test：两步 smoke，训练 300 步检查 teacher edge-gradient loss 和 fine projection，再 1000 步/保留集；必要时只改变一次 λ，不做广泛无控制扫描。
- Prior work：A8 257² 32/8 image-only 教师已验证；C 单例 target-map oracle 加细边导数 MSE 可恢复细频；纯 C 多样本图像/残差梯度损失均几乎不恢复 fine 模式。
# C multiscale Fourier-edge research card（2026-09-23）

- Question：C 的 image CNN 在 32 周期 conductance Fourier bins 中几乎没有能量，而目标坐标 oracle 的正边权在相同 bins 具有显著能量。显式提供一个多频正弦 edge basis，并让图像网络预测每个频率的边权系数，能否在保持所有边权正、同一细网格 PCG 和小显存的同时恢复细模式？
- Exact claim：取固定频率集合 {1,2,4,8,16,32}，在每类边定义相应 sine/cosine 连续基函数的边中点采样；CNN 输出每样本 3K 个谱系数，作为原 coarse/fine 边 logits 的加项。所有最终边权仍是 1+15 sigmoid(logit)，故精确 Tutte 解的拓扑条件不变。比较应标明这个 basis 包含目标已知的频率 32，是有利的任务先验，不是通用 Beltrami 输入。
- Assumptions：32/8 high32 多样本图像、257²/512²、同一正弦-PCG、学习率/步数；CNN pooled features 与频率相关 pooled features预测系数，无目标 map/μ 监督时为 image-only 层。
- What would falsify it：该 head 无梯度或模式系数能变大却 P1 映射细振幅仍零；若 teacher-gradient 训练也不能恢复，则至少当前基方向或图像幅度估计仍错误。若成功，也只证明带已知模式字典的受限目标族可行，需再测试频率/相位偏移和真实图像。
- Smallest decisive test：小网格张量形状/VJP；257² 300 步 image-only，看 C 图像、map、32 频谱；若无信号再一次 300 步 teacher-gradient，并直接优化 18 个 Fourier edge 系数拟合目标坐标，区分字典容量与 CNN 推断；之后仅对有改进者做 1000/fresh。
- Prior work：c_edge_spectrum_trainitem29.json 中直接边 latent oracle 的 32×32 Fourier bin power fraction 为水平约0.21、竖直约0.40，而多样本像素/图像梯度/教师 CNN 均低于 2e-5。这个观察支持针对性改变表示，而不继续只调 loss。
# A8 photographic-content research card（2026-09-23）

- Question：A8 在合成正弦/高斯纹理上准确，但其 image-to-latent 网络和光度 normal equation 对不同纹理是否仍有用？在保持相同 257² 控制网格及已知 high32 目标 map 的同时，仅将图像内容换成软件包随附的真实摄影/扫描灰度图，测试固定检查点的内容域外表现。
- Exact claim：使用 camera、coins、moon、page、grass、gravel 这六张本地 scikit-image 示例灰度图，缩放/标准化为512²；按与训练 high32 相同的系数范围构造解析真 map，令 fixed=warp(moving,true_map)。A6/A7/A8 检查点不再更新，报告同一照片和形变实例的 image MSE、query-map RMSE、面 μ RMSE、最大 μ、最小面积及 A8 初始 cap 前提成立比例。它是“真实图像内容+合成已知形变”，不是临床真实配准。
- Assumptions：示例图像来自已安装库，不下载外部数据；预处理只用图像自身统计，不使用目标 map；真实 map 仅用于 fixed 生成和事后评价。照片 ID 与系数配对在所有模型完全一致。
- What would falsify it：A8 图像损失虽低但 map/μ 大幅恶化、摄影平坦区出现 cap 输入违反、或性能对某类纹理极不稳定。这会限制此前合成纹理结论的适用范围，不能通过剔除困难图像掩盖。
- Smallest decisive test：每图至少 16 个系数变体，共 96 例，batch2 同源 GPU；对 A6、A7、A8 逐面测全部131072面。若差异大，再分析按图像类型的表，不训练到测试集。
- Prior work：high32 合成纹理测试是训练同分布；A8 真实摄影输入的泛化未测。scikit-image 随包提供这六张固定示例数据，仅用作内容域变化试验。

# A9 initial-QC homotopy card（2026-09-23）

- Question：A8 的 QC-safe feedback 只保持输入已有的 |μ|<0.8，而摄影内容域外测试中 96 例仅 66 例满足该前提。能否用一个可微的全局 latent 相关标量使初始 P1 map 无条件进入该 QC 集，再调用已证明的局部 QC-safe feedback？
- Exact claim：令固定单位矩形原网格的恒等 P1 map 为 I，任意初始顶点图为 F，每面复导数记 α=F_z、β=F_{\bar z}。考虑原网格 P1 顶点同伦 H_t=(1-t)I+tF，于是 α_t=1+t(α-1)、β_t=tβ。对每面二次多项式 q(t)=κ²|α_t|²-|β_t|²，q(0)=κ²>0。求它在 (0,1) 的最早实根，跨全部面取最小值，乘安全系数 η<1，并和1取小者，则所有面有 |μ(H_t)|<κ；固定边界不动，故输出为原网格 P1 homeomorphism。该缩放只是 A8 可选的初始 QC 约束，不是主要拓扑保障或求逆求解器。
- Assumptions：F 边界保持恒等；源三角剖分一致；精确实数理论；有限精度还需独立全面证书；η=0.99、κ=0.8；从零到所选 t 的每面 q 连续且不越过最早根。若 q(1)>0 但在中间曾越界，也必须找到最早根，不能仅检查端点。
- What would falsify it：任何面最终 |μ|≥κ、非正面积、错误求根、边界运动、梯度与中心有限差分不符，或大幅削弱真实图像上的精度使实际价值很低。
- Smallest decisive test：side9 人工强扭曲与随机 A8 输出，逐面 q 和 VJP 对照；冻结 A8 在同一 96 个摄影内容实例上对比可选 homotopy 的 cap、map、image、时间；若尚有价值再做 257² 训练评估。
- Prior work：平面 Beltrami 的 α/β 表示及二次不等式、A8 局部 QC-safe 更新；不主张该同伦是文献新定理，价值只在具体神经层实现和成本。

# A8 one-pass Pareto card（2026-09-23）

- Question：A8 两次安全谱反馈虽准确，但 batch2 完整 forward/VJP 为29.55/62.62毫秒、峰值657MB。一次更强的 QC-safe 反馈是否能以更少时间/显存达到相近的 image、map 和细频质量？
- Exact test：从同一个 A6 QC045 检查点初始化 encoder，保持32/8 high32 图像、257²控制、512²查询、1000次 Adam image-only、QC cap0.8、area floor0.8、每次谱top16不变；仅把反馈次数从2改为1，并分别试 gain1与gain2。训练后逐面评价 Beltrami、面积、初始cap合格数和细频振幅，在 fresh128 种子939031交叉检验最优者。
- Assumptions：两组与 A8 参考共享设备GPU2和同一数据生成及 batch2；反馈减少但构造拓扑保证不变；cap 前提仍需单独记录；计时须包含 CNN、所有query、一次最终图像采样和VJP。
- What would falsify it：单次反馈细频明显不足、最大 μ 或面积退化、无法减少训练墙钟或 VJP 时间，或者保留集收益不能在 fresh128 重现。
- Smallest decisive test：side9 一次 pass VJP/逐面已有测试；直接两次1000步同源GPU运行并保存逐例数组；如果两者明显差再考虑是否需要新的融合策略。
- Prior work：A7 单次 gain0.25 无硬 cap，A8 两次 gain1硬cap；更强一次反馈尚未在同一协议下测过。

# Cross-route photographic-content card（2026-09-23）

- Question：Route B 的精确两层257²组合与 Route C 的正边权正弦-PCG在同一真实摄影内容+合成已知形变数据上如何表现？与已测试A8相比，是否有不同的图像/map/μ/面积取舍？
- Exact test：冻结训练完的 B 双细层 image-only、C 普通 edge CNN、C 六频 edge CNN，以及 A8 检查点；96例六种摄影灰度内容、种子973031、高32解析形变，512²图像、257²原控制网格、batch2同GPU，报告 image/query-map、源面重心链式μ、全部原面或两个因子全部原面最小面积和最终拓扑的表示类型。
- Assumptions：B 的输出是两个各自认证的PL homeomorphism精确复合，源面重心链式Jacobian只是误差采样，不能把512²查询插值看作新的有保证P1；C 和 A8 是原网格P1。B的几何误差基于复合的导数，C/A8基于原面常值导数，基准可比较但数值采样方式需标出。
- What would falsify it：模型装载或复合顺序错误、B任一因子面翻转、C真实残差超界、评价图像不同源、或者仅以photo image MSE掩盖map/μ差异。
- Smallest decisive test：每路线一个双样本smoke，与各自原 high32 检查点值核对；再96例一次性评价，按照片分组，以便定位纹理差异。不得重新选择训练配置以迎合此测试。
- Prior work：02、09、11、13的合成纹理训练和14的A6/A7/A8摄影输入评估；本试验仅统一内容域外对照，不把照片+合成形变称为真实临床配准。

# Route C photometric spectral response card（2026-09-23）

- Question：六频 conductance 字典可用18个直接几何 oracle 系数产生32周期目标，但CNN从图像只恢复约1–3%振幅。能否把每个边系数在单位矩形均匀正边权解附近的映射响应预计算，用显式图像光度法小型正规方程估计系数，形成不依赖全边CNN的快速可微 image-to-latent 层？
- Exact formulation：边 logit 为 z_e=z_0+Σ_{k=1}^{18}a_kφ_{ke}，c_e=1+15σ(z_e)>0；平衡解Y(a)定义为 A(c(a))Y_I=b(c(a))。在a=0预计算响应 V_k=∂Y/∂a_k，可由中央有限差分两次高精度PCG数值逼近，并用更小步长检验收敛；运行时把 V_k 插值到512²查询点，设 r=I_f-I_m、G_k=∇I_m·V_k，求 (GᵀG+λI)a=Gᵀr，再经同一个正conductance/PCG解生成最终原网格P1 map。用适当截断的a保持数值有界，正conductance保证精确平衡解拓扑；有限精度仍检查真实残差和所有原面。
- Assumptions：同一18基含已知频率32，是强任务先验；光度一阶近似只在目标位移小、图像采样可微区域近似成立；正规方程可能病态，需要记录条件数、λ、系数大小，不把小线性系统说成无成本。闭式层应可从图像损失向图像/可训练校准参数传VJP，若系数裁剪或分支切换则仅分段可微。
- What would falsify it：高频响应近零、中央差分不收敛、G的32周期列严重相关无法分辨、估计系数太大且真实解不改善image/map/μ、或者512²设计矩阵与小系统代价超过A8。若失败，仍能定位是几何响应、光度可识别性还是线性化问题。
- Smallest decisive test：side33响应差分与VJP/面积；257²预计算18响应，先一个预定train item29和heldout8的图像-only估计，再新摄影96例；真实目标只用于事后评价。先单步，不引入大规模网络或外部求逆。
- Prior work：Route C 六频 conductance oracle（13节）、Lucas–Kanade光度线性化与隐式映射灵敏度是已有数学思想；这里检验具体正边权神经层的工程可行性，不宣称通用 forward Beltrami solver。

# Route C photometric-calibration training card（2026-09-23）

- Question：128²光度正规方程+18预计算 conductance 响应在257²控制/512²图像上已能恢复约50%细频、完整VJP可算且约36/31毫秒；能否只学习18个模式增益，在真正32训练/8保留图像到latent多步训练中改善最终图像/map，同时保留较低μ误差和小显存？
- Exact test：固定响应、fit128、全细网格正边权PCG、mean ridge0.005，令解析估计a_k(I_f,I_m)乘以可训练正增益g_k=exp(θ_k)，θ初始0。最终边logit z=z0+Σg_ka_kφ_k，训练损失只为最后一次512²图像warp MSE，32训练/8保留、batch2、1000 Adam；目标map和真μ只用于报告。记录每模态增益、真残差、全部原面、fwd/VJP/显存、fresh128。与同架构未训练g=1直接比较，不能与C edge-CNN差异解读为纯solver收益。
- Assumptions：训练图像与已有high32协议完全相同；模式正增益不破坏c∈(1,16)但极大值可恶化条件数或面数值，故限制θ在有限范围且每步检查求解残差；显式频率32是任务先验。若非线性裁剪模式参数，普通一阶VJP只在不触发边界的区间成立。
- What would falsify it：梯度为零/不稳定、1000步图像损失未降、保留/fresh map与μ明显恶化、正PCG不收敛、峰值显存/时间无优势，或者训练仅把18增益推至边界却无法拟合目标。
- Smallest decisive test：129²/128²五步 smoke 检查有限VJP和面；257²先100步趋势，再1000步完整训练，独立8保留和新种子1170031比较。
- Prior work：本阶段C 18基的几何oracle与光度闭式诊断，以及结构化Tutte隐式VJP；18增益仅是低维校准，不宣称已经学习任意μ。

# Route C nonlinear photometric refinement card（2026-09-23）

- Question：18响应的一步线性化在合成图上可用，但摄影内容96例表现很差，fit分辨率从128增到256还更差。是否是恒等处一次光度线性化的位移/图像非线性导致？在当前映射处重新计算图像残差和采样图像梯度，复用固定几何响应，再做一到两步18维修正，能否改善图像/位置，尤其是摄影域外？
- Exact formulation：初值a0由128²正规方程得到。第t步求细网格正导纳平衡图Y(a_t)，在fit128查询p计算残差r_t=I_f(p)-I_m(Y(a_t,p))，图像梯度∇I_m(Y(a_t,p))，以原点预计算V_k(p)近似当前∂Y/∂a_k，解带同一ridge的18维正规方程得δa_t；设a_{t+1}=a_t+ηδa_t，η=0.5，再用真实非线性边权/PCG求图。每步的正导纳与全面数值面检查保持拓扑，但复用V_k是近似Gauss–Newton，不可称精确Newton或保证下降。
- Assumptions：输入图像与映射不超出单位正方形的规范坐标；梯度通过采样moving的中心差分近似，边界处用border；所有相同fit128、高32目标和既有train32/heldout8/photo96数据；先评估无学习增益版本，不因photo结果调十几个参数。
- What would falsify it：训练与摄影图像误差未降、map/μ明显恶化、PCG不收敛、每步额外时间过大，或第2步发散。若一阶固定V不够，可进一步考虑当前导纳处精确灵敏度，但要先量化成本。
- Smallest decisive test：side129双样本smoke；257² train32一次η0.5与两次η0.5；仅有改善的版本到heldout8和photo96，同一共用指标。训练参数仍为0时只是图像条件解析估计，不把它冒称训练好的CNN。
- Prior work：Gauss–Newton/Lucas–Kanade重线性化和本阶段的18模式解析响应；固定响应复用是一阶近似工程尝试。

Follow-up after train/photo evidence: two untrained refinement passes improved photographic image MSE from 0.0342 to 0.0177 but cost roughly three PCG solves; on heldout high32 two passes reached about the same image/map error as the 18-gain trained single-solve layer. One decisive hybrid check will start from the fixed 300-step learned mode gains and add exactly one raw-coefficient correction (not another learned gain multiplication), then test train32/heldout8/photo96. A large image gain at modest added cost would justify a hybrid; otherwise the single-solve calibrated layer remains the practical C candidate.

# Route C reusable module card（2026-09-23）

- Question：18模态校准训练目前通过研究脚本拼接函数完成；能否把它整理为一个可直接嵌入PyTorch网络的层，并保持与已发表JSON训练路径相同的输出、正面积和一阶VJP？
- Exact claim：PhotometricSpectralTutteLayer 接受同device float32固定/移动图像(B,1,H,H)，在prepare阶段预计算18个边模态及全细网格响应，forward返回(B,257,257,2)原网格P1顶点图，forward_with_latent另返回(B,18)系数。唯一训练参数为18个raw gains；同一图像/检查点下与脚本公式逐元素一致到float32预计算误差，完整image warp损失可反传到该参数和输入图像。层保持正conductance+固定边界，数值面证书由solver执行。
- Assumptions：prepare显式在目标device执行，边模态频率集合固定且低于Nyquist；fit128只是系数估计网格不是控制网格；响应buffer不持久存到检查点，加载权重后须prepare。检查点只有18参数，因此不能把预计算成本藏在训练计时中。
- What would falsify it：side67旧公式/新模块差异超过1e-5、257²旧/新差异大、反向梯度为零/非有限、正面积不成立、显存/计时明显增加或API未准备就静默输出。
- Smallest decisive test：side67所有基/响应及输出和VJP测试；257²加载300步参数，8保留中同一batch的旧脚本与新模块map逐元素比较，再完整forward/VJP、真实残差和面证书。
- Prior work：本阶段既有工具脚本的解析响应与正弦-PCG模块；这一步是可用API封装与回归核查，不称新算法。

# Route C million-control scaling card（2026-09-23）

- Question：18参数C光度响应层在257²上快而省显存，但其precompute要解18个全细网格响应、训练VJP要解前后伴随。是否能在513²/1025²真正**控制顶点**网格上完成batch1完整forward/VJP而不显存爆炸？257²学得的模式增益能否直接迁移分辨率而保持同一high32测试图像的几何质量？
- Exact test：固定已训练300步18增益检查点、同一8个保留high32图像和512²query、fit128、GPU2，分别新建257/513/1025控制层，逐层prepare各自边基与响应；只在257用训练过的几何规模，其余是零额外训练的分辨率转移。每规模检查batch1一个保留样本的完整图像loss forward/VJP、真实前向与伴随残差、全部原面最小面积、峰值allocated、prepare时间；在8例上统计image/map/面μ及细频振幅，明确query与控制规模。
- Assumptions：1025²有1,050,625控制顶点、2,097,152三角形，图像仍512²；18基系数、ridge、增益固定；每个规模均以自身均匀正边权解计算响应，故预计算成本按规模重付但可以跨样本复用。不要把已训练257²输出复制插值作为1025²精度。
- What would falsify it：1025² prepare/forward/VJP OOM、PCG达不到真实残差、面翻转、时间/显存不可接受、或同一增益跨尺度map/μ大幅退化。即使1025有正面结果也只是8例无再训练迁移，不是百万点训练泛化。
- Smallest decisive test：side513一个样本和VJP，再side1025一个；资源充足且均正确才扩到8例几何与速度统计。准备前检查GPU空闲及可用显存；不碰其他任务。
- Prior work：A8在1025²未训练扩展性已测，C正弦-PCG纯解码器1025²已测；本层新增18响应预计算和128²光度估计，不能沿用其时间/显存结论。

# Route C million-control multi-step training card（2026-09-23）

- Question：1025² 控制网格上 batch1 完整 forward/VJP 已经通过，但这不等于实际优化循环能够多步稳定运行。固定 18 模态解析图像到 latent 和 257² 已学 300 步增益作 warm start，是否能在真正百万控制顶点、512² 图像上继续进行 300 步 image-only Adam，并保持全部原面正向、有限梯度和可接受显存？
- Exact test：使用相同 train32 seed55101 / heldout8 seed99317 high32，1025²控制、fit128、image512、batch1，加载257²的18增益，用学习率0.01训练300步；每步完整求1025²正conductance平衡 map、512² warp MSE与隐式VJP，只更新18 gain。记录初末训练/保留 image MSE、真实求解/伴随残差最大值、每步面积检查、loss轨迹、整段时间、中位fwd/VJP与训练峰值已分配显存；几何细指标用独立评价脚本读新检查点补测。预计算和训练分开计时/显存。
- Assumptions：18已知频率是强任务先验，这只是同族继续训练，不是百万维自由 latent，也不是照片/临床泛化。优化器状态从空开始，故不是完全复现257训练轨迹；同一GPU2若中途被占用则改用空闲卡，不影响他人。
- What would falsify it：迭代残差/面积失败、梯度非有限、OOM、训练保留误差明显恶化、耗时太高或预计算再次不稳定。若仅保留误差不改善，也要报告百万点训练在计算上可行但无精度收益。
- Smallest decisive test：远程先10步并检查日志/资源，然后继续同一配置完整300步；不得把单个VJP当作多步训练。
- Prior work：17节257²增益训练及20节1025²零训练转移；这里独立补齐实际百万控制顶点优化的工程证据。

# Route C response-precompute memory card（2026-09-23）

- Question：1025²的18响应一次批量PCG预计算峰值约7.6GB，明显超过每步训练1.1GB。能否将响应按小块求解，立即投影到128²拟合 query 后丢弃全分辨率响应，在不改变最终映射和VJP的前提下降低冷启动峰值？
- Exact claim：18个响应满足同一个均匀正导纳线性算子作用于18个互不耦合的二维右端，因此批量求解与任意分块求解在精确算术中逐列相同。边模态基完整保留作运行时导纳；仅局部构造当前分块的导纳导数、右端、PCG轨迹和顶点响应，并将插值后的(块,128,128,2)响应存储。比较chunk=1,2,3,6,18 的precompute秒/peak allocated、响应查询max abs差、257/1025同检查点最终 map及VJP。不得把 GPU cache reserved 的变化冒称数学差异。
- Assumptions：同一float64 PCG真实残差阈值1e-10，每个分块允许略不同迭代停止/舍入；边基总量仍为O(18N²)，此优化只去除批量PCG的大中间张量，不能宣称O(N²)内存变为常数。
- What would falsify it：peak没有显著下降、时间超过可接受冷启动预算、响应/最终map偏差超浮点预期、梯度或拓扑发生改变。若18边基本身成为主峰，要测出而不是继续堆内存方案。
- Smallest decisive test：side257 chunk2 vs18响应/输出；然后1025 chunk2的GPU2峰值和时间；若顺利再扫描其他块大小，选无需模型精度调参的工程默认值。
- Prior work：同一多右端SPD系统可按列解是线性代数直接结论；本仓库20、21节给出7.46/7.60GB的实际冷启动峰值及完整训练低于1.1GB的对比。

# Route A8 million-control resolution-transfer card（2026-09-23）

- Question：Route A8 在257²同high32任务上优于C的image/map精度，1025²目前只有未训练零head扩展性测试。能否把257²已学卷积体与现有7级头按相同几何尺度迁移到1025²，再让新增细两级头保持零初始化，完成真实1025²的高频image-to-latent forward/VJP及必要时多步继续训练？这提供与C更有意义的百万控制顶点对照。
- Exact claim：ConvexQuadLocalImageEncoder的前7级head与shared body/local head权重张量尺寸不依赖side，故可装入1025²架构；新增级head保持该类的零初始化。输出仍由1025² HierarchicalConvexQuadLocalLayer和SpectralSafeFeedbackLayer在原网格构造，不能称只是257² map插值。每个最终原面需要检查正定向；A8的0.8 Beltrami cap依赖初始map已达标，不能因迁移自动声称硬0.8 cap。
- Assumptions：训练/保留high32数据、512²图像、32周期、相同A8设置；先无再训练评价8例，量初始cap资格、最终map/μ/面积与完整fwd/VJP/峰值。若有效，再以同图像损失对32训练样本继续300步，报告8例和新种子。新增级可能改变粗层参数语义，故装载成功不等于函数等价。
- What would falsify it：头索引尺度不对、映射出现非正面、显存超出空闲GPU、初始cap在大多数样本失败、精度远差于257²或训练不能改善。若失败，区分分辨率迁移问题和A8构造本身的问题，不以C成功替代A路线。
- Smallest decisive test：检查latent_sides和state_dict缺键仅对应新增头，GPU2上一个512²图像/1025²map的前向VJP和全面，再8例；仅前级正确才启动300步。
- Prior work：11/14节A8的257²训练、摄影评估和1025²未训练伸缩；22节C在相同控制规模的低显存实训作为对照。

# Route A8 physical-feature-scale diagnostic card（2026-09-23）

- Question：把257² A8权重直接用于1025²后，图像特征卷积核的物理尺度缩小四倍，保留image/map误差较原257²显著变差。若仅让共享体仍在257²物理特征网格运行，同时完整1025²解码、新两级head仍零初始化，精度是否大幅恢复？
- Exact test：同一已训练257²权重、1025²解码、8张heldout high32、512²图像，无再训练。基线encoder把图像resize到1025²再运行卷积；对照先resize到257²再运行同权卷积，前七级head直接从该feature插值取样，新增head零输出；local_head的257²logit bilinear提升到1025²后裁边。这只是改变feature采样尺度，不改变控制顶点数、decoder、训练权重或image查询。比较8例image/map/面μ/拓扑和cap资格、forward/VJP/显存。
- Assumptions：257²卷积输入尺度是原训练习惯，提到1025²的local logit会变平滑，故改善不代表细控制自由度已充分使用。若大幅改善，下一步才设计低频coarse-feature+真正fine-feature增量，避免只靠粗图重采样。
- What would falsify it：结果无改善或更差，表明零新增头/细层几何/光度提示等其他因素主导；仍需区别尺度失配的多个来源。
- Smallest decisive test：同一第0例基线与对照，检查前七级head逐元素与原257²网络一致；再同8例完整评估及一次VJP。
- Prior work：23节直接迁移损失，卷积有限物理感受野及已有ConvexQuadLocalImageEncoder的结构；这只是控制变量诊断，不称新的已训练layer。

# Route A coarse-to-fine exact P1 refinement card（2026-09-23）

- Question：A8从257²重新解码到1025²使保留image/map明显恶化。能否保留已学257²连续P1映射本身，利用网格嵌套把它**精确**细分为1025²原网格P1，再在这个安全base上进行一到两次由当前图像残差产生的细网格局部正向更新，从而既保持粗层质量又打开真实细控制自由度？
- Exact claim：257与1025顶点数分别是2^8+1和2^10+1，且两网格均采用相同SW–NE对角线。任一细三角形包含于一个粗三角形，故对粗P1 map在细顶点做**三角形重心插值**后，按细网格P1延拓与原粗map逐点相同；不是双线性插值，也不改变其homeomorphism。随后 SafeColoredVertexRelaxation / SafeColoredQCRadialRelaxation 使用图像残差提议细顶点更新，并以解析有向面积界选择幅度，每步返回固定边界、全部细面正向map；整个过程是从image-conditioned coarse latent和fine residual hint生成映射，不是对最终loss做f+t(-∇L)线搜索。最终输出固定1025² P1，训练梯度对coarse encoder可经fine update反传。
- Assumptions：底图A8在257²原面已正；中间P1细分的拓扑等价依赖严格嵌套和同对角线；细更新的QC cap若使用仍依赖base符合cap，不能由正面积自动推出；photometric hint在1025²从512²图像过采样，可能改善有限。先固定257²已学权重，无再训练测试8例，对照粗映射本身、一次与两次细反馈，再决定训练。
- What would falsify it：细分map与粗map在512²查询不一致、细分出现非正面、细更新使图像/map明显恶化、VJP/显存不可接受，或新增细更新幅度几乎为零而没有实际细控制能力。
- Smallest decisive test：side9到33上的随机有效粗map，精确重心插值并比较大量query与所有细面；随后257→1025一个high32样本的forward/VJP和面积，再8例/训练。
- Prior work：A8显式细网格安全局部更新及本阶段23节分辨率迁移失败；嵌套单纯形网格的P1细化是有限元基本性质。此处重点是让同一安全映射在新细网格保真，并从图像条件latent加细尺度校正。

Follow-up after eight heldout images: exact 257→1025 P1 refinement preserves image/map to float32 query tolerance; two safe fine feedback passes improve heldout image MSE from1.85e-5 to8.30e-6 and query-map RMSE from0.000409 to0.000269, all fine faces positive and final maximum |mu|0.783. A subsequent 300-step image-only train through this 1025 layer runs but heldout image/map slightly worsen, so retain frozen coarse weights as primary candidate. Independent fresh128 and six photographic-content sources with 16 variants each are now required before claiming useful generalization; the target maps remain known synthetic high32 and photos are not clinical registration.

Photo-domain protocol detail: the original257² A8 initial map exceeds the 0.8 QC cap for 30/96 photographic-content variants. Because the fine SafeColoredQCRadialRelaxation assumes an input under that cap, use the already-tested identity-to-initial homotopy cap on the 257² coarse decoder for both the pass0 comparison and the pass2 refinement. The homotopy is not a post-hoc fold repair; it is a continuous, analytically bounded step from identity inside the decoder before the QC-preserving feedback. Report this branch separately from the uncapped synthetic branch and verify all 96 coarse inputs satisfy the fine-refiner precondition.

Finite-precision follow-up: on photo96 with nominal 0.8 cap, observed final max |mu|=0.8000009, about9e-7 over threshold, although all faces remain positive and image/map improve. Test a planned internal cap0.795 for both coarse homotopy and fine refiner; this should leave a numerical margin under 0.8 while quantifying any image/map/μ cost. Do not describe theoretical exact cap as a verified strict float32 cap unless the output check passes.

Image-versus-Beltrami trade-off follow-up: with internal cap0.795, two fine passes improve image and query-map errors on all 96 photographic variants, but improve face μ MSE on only 3/96 (aggregate RMSE0.1211→0.1483). Test exactly one pass at gain1 and two passes at gain0.5 under the same frozen weights/cap, to quantify whether smaller safe correction retains most positional gain with less distortion. This is a predeclared two-point ablation, not a broad hyperparameter search; include all three metrics and topology margins.

# Reusable exact nested refinement layer card（2026-09-23）

- Question：1025²精确细分+安全反馈目前在研究脚本中拼接。能否把它封装成可插在任何合格257² P1 coarse-map层后的PyTorch模块，使输入/输出、准备成本、先验拓扑条件和VJP接口可直接复用，而不是只能复现一个脚本？
- Exact claim：模块输入同设备float32 fixed/moving图像(B,1,H,H)及coarse_map(B,257,257,2)，输出fine_map(B,1025,1025,2)。prepare建立固定重心插值table；forward先检查coarse边界固定、全部原面正向、最大|μ|<内部cap，再精确P1细分，按配置的0/1/2次图像残差细安全更新，最后检查所有fine原面正向与数值最大|μ|<cap。反向路径穿过粗图/图像到损失，不保存无关Krylov轨迹；局部refiner可checkpoint。默认拒绝不满足前提的输入，而不是默默产出越界图。
- Assumptions：两个结构化网格同SW–NE对角线，fine_side-1是coarse_side-1的整数倍；边界身份固定；QC安全层的精确证明对每步输入满足cap，float32还须实际逐面证书。显式证书需要额外扫描O(N²)，速度测量应清楚列入。
- What would falsify it：模块与脚本同数据输出差大、梯度到coarse_map消失/非有限、先验不满足却未拒绝、photo内部cap0.795仍有数值越界、面证书失效或速度显著降低。
- Smallest decisive test：side9→33的随机安全map、fake64图像，一步模块输出正面且VJP到coarse/map；违反cap/boundary应抛错。再1025²一个预训练A8保留样本与研究脚本逐点对照、完整forward/VJP显存/速度。
- Prior work：现有SpectralSafeFeedbackLayer、SafeColoredQCRadialRelaxation、24节脚本与精确网格细分单元测试；这是API封装，不宣称新拓扑定理。

# Route B same-GPU frozen forward/VJP card（2026-09-23）

- Question：B双257²精确PL组合的已有image-trained成绩和forward/VJP在Element CPU，而A8/C主要在ai GPU2；至少测一次同GPU同图像尺寸/真实batch的冻结B层，避免最终路线速度表直接混比不同设备。
- Exact test：加载既有twofine257_high32_image1000检查点，保持32/8 high32 seeds、512²最终query、batch1、两个257²因子、float32。ai空闲GPU2上运行原CF2脚本5个lr=0的forward+VJP计时步，排除首步后取中位，同时评价固定8例image/map与每因子全部131072面的面积；权重绝不更新。与A8/C同GPU速度只在口径相同部分比较，B精确组合不是原网格P1。
- Assumptions：lr=0的Adam不会改变参数；原脚本仍会组装优化器和训练数据，峰值显存包含数据常驻，需标注。原脚本的query动态穿过两个因子，不做第二次图像重采样；几何μ在既有02节另测。
- What would falsify it：GPU函数不支持动态query、VJP非有限/显存爆炸、同检查点保留image/map偏离CPU基线显著、或任一因子面非正。
- Smallest decisive test：先一轮冻结step和8例指标，若正确再5轮中位，不另造B模型。
- Prior work：02和16节B同数据精确组合/摄影内容结果，当前A8/C同GPU测时。
