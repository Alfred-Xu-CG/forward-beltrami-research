# Route C：已知64周期导纳模态在摄影图像内容上的迁移

## 问题和映射契约

在[34节](34_route_c_high64_mode_and_training.md)中，加入预知的64周期边导纳模态、再用32张合成纹理图像做300步图像损失训练，改善了同生成族的高频Beltrami误差。本实验问：当移动图像改成摄影内容，目标仍是**已知的合成32周期形变**时，新增模态与训练是否仍改善图像、位置和面Beltrami误差？还要区分光度拟合点阵由128²变256²的影响。不存在临床图像或未知真配准；全部目标真映射在生成时已知，但不被输入层，也不进入任何重新训练，本节只冻结评价。

最终控制网格固定为 **1025×1025顶点、2,097,152个原三角形**；图像与最终查询均为512×512，光度拟合分辨率单列为128²或256²，不能冒充控制网格。输入是固定、移动灰度图 $(I_f,I_m)$，输出为单位方形固定原网格全部顶点 $Y$；原三角形上的P1仿射延拓是所讨论的映射，不是小控制网格在512²图像点上的插值代用品。

Route C 给每条无向边 $e$ 一个对称严格正导纳

$$c_e=1+15\sigma\left(-0.8+\sum_{k=1}^{K}a_k\phi_k(e)\right),$$

其中 $K=18$ 或 $21$，$\sigma(t)=1/(1+e^{-t})$，$\phi_k$ 是边中点处事先指定频率的水平、竖直或东北对角方向正弦/余弦基。18模态每方向频率1、2、4、8、16、32；21模态再加入64。图像条件系数 $a$ 通过均匀导纳附近的解析响应 $V_k$、移动图梯度和图像残差，在拟合点阵上解21维或18维岭正规方程，然后有界变换并乘可训练正增益。最终**不是**把这些线性响应当映射，而是在所有1025²控制顶点上重新求带固定方形边界的正导纳平衡方程 $A(c)Y_I=b(c)$。精确算术下正边权、固定边界与本方形三角剖分满足同胚条件；本次浮点输出还检查每个原三角形的行列式、边界和真实线性残差。VJP通过同一对称系统的隐式伴随求解，未保存共轭梯度迭代轨迹。公式、点阵响应和梯度推导见[34节](34_route_c_high64_mode_and_training.md)。

## 数据、复算口径与可比性

摄影内容来自 scikit-image 0.22.0 的 camera、coins、moon、page、grass、gravel 六张灰度图，各缩放至512²并按程序标准化。每张独立附加16个合成形变，共96例。实际随机种子是 **973031**；系数独立取 $a_x\in[0.005,0.015]$、$a_y\in[0.010,0.025]$、$a_f\in[-0.002,0.0025]$。令 $b(x,y)=\sin(2\pi x)\sin(2\pi y)$、$s(x,y)=\sin(64\pi x)\sin(64\pi y)$，真映射为 $F_*(x,y)=(x+a_xb+a_fs,\ y+a_yb+a_fs)$，固定图由移动图经 $F_*$ 一次双线性采样生成。96例顺序是每张照片相邻16例，故统计复核按照片为簇，不假装96例来自96张独立照片。

此合成真映射本身有明确的正定向下界，而不是随意制造折叠目标。以 $v=(a_x,a_y)$、$w=(a_f,a_f)$ 写 $DF_*=I+v(\nabla b)^T+w(\nabla s)^T$。二阶行列式展开为 $\det DF_*=1+v\cdot\nabla b+w\cdot\nabla s+\det(v,w)\det(\nabla b,\nabla s)$。因为 $s_x+s_y=64\pi\sin(64\pi(x+y))$、$|b_x|,|b_y|\le2\pi$、$|s_x|,|s_y|\le64\pi$，在上述全部系数范围内

$$\det DF_*\ge1-2\pi(0.015+0.025)-64\pi(0.0025)-256\pi^2(0.0025)(0.020)>0.119.$$

$b,s$ 在方形边界消失，故 $F_*$ 固定边界；连续局部正定向再结合边界度数1，给出真目标是整个方形上的同胚。这是连续目标的性质，**不**自动保证把它在某个粗网格采样再P1插值仍无折叠；所有候选输出另行检查全部原面。

image MSE 是512²像素上 $|I_m(F(q))-I_f(q)|^2$ 的逐例均值再对96例平均。query-map RMSE 是全部96×512²查询的两坐标分量均方根，单位为方形边长。面Beltrami RMSE在全部96×2,097,152原面上，将预测P1常Jacobian的 $\mu=(u_x-v_y+i(v_x+u_y))/(u_x+v_y+i(v_x-u_y))$ 与目标解析Jacobian在原面重心处的 $\mu$ 比较，先平方平均后开根；因而不是仅抽几个面的代理量。最小行列式、最大预测$|\mu|$、前向真实相对残差均对全部例子取最坏值。每个条件的响应预计算一次，稳态计时含图像条件系数、真正的细网格求解、512²查询/warp/图像损失以及一次把该损失传至输入图像和所有增益的VJP。

计算在 `ai-codex-mihomo-codex` 的**CPU**执行，Intel Xeon Gold 6238R、Python3.11.7、PyTorch2.4.0、每作业4个OpenMP线程；当时GPU已有其他训练进程，未占用它们。为了节省墙钟，不同条件的CPU作业并发运行，因此各条件的秒数**不是隔离的性能对照**；只比较同例指标。`fit_side`、检查点和频率字典之外，数据生成、控制网格、浮点精度和求解容差一致。18模态基线加载 `c_photometric_calibration_mean0005_300.pt`；21模态冻结状态也加载它，新增三个增益初始化为1；21模态训练状态加载 `c_photometric21_high64_1025_train300.pt`，这是在**合成纹理high64**目标上另训300步的模型，不是摄影96例训练。

### 256²拟合点阵：额外64模态和重训的迁移

| 状态 | 摄影96例 image MSE | query-map RMSE | 全原面$\mu$ RMSE | 最小面det | 最大预测$|\mu|$ |
|---|---:|---:|---:|---:|---:|
| 原18模态，high32已训练增益 | 0.0372251 | 0.00595492 | 0.110160 | 0.71251 | 0.17910 |
| 21模态，沿用18增益、新增64增益未训练 | 0.0372176 | 0.00595487 | **0.109194** | 0.69867 | 0.18988 |
| 21模态，high64再训300步增益 | 0.0370890 | 0.00593015 | 0.113384 | 0.65538 | 0.21858 |

从原18模态**只加64模态**到冻结21，image几乎不变、map几乎不变，面$\mu$ RMSE由0.110160降至0.109194；按照片簇重采样的面$\mu$ MSE均差 $-2.119\times10^{-4}$，探索性95%区间 $[-3.301,-1.077]\times10^{-4}$，六张照片的簇均值都改善。故频率字典扩容本身在256²拟合下**没有**造成导数退化。

从冻结21到**high64另训300步**的21模态，image/map每例都改善，全面面$\mu$ MSE却在**96/96例全部变差**，面$\mu$ RMSE升至0.113384，最小面积裕度下降。此训练步骤的配对面$\mu$ MSE均差 $+9.327\times10^{-4}$，按照片簇重采样的95%区间 $[+2.897\times10^{-4},+1.938\times10^{-3}]$。因此此域外导数损失主要出现在增益训练，而非加入64模态的冻结表达。原18到训练21的两状态最大全面前向真残差分别约 $9.96\times10^{-11}$ 和 $9.99\times10^{-11}$，所以这不是明显的线性求解停机失败。对这两个端点，image/map每例都更好，面$\mu$ MSE只有28/96例更好；六张照片的平均image/map都更好，面$\mu$只有一张更好。

以照片为六个簇、每簇16例，固定seed20260923做20,000次**配对簇重采样**，训练21减原18的逐例MSE平均差及探索性95%百分位区间为：image $-1.3615\times10^{-4}$，区间 $[-1.7772,-0.9780]\times10^{-4}$；map MSE $-2.9442\times10^{-7}$，区间 $[-3.6789,-2.2141]\times10^{-7}$；面$\mu$ MSE $+7.2082\times10^{-4}$，区间 $[+1.19\times10^{-5},+1.7863\times10^{-3}]$。只有六张原始照片，区间不能推广到未知摄影或医学图像分布；它只是防止把同一照片的16次变形误算成16份独立内容证据。

### 128²拟合点阵：沿用原先的摄影设置

| 状态 | 摄影96例 image MSE | query-map RMSE | 全原面$\mu$ RMSE | 最小面det | 最大预测$|\mu|$ |
|---|---:|---:|---:|---:|---:|
| 原18模态，high32已训练增益 | 0.0325405 | 0.00491287 | **0.111951** | 0.59661 | 0.27614 |
| 21模态，新增64增益未训练 | 0.0325538 | 0.00491568 | 0.113098 | 0.57059 | 0.30598 |
| 21模态，high64再训300步增益 | **0.0324780** | **0.00489605** | 0.133795 | 0.47484 | 0.40035 |

原18模态CPU本次复算的image/map与[28节](28_route_c_photo_resolution_ablation.md)先前GPU记录在显示精度一致。因此把拟合点阵加密至256²，虽然18模态面$\mu$ RMSE略降，却使其摄影任务image/map明显变差；“拟合点更多必然更准”不成立。注意21模态是在256²拟合上训练的，此128²列只是**推断期分辨率迁移**。在128²拟合下，只加64冻结模态使image/map略差，面$\mu$也略差；随后high64增益训练使map更好，但面$\mu$ RMSE大幅升至0.1338。训练21减冻结21的面$\mu$ MSE在**96/96例都变差**，六张照片均值都变差；以照片为簇的均差 $+0.005110$、探索性95%区间 $[+0.002639,+0.008622]$。同一步的image MSE均差虽为 $-7.579\times10^{-5}$，六簇重采样区间 $[-1.937\times10^{-4},+2.25\times10^{-6}]$ 跨零，不能把128²摄影图像均值改善写成稳健结论。map MSE均差 $-1.926\times10^{-7}$、区间 $[-2.654,-1.177]\times10^{-7}$，六张照片簇均值均下降。

这项消融将两个本来容易混淆的限制分开：**导纳字典加密**在256²拟合下有一点面导数收益，而**仅图像损失训练全局增益**可牺牲域外面导数精度来换取小幅图像/位置收益；**光度拟合点阵加密**也不保证摄影内容的图像/位置误差更低。三者都不是拓扑失败：所有六个状态的预测全部原面det严格正，前向真相对残差均小于$10^{-10}$，首例完整损失VJP的输入图像及增益梯度范数均有限非零、伴随真相对残差小于$10^{-10}$；但同胚和正确梯度都不保证准确配准或Beltrami恢复。

原始记录还允许看32周期**目标细位移幅值**：在每例1025²顶点把预测的两个坐标位移分别投影到 $s(x,y)=\sin(64\pi x)\sin(64\pi y)$，再对已知真幅值 $a_f$ 做过原点最小二乘斜率，两个坐标的斜率取平均。理想斜率为1。六个状态按上述两表行序，在128²拟合下仅为0.247/0.263/0.258，在256²拟合下仅为0.223/0.237/0.236；所以既有图像和位置的小幅改善并没有接近恢复摄影合成目标的32周期位移幅值。这是一个单模态诊断，不等于整张映射的RMSE或完整频谱分析。

## 旧18增益与新增64增益的二因子归因

训练21模态时，新增64频率在水平、竖直、对角三个边方向的正增益 $\exp(3\tanh\gamma_k)$ 从中性值1变为约2.830、2.824、5.384；原18个增益也有较小漂移。因此上面“冻结21→训练21”的变化还不能直接归因于高64增益。我们保留**完全相同的21模态字典、256²拟合、1025²控制、96张摄影合成输入**，仅把两个增益分组交叉组合：$O=0$ 表示旧18增益来自high32已训练基线、$O=1$ 表示来自high64再训300步；$H=0$ 表示三个新增64增益均为1、$H=1$ 表示来自high64再训。工具 `tools/phase6_splice_conductance_gains.py` 仅逐项复制已保存的raw gain张量，四组输出再次求**真实非线性正导纳平衡方程**，没有线性插值映射或修补折叠。

| 256²拟合，摄影96例 | image MSE | query-map RMSE | 全原面$\mu$ RMSE | 最小面det |
|---|---:|---:|---:|---:|
| $O=0,H=0$：冻结21 | 0.0372176 | 0.00595487 | 0.109194 | 0.69867 |
| $O=0,H=1$：旧18不变、只放大64增益 | 0.0372272 | 0.00595711 | 0.113347 | 0.65116 |
| $O=1,H=0$：旧18已训、64仍中性 | **0.0370787** | **0.00592777** | **0.109231** | **0.70151** |
| $O=1,H=1$：完整训练21 | 0.0370890 | 0.00593015 | 0.113384 | 0.65538 |

在固定旧18增益为基线时，仅放大64增益使面$\mu$ MSE均差 $+9.242\times10^{-4}$，六照片簇重采样95%探索性区间 $[+2.733\times10^{-4},+1.936\times10^{-3}]$，96/96例均变差；query-map MSE也96/96例变差，image MSE均值多 $9.54\times10^{-6}$。在固定64为中性时，仅替换已训旧18增益使image MSE均差 $-1.389\times10^{-4}$、map MSE均差 $-3.22\times10^{-7}$，两者均在96/96例变好；面$\mu$ MSE均差仅 $+8.11\times10^{-6}$，六照片簇区间 $[-2.80\times10^{-6},+1.81\times10^{-5}]$ 跨零。把另一个分组固定在已训值再比较，两个方向的均差几乎相同，交互在这批数据上很小。**因此在该层、这批摄影输入和256²拟合条件下**，新增64增益的放大主导了导数退化，旧18增益漂移主导了图像/位置收益。

$O=1,H=0$ 在这96例上甚至同时优于原18模态与完整训练21模态的image、位置和面$\mu$，但这个组合是在看过**同一摄影集合**的结果后构造，属于机制诊断和待检候选，不是未见摄影内容上的独立性能证明。它也可能丢失high64目标的真实高频恢复；另一组新128例high64合成样本的回测必须与此并列，而不能只展示摄影受益。四状态原始文件包括两份交叉检查点 `checkpoints/c21_{baseold_trained64,trainedold_neutral64}.pt`、两份对应的 `raw_results/c21_{baseold_trained64,trainedold_neutral64}_photo96_fit256_cpu.json`，逐对照片簇区间在 `raw_results/c21_{gain64,oldgain}_effect_*photo96_cluster.json`。

### 在未见的 high64 合成目标上的反向权衡

固定另一个此前未参与300步训练的128例 high64 合成集合（seed 1170031），使用同一1025²控制网格、512²图像、256²拟合点阵和21个导纳模态，复算 $O=1,H=0$ 与 $O=1,H=1$。这128例不是未见摄影内容；其用途是检验摄影后验候选有没有牺牲其原来专攻的64周期目标。两个条件都完整重解正导纳方程和隐式VJP，均在四线程CPU上运行；并发计时不能作为GPU速度比较。

| high64新128例 | image MSE | query-map RMSE | 全原面$\mu$ RMSE | 64周期位移幅值斜率 | 最小面det |
|---|---:|---:|---:|---:|---:|
| $O=1,H=0$：摄影后验候选 | 0.000139788 | 0.00105374 | 0.105665 | 0.2151 | 0.73667 |
| $O=1,H=1$：原high64专门训练 | **0.000132476** | **0.00102948** | **0.056666** | **0.8285** | 0.47344 |

高64增益从中性变回训练值时，面$\mu$逐例MSE在128/128例下降，均差 $-0.007954$；对128个合成样本作探索性配对bootstrap的95%百分位区间为 $[-0.009258,-0.006696]$。image MSE在98/128例下降，均差 $-7.31\times10^{-6}$、区间 $[-1.06\times10^{-5},-3.98\times10^{-6}]$；query-map MSE在101/128例下降，均差 $-5.05\times10^{-8}$、区间 $[-7.25,-2.79]\times10^{-8}$。这种重采样只刻画当前合成生成器下有限样本波动，不证明跨图像分布泛化。两个条件的全部原面仍正向，最大真实前向相对残差分别为 $9.38\times10^{-11}$、$9.84\times10^{-11}$，首例隐式伴随真实相对残差分别为 $1.43\times10^{-11}$、$2.91\times10^{-11}$。

结论不是“中性64更好”或“训练64更好”的无条件排序，而是一个**可测的任务权衡**：放大64增益在这些摄影high32目标上牺牲面导数精度，在high64目标上却是恢复细位移幅值和导数精度的关键。仅靠现有全局21维增益、无任务识别的单一检查点，没有证据同时达到两种目标。后续可研究根据图像证据决定64增益的输入条件化或导数监督，但必须在彼此未见的摄影和high64目标上同时评估，并检验该条件化能否保留严格正导纳和快速伴随。

### 未参与增益选择的摄影内容检查

为避免只在前述六张照片上后验选取$O=1,H=0$，另取 scikit-image 自带的 astronaut、coffee、chelsea、rocket 四张**此前未用作模型训练或增益选择**的彩色照片。按 $0.2126R+0.7152G+0.0722B$ 转灰度，再沿用同一512²缩放及逐图标准化；每图8个由新seed **703231**生成的已知high32合成形变，共32例。控制网格仍为1025²、原面2,097,152、图像与查询512²、拟合点阵256²，冻结模型，不重新训练。真映射的系数范围和连续正定向下界与前述96例相同。这是**新图像内容的样本外检查**，但目标形变仍是合成且只有四个独立内容源。

| 新四照片×8例 | image MSE | query-map RMSE | 全原面$\mu$ RMSE | 32周期位移幅值斜率 | 最小面det |
|---|---:|---:|---:|---:|---:|
| 原18模态 | 0.00930841 | 0.00533622 | 0.102847 | 0.3982 | 0.66534 |
| 21模态，训练旧18增益＋中性64 | **0.00922476** | **0.00530273** | **0.100775** | **0.4176** | 0.65408 |
| 21模态，全部high64训练增益 | 0.00923248 | 0.00530508 | 0.105161 | 0.4169 | 0.64533 |

中性64候选相对完整high64训练状态，image MSE在28/32例、map与面$\mu$ MSE在32/32例下降；按照片为四簇、每簇8例的20,000次配对重采样，三项均差（候选减完整）为 $-7.72\times10^{-6}$、$-2.50\times10^{-8}$、$-9.03\times10^{-4}$，探索性95%百分位区间分别为 $[-1.47\times10^{-5},-7.83\times10^{-7}]$、$[-4.82\times10^{-8},-1.75\times10^{-9}]$、$[-1.91\times10^{-3},-7.55\times10^{-5}]$。与原18模态相比，中性64候选的image/map在32/32例下降，面$\mu$ MSE在26/32例下降；四张照片各自的三项平均差都向好。但**四个内容簇太少**，bootstrap区间高度依赖这四张照片，不是分布外保证，更不能外推到临床影像。

三状态全部2,097,152原面在每例均正向，最大前向真相对残差均小于$10^{-10}$；首例图像与增益的VJP均有限非零、真实伴随相对残差小于$10^{-11}$。CPU并发作业的单次秒数仍不作速度排名。这个新内容检查提升了对“旧18增益漂移有用、64增益宜按任务适配”的信心，但与上节high64反向权衡共同说明：当前候选不是一个跨形变频谱普适获胜的神经层。

## 代码与原始证据

入口为 `tools/phase6_scale_photometric_layer.py`，调用 `--side 1025 --photo-variants 16 --photo-seed 973031 --fit-side {128,256} --device cpu --repeats 1`，再用 `--checkpoint`、`--frequencies` 分别选上表状态。配对分析为 `tools/phase6_paired_bootstrap.py`，使用 `--cluster-size 16`。原始逐例JSON是 `raw_results/c{18_high32,21_high64_frozen,21_high64_trained}_photo96_fit{128,256}_cpu.json`；256²基线/训练端点对照为 `raw_results/c21_vs_c18_photo96_cluster_bootstrap.json`，加模态、再训练的分段对照与128²迁移各有同名 `*_cluster.json` 文件。

high64反向权衡的两个逐例文件为 `raw_results/c21_{trainedold_neutral64,fulltrained}_high64_fresh128_fit256_cpu.json`，配对统计为 `raw_results/c21_h64gain_effect_high64_fresh128_cpu_bootstrap.json`。若只希望检验同一训练检查点的CPU/GPU数值复算，则后者CPU结果可与[34节](34_route_c_high64_mode_and_training.md)既有GPU新128例对照：image MSE为0.0001324764558与0.0001324764557，map RMSE为0.001029480191与0.001029480189；这个吻合仅支持同一实现跨设备复算，不是独立物理真值验证。

新四照片逐例文件为 `raw_results/{c18,c21_neutral64,c21_fulltrained}_newphotos32_fit256_cpu.json`；三份配对照片簇统计为 `raw_results/c21_{neutral64_vs_fulltrained,neutral64_vs_c18,fulltrained_vs_c18}_newphotos32_cluster.json`。`tests/test_phase6_photo_bank.py` 保证旧六张默认输入不因新增彩色图像支持而变化；`tests/test_phase6_splice_conductance_gains.py` 保证交叉检查点只更换预定的增益坐标。

注意：旧版 `phase6_scale_photometric_layer.py` 在摄影分支硬编码实际种子973031，却把未使用的合成保留集 `--seed` 默认99317写入JSON `seed` 字段。以上原始试验由旧版运行，故其 `seed=99317` **仅是错误的元数据**，不是实际随机种子；代码现已增加 `--photo-seed` 并在新运行中报告实际值。旧原始JSON保持原样而不篡改，真实数据由当时摄影分支源码、相同逐例真细幅值和本节明确的生成公式界定。
