# Phase V worklog

UTC date: 2026-09-22
Route: I — N=49 directed CUDA reliability diagnosis
Question: Why do all normal-strength N=49 CUDA engineering configurations fail, and is there a bounded nonsymmetric solver correction with constant saved autograd state?
Precise claim/hypothesis: The failure is an adjoint float32 attainable-residual limitation of the stationary fallback and/or BiCGStab recurrence replacement, rather than loss of invertibility of the represented positive Tutte operator. A restarted Krylov correction with separately accumulated residual may attain the unchanged 1e-5 final represented true-residual threshold more reliably. This is a numerical hypothesis, not a guarantee for all positive weights.
Assumptions: reproduce seed 1701 and strength 0.15, compare the same represented probabilities/RHS against CPU float64 sparse LU, keep final true-residual acceptance and returned-P1 topology checks, use only an idle AI physical GPU 1.
What falsifies it: double sparse LU has a large residual, attainable represented float32 residual exceeds 1e-5 even after accurate LU rounding, the candidate materially changes VJP or forward accuracy, or its stored autograd state grows with iteration count.
Smallest decisive test: isolate primal and dense-loss adjoint at N=33/49; record one-norm condition estimate, rounded-LU residual, BiCGStab and stationary residuals; only then compare a bounded restarted solver and direct-reference VJP on N=49.
Prior work: preserved normal-strength failure receipts in route1_engineering_gpu_02019fe.jsonl, prior stationary fallback audit, and the existing CPU double sparse direct oracle.

N49 reliability result (2026-09-22): Normal-strength failures isolate to the float32 dense-loss adjoint. For the identical represented matrix/RHS, CPU double LU gives relative residual 4.76e-14 and estimated cond1(A transpose) 1098; rounding its adjoint to float32 and applying native CUDA scatter gives about 1.4e-5, above the unchanged 1e-5 threshold. This is observed arithmetic noise, not a hard lower bound: favorable stationary iterates can narrowly pass, with strong run-to-run iteration variation. Merely evaluating matvec in double while retaining float32 recurrences still breaks down. Production code and all original negative receipts are unchanged. Existing explicit float64 CUDA mode instead passes all 18 N49/batch1,4,8/layers1,2,4/seed1701,1702/256-square configurations (warmup1, repeats2), maximum primal/adjoint iterations121/124 and residuals below1e-10, with all returned control-map certificates passing. Three N49/batch4 direct-CPU comparisons give maximum dense-map absolute error3.45e-9, row-logit VJP relative L2 error4.54e-9, boundary VJP relative L2 error1.46e-9. The additional N49/batch8/layers4/512-square case passes at18.214s and1212.617MiB peak allocated; this is a reliable bounded GPU option, not a speed claim over CPU direct. Preserved raw evidence: route1_directed_float64_reliability_gpu_e31586c.jsonl/.csv, route1_directed_float64_direct_vjp_e31586c.jsonl, route1_directed_float64_n49_b8_l4_512_gpu_e31586c.json. Diagnosis/reproducers remain under tmp/phase5_directed_gpu_reliability.md and associated scripts. Independent review remains pending with the coordinator; stronger logits and optimization trajectories are outside this result.

UTC time: 2026-09-21T16:10:26Z
Route: setup
Question: Can the 18-hour phase be anchored to an immutable real wall clock and a clean verified baseline?
Precise claim/hypothesis: The checked-in start receipt is created once, elapsed time is computed from the system UTC clock, final review is denied before 17h15m, and completion is denied before 18h.
Smallest decisive test: exercise the guard immediately and at injected timestamps bracketing both thresholds.
What falsifies it: overwriting the receipt, accepting a pre-threshold timestamp, or using handwritten elapsed labels.

Result: Phase V began from commit `748aa84`; clean baseline is `317 passed, 1 skipped, 1 warning in 123.84s`. The optional skip is the absent official surface-data package and the warning is Paramiko's external Blowfish deprecation.
Checker needed: yes, before treating the clock gate as authoritative.
Next: test-drive `tools/check_phase5_elapsed.py`, then audit the existing Tutte implementation and official TutteNet sources.

UTC time: 2026-09-21T16:18:31Z
Route: I — Fast Tutte neural layer
Question: Can one fixed control mesh support a reusable batched Tutte solve and precomputed 256²/512² dense queries without repeating point location or sparse factorization in backward?
Precise claim/hypothesis: Fixed source topology permits one neighbor table and one pixel-to-face/barycentric table; a direct SuperLU backend can reuse a single numeric factor for x/y right-hand sides and transpose adjoints, while matrix-free directed and symmetric backends can expose the same map and VJP contract.
Assumptions: one simple triangulated disk; a positively oriented weakly convex rectangle boundary; strictly positive valid-neighbor weights; all query pixels lie in the source control domain.
Smallest decisive test: on an 11×11 control-vertex grid, compare direct output/VJP against an independently assembled reference and compare precomputed dense warp against analytic identity at 256².
What falsifies it: direct/reference disagreement, adjoint finite-difference failure, per-forward point location, negative control-face orientation, or iterative residual failure.
Prior work: Phase III directed Tutte implicit layer; official TutteNet paper/code audit is running independently.

Result (2026-09-21T16:35:25Z): Independent adversarial review found four concrete failures in the legacy directed layer: boundary-loop order was mistaken for global vertex order on a boundary-only mesh; a weakly convex collinear boundary ear could collapse a face; finite extreme logits could underflow a supported probability to zero; and an unused isolated source vertex was accepted. Regression tests now cover all four. The implementation scatters/gathers the boundary permutation correctly, rejects unused vertices and zero supported probabilities, and accepts a solve only when every original oriented face has strictly positive numerical signed area. The focused directed suite is `9 passed`; this is a finite-precision certificate for the returned P1 map, not yet a proof that every admitted generic source mesh is a simplicial disk.

Dense-query result (2026-09-21T16:35:25Z): `StructuredDenseQueryTable` analytically precomputes source triangle indices and barycentric weights for `structured_rectangle`. At 512 squared queries its affine-map error is at most `6.66e-16`; 104 independently located triangle/corner queries agree to `1.30e-15`; `torch.autograd.gradcheck` passes. The implementation explicitly prepares device/dtype data outside the forward path. An independent review exposed a `cpu:0`/indexless-device cache-key mismatch; a regression test and actual-allocation key normalization fixed it. Final independent verdict: PASS, `5 passed`; prepared interpolation contains gather, multiply, reduction, and reshape operations but no transfer or point location. CUDA key semantics were inspected but not executed locally because no local CUDA device exists.

Checker needed: yes. The generic source-disk hypotheses and direct-solver factor-reuse path remain open and are being checked separately.
Next: close the source topology certificate, implement the one-factor direct oracle, and only then compare matrix-free backends.

UTC time: 2026-09-21T16:38:00Z
Route: shared benchmark, first used by Route I
Question: Can all three routes be evaluated on the same valid deformation and image data without confusing a forward geometric map with the backward sampling grid used by image registration?
Precise claim/hypothesis: A deterministic suite of orientation-preserving analytic maps, supplemented later by positive-Tutte random maps, can be sampled on every structured control mesh and accepted only after an independent P1 injectivity audit. For images, the learned PL homeomorphism is defined as the backward coordinate map from fixed-image coordinates to moving-image coordinates; therefore `grid_sample(moving, F_theta)` directly produces the warped moving image and no differentiable inverse is hidden in the benchmark.
Assumptions: align-corners pixel coordinates on the closed unit square; moderate deformation parameters with explicit positive continuous Jacobian bounds; structured source disks; image intensities normalized to `[0,1]`.
What would falsify it: a named analytic map with nonpositive analytic determinant, a sampled P1 face with nonpositive orientation, nondeterministic data, an inconsistent corner/pixel convention, or an image pair that can be fit without calling the decoder and dense warp.
Smallest decisive test: check every named map on 11-by-11 control vertices, independently audit the returned P1 map, verify identity sampling exactly at pixel centers, and verify each of four image families is finite, nonconstant, deterministic, and normalized.
Prior work: Phase V common benchmark specification; PyTorch `grid_sample` backward-coordinate convention; existing independent `audit_injectivity` routine.

UTC time: 2026-09-21T16:50:00Z
Route: I — rectangle boundary and learnable modulus
Question: Can boundary freedom be exposed to optimization while preserving the target rectangle boundary as an orientation-preserving homeomorphism by construction?
Precise claim/hypothesis: For every side, softmax-normalized positive segment lengths and cumulative sums place all noncorner boundary vertices in strict side order; sharing the four exact corners closes the polygon. A positive height `H = H_min + softplus(m)` supplies a differentiable learnable modulus without allowing a collapsed rectangle.
Assumptions: source is a certified structured rectangle; the source boundary loop follows its oriented four sides; width is fixed to one to remove global scale; logits are finite and do not underflow to zero in the returned dtype.
What would falsify it: a corner mismatch, duplicate/reversed adjacent boundary points, a zero segment after dtype conversion, wrong loop-to-side indexing, nonpositive height, or a failed finite-difference modulus/boundary-logit gradient.
Smallest decisive test: on unequal 4-by-3 and 1-by-3 grids, check identity logits recover uniform side spacing, random logits preserve exact corners and strict order, and autograd/finite differences agree for every side plus modulus.
Prior work: Phase V Route I-B specification and TutteNet's cumulative boundary-angle construction.

Direct-solver result (2026-09-21T16:52:06Z): `DirectTutteLayer` is a CPU correctness/reference backend supporting unbatched, batched, and singleton-broadcast directed row-softmax systems. Each sample calls SuperLU factorization exactly once, solves the two coordinate columns as one multiple-right-hand-side call, retains that factor with the autograd graph, and uses the identical factor's `trans='T'` solve for the two-column adjoint. A batch-two instrumented forward/backward therefore records two factorizations and the solve sequence `N,N,T,T`; an independent variable-degree dense reference agrees within `4.45e-16`. Non-symmetric gradcheck and joint logits/boundary directional finite differences pass. The first independent review found a real float64 alias: the returned tensor shared NumPy storage with the primal saved for backward, so an in-place user update silently doubled selected gradients. The saved primal is now a separate read-only copy; independent recheck found zero in-place/non-in-place gradient discrepancy for unbatched and batch-one/two cases. A returned-float32 collapse that remains positive in the hidden float64 solution is also explicitly rejected. Final focused result: `18 passed`; independent verdict: PASS. Limitations remain explicit: sequential CPU only, first derivatives only, no cross-forward symbolic cache.

Shared-benchmark result (2026-09-21T16:52:06Z): Eight deterministic analytic orientation-preserving maps have stated positive continuum determinant lower bounds and pass independent P1 audits on every required 11/17/25/33/49 control-vertex grid. Four deterministic image families are finite, nonconstant, normalized, and pass 256-squared float32/float64 checks. The convention is fixed-to-moving backward coordinates with `align_corners=True`. Initial review found two silent validity holes: border padding hid out-of-domain maps, and NaN coordinates could return finite border intensities. `make_registration_pair` now rejects a map leaving the sampled unit square unless a future explicit extended-domain/mask protocol is provided, and the warp rejects all nonfinite images/maps. Independent closure recheck: PASS, `6 passed`. At present only local compression and boundary sliding from the analytic suite are admitted as in-domain registration pairs; the remaining maps remain valid geometric expressivity tests.

UTC time: 2026-09-21T16:54:00Z
Route: I — symmetric-conductance matrix-free backend
Question: Does the symmetric positive-conductance subfamily provide a genuinely GPU-capable, memory-bounded alternative to directed sparse LU, and what expressivity is lost?
Precise claim/hypothesis: With one strictly positive conductance per undirected edge incident to the interior, the reduced Dirichlet graph Laplacian is symmetric positive definite. A batched edge-scatter operator therefore admits matrix-free conjugate gradients for both forward and identical adjoint systems. Custom implicit differentiation needs only conductances, the converged primal, boundary values, and constant-size Krylov work vectors; it does not store an iteration tape. Boundary-boundary conductances are excluded because they cannot affect the interior harmonic extension.
Assumptions: a connected certified triangulated disk, nonempty Dirichlet boundary, positive finite conductances, a convex ordered target boundary, and forward/adjoint residuals tight enough for the requested dtype.
What would falsify it: a nonpositive reduced quadratic form, failure of the edge operator against an independently assembled dense Laplacian, CG residual or adjoint identity failure, gradient disagreement with dense autograd/finite differences, hidden sparse assembly/factorization, topology failure in returned dtype, or memory growth proportional to iteration count.
Smallest decisive test: on a nonsquare 4-by-3 structured disk, compare matrix-free products and solutions with a dense SPD reference; run double gradcheck for conductance logits and boundary; instrument that SciPy factorization is never called and that only a constant number of state-sized tensors are retained.
Prior work: classical weighted Tutte embeddings and Dirichlet graph Laplacians; Phase V Route I-C/D; the directed direct oracle implemented earlier in this phase.

Topology-theorem result (2026-09-21T16:56:00Z): The exact missing weak-convex theorem was identified as Floater (2003), Theorem 6.1: for a triangulated disk and a positive convex-combination map whose boundary maps homeomorphically to a convex region, injectivity is equivalent to excluding any dividing edge (a non-boundary edge joining boundary vertices) whose image lies in the target boundary. Thus collinear rectangle-side subdivisions are admissible, but the dividing-edge clause is essential. `DirectedTutteSystem` now performs a one-time combinatorial disk certificate (unique/all-used simplices, coherent edge incidence, connected face complex, interior-cycle/boundary-path vertex links, one boundary cycle, Euler characteristic one), an exact dyadic-integer source realization/simple-boundary check, and caches dividing edges for a pre-solve target-boundary test. It also rejects reuse with a same-size but different mesh. Thin structured rectangles remain accepted. Independent implementation review ran the specified 35-test set, checked 20 independent square chords and all required grid sizes, and returned PASS. This closes the fixed-input floating-representation source and Floater hypotheses; it does not turn legacy pre-cast target diagnostics into exact real-arithmetic statements.

Boundary result (2026-09-21T16:56:00Z): The four independent per-side softmax distributions, exact shared corners, and `H_min + softplus(m)` modulus pass double gradcheck and random broadcast/finite-difference checks. Independent review exposed that positive softmax entries alone do not ensure representably distinct cumulative vertices, registered-buffer tuples become stale after device migration, inverse softplus overflowed for large heights, and a large finite float32 modulus could realize an infinite height. Red regressions now require strict order of the actually realized coordinates, query registered side buffers dynamically, use a stable inverse softplus, and reject nonfinite realized height. Combined verdict: PASS, `42 passed, 1 CUDA-unavailable skip`; meta-device migration was verified, while real GPU execution remains to be measured remotely.

Directed matrix-free result (2026-09-21T17:10:00Z): `MatrixFreeDirectedTutteLayer` applies (A=I-P_{II}) by neighbor gather/weighted reduction and (A^T) by scatter-add, with vectorized batch/two-coordinate BiCGStab and a custom implicit adjoint. It assembles no CSR, performs no factorization, and saves probabilities plus one independent primal rather than the Krylov trajectory. Independent normal-scale dense and directional checks passed, including an irregular nonsymmetric disk. The first review nevertheless found a serious scale-dependent false certificate: squared residual norms underflowed after an iteration, reporting zero residual and corrupting tiny-cotangent gradients. Every norm and dot product now uses component scaling and explicitly rejects unsafe rescaling/subnormal cases; true residual reporting no longer clamps a small denominator. Original float64/float32 raw 3-by-3 and irregular-disk probes now either solve accurately or fail closed with a nonconverged report. Focused result: `39 passed, 2 CUDA-unavailable skips`; independent verdict: PASS for the bounded CPU numerical scope. Arbitrary-scale availability, actual CUDA behavior, and GPU performance remain unverified until remote execution.

Symmetric matrix-free result (2026-09-21T17:47:00Z): `MatrixFreeSymmetricTutteLayer` assigns a strictly positive softplus conductance to every active undirected edge and applies the reduced SPD Laplacian by edge scatter, with a vectorized two-coordinate CG primal and an implicit transpose-identical adjoint. Per-sample conductance normalization is kept inside autograd so global-scale gauge invariance and its VJP are preserved. Independent review first falsified the implementation on huge upstream adjoints and boundary-only empty conductance sets; per-RHS max-absolute scaling, scaled absolute tolerances, finite reconstruction checks, and an explicit empty-edge branch close both cases. Fresh `14 passed`; dense, finite-difference, extreme-scale, thin-grid, and iteration-independent saved-state checks passed. Independent verdict: PASS for CPU float32/float64. Actual CUDA correctness and performance remain unverified until the remote run.

UTC time: 2026-09-21T17:12:00Z
Route: I — mandatory single-instance optimization
Question: Before adding any CNN, can neutral raw Tutte parameters recover one valid target map and one 256-squared synthetic image pair through the actual solve, dense warp, and backward pass?
Precise claim/hypothesis: A target generated once by positive directed weights and a strictly ordered rectangle boundary is exactly representable by the directed decoder. Starting from uniform row and side logits, direct optimization should reduce supervised dense-map error and image-warp error while every iterate is accepted by the hard returned-P1 topology screen. The same fixed target will later expose the approximation/optimization gap of symmetric conductances rather than changing data between backends.
Assumptions: fixed structured control mesh, deterministic seed, fixed-to-moving backward image convention, unit-square image case, explicit solve count, and no network/encoder. Supervised fitting may learn modulus; image fitting initially fixes unit height to prevent out-of-domain padding from becoming an implicit objective.
What would falsify it: loss reduction without a decoder solve, target generation that fails its independent certificate, any accepted folded iterate, hidden clamping/border padding, a wrong gradient under finite differences, or a method-specific target.
Smallest decisive test: on a 5-by-5 control-vertex grid and 32-squared queries, demonstrate deterministic target generation and strict reduction from neutral parameters in short Adam supervised and image runs, recording one real solve per closure; then scale the unchanged protocol to control sizes 17 and 25 at 256 squared and compare Adam with LBFGS.
Prior work: Phase V mandatory instance-optimization protocol; the direct, boundary, dense-query, and shared-image components verified above.

Instance-stack result (2026-09-21T17:52:00Z): The no-network runner now performs one actual Tutte solve and dense coordinate warp at every Adam evaluation or LBFGS closure, audits the returned control map independently, and reports primal and adjoint global solves separately. A SuperLU proxy matched the reported Adam `3+2=5` and LBFGS `13+12=25` calls. Audit time is separated from optimizer-only overhead, and the first audited threshold observation records its wall time and solve count without charging a not-yet-run adjoint. A prebuilt target can be generated once and shared across backends; shape, finiteness, unit-domain range, control topology, and dense-versus-control P1 consistency are rechecked, and target tensors are detached and left bit-identical. Independent direction derivatives, nonzero affine Beltrami metrics, image convention, six shared-target image runs, and all three backends passed on CPU; focused result `14 passed`. This closes the bounded CPU plumbing, not medium-scale convergence, CUDA, accepted-state LBFGS semantics, or the full Route I experiment.

UTC time: 2026-09-21T17:53:00Z
Route: I — multi-layer square-domain composition
Question: Can one, two, and four hard-bijective Tutte layers be composed without dense image resampling, while retaining differentiability and a precise topology claim?
Precise claim/hypothesis: If every layer is a certified orientation-preserving P1 homeomorphism from the closed unit square to itself, their continuum composition is a homeomorphism. A dense output can be evaluated by applying the first fixed-query P1 table and then locating the preceding output points in each subsequent structured source mesh. The composed dense samples remain differentiable almost everywhere, but they are not a new P1 map on the original control triangulation and therefore must not receive that stronger certificate.
Assumptions: every intermediate boundary has realized height exactly one; each solver independently certifies its returned control map; dynamic points remain within a stated roundoff tolerance of the unit square; composition order and batch broadcasting are explicit.
What would falsify it: any layer exits the unit square, any per-layer certificate fails, composition uses repeated image interpolation instead of coordinate-map composition, affine composition is not exact to floating-point tolerance, finite differences disagree away from triangle boundaries, or memory grows with solver iterations rather than only layers/state size.
Smallest decisive test: use two in-square affine contractions only as a low-level interpolation/composition algebra fixture, compare against analytic matrix composition and directional derivatives, then separately compose genuine non-affine unit-square Tutte homeomorphisms and run batch one/four/eight and layers one/two/four at 256-squared queries. A fixed-corner affine square automorphism would be only the identity and is not used as a nontrivial topology fixture.
Prior work: the fixed `StructuredDenseQueryTable.interpolate` and independently reviewed dynamic `interpolate_points`; the boundary module can realize exact unit height from a fixed raw modulus.

UTC time: 2026-09-21T18:18:00Z
Route: I — reliable float32 GPU Krylov solves
Question: Why do CPU-verified matrix-free solvers fail during the first realistic 17-by-17, 256-squared CUDA optimization, and can the failure be removed without hiding residual error or storing iteration histories?
Precise claim/hypothesis: The first remote run separates two numerical causes. Directed BiCGStab forward solves pass, but the unpreconditioned recurrence became unstable before satisfying 5e-6 in the observed CUDA adjoint; a true-residual tolerance of 1e-5 stops before that observed breakdown and must be validated against the direct VJP and across larger meshes, without claiming a universal float32 accuracy floor. Symmetric CG stops on a recursively updated residual while its recomputed true residual remains about 5e-6; recomputing the represented true residual at each apparent convergence and periodically every 32 iterations, with a restart for any still-active right-hand side, should prevent false stopping while preserving CG conjugacy between reliable updates and retaining constant-memory implicit differentiation. Any Jacobi preconditioner is a separate conditioning ablation, not the explanation for the uniform-conductance failure.
Assumptions: CUDA scatter arithmetic is float32 and may be nondeterministic; all acceptance remains based on an explicit true residual; an x/y multi-right-hand-side solve counts once; the direct CPU float64 solve or an independently assembled dense SPD solve is the accuracy reference.
What would falsify it: the failures occur before the adjoint, relaxed tolerance still breaks across N=17/25/49 or yields material map/VJP error, reliable-residual CG fails its explicit residual, any new path silently reports recursive residual as truth, or saved autograd state grows with iteration count.
Smallest decisive test: reproduce the exact seed at N=17 and 256-squared on A40; record forward versus adjoint diagnostics; add a CUDA regression; compare map and VJP against float64 references; then sweep N=17/25/49 and batch 1/4/8 before accepting a solver configuration.
Prior work: original fail-closed logs in `raw_results/route1_gpu_n17_i1_adam_smoke.json` and stage-isolation logs `gpu_solver_debug*.log`; CPU extreme-scale and constant-memory checker results for both matrix-free backends.

Reliable-GPU result (2026-09-21T18:51:43Z): The original A40 failures remain preserved as negative evidence. For the symmetric family, candidate/periodic true-residual replacement fixes false recursive stopping, but the original strict `2e-6` N=49 stress still fails at `6.609e-6`; changing the float32 default to `1e-5` is therefore an explicit availability/accuracy tradeoff, not a universal cure or a float32 accuracy theorem. At N=49, an independent CPU-float64 direct solve gives map max/L2-relative errors `9.36e-5/6.23e-5`, boundary-VJP max/L2-relative errors `6.87e-5/1.76e-5`, and one edge-logit directional VJP relative error `8.49e-4`. The N=49, batch-eight, four-layer A40 stress succeeds in `17.21 s` measured end-to-end with `198.65 MiB` peak allocated and all 32 returned P1 maps certified, but it is one warmup-free repeat and not stable timing evidence. For the directed family, CUDA float32 BiCGStab breakdown now falls back to the stationary iteration `x <- x + (b-Ax)` only for the certified Tutte operator. Exact-arithmetic row substochasticity plus boundary reachability gives `rho(P_II)<1`, but rounded float32 arithmetic and the `20*max_iter` budget have no uniform rate guarantee; only the represented true residual can accept a result. The N=49 stress used fallback in two of four primal and all four adjoint layer calls, reaching 2,930/6,524 fallback iterations and `37.18 s`; its independent CPU-double replay residual `1.001176e-5` is slightly above the native float32 acceptance threshold and is recorded as a mixed-precision discrepancy. An independent checker found and reproduced a fail-open counterexample where a finite `3e38` float32 right-hand side overflowed the fallback norm and returned an unconverged zero solution. Initial/right-residual norm guards and a mandatory converged-report check now fail closed; a red-then-green regression covers it. The symmetric reliable-update mask was also intersected with active right-hand sides so zero/already-converged columns no longer force an extra true-residual matvec every iteration. Latest independent verdicts are PASS for the bounded directed correctness claim and CONDITIONAL for the symmetric default and broader performance/stability claims; formal multi-seed matrix evidence remains pending.

Cross-version composition correction (2026-09-21T19:29:22Z): The first complete Linux runs falsified the apparently exact fixed-modulus construction: converting an inverse-softplus value to float32 and applying Torch `softplus` happened to return exactly one on the Windows build but an adjacent representable value on both remote Torch builds, so a strict `height == 1` check rejected every float32 composition. `StructuredRectangleBoundary.at_height` now realizes an already represented positive height directly; square composition passes the binary-exact value `1.0` and never calls inverse softplus. Independent broadcast, joint-logit/height VJP, invalid-input, float32/64 corner, and no-inverse tests passed. The original failing environments then gave `125 passed, 7 skipped` on Element CPU and `132 passed` on AI A6000 at commit `ee733b5`. This is an exact represented-height statement, not a claim that arbitrary learned softplus moduli are exactly prescribed real numbers.

Reference-and-sweep result (2026-09-21T19:29:22Z): The engineering comparison now includes a genuine legacy reference adapter rather than renaming the improved backend. Each nonempty CPU sample invokes the Phase-III legacy implementation, realizes probabilities/assembly in NumPy float64, factors `A` and `A^T` separately during forward, and reuses the stored transpose factor only in backward. Improved direct instead factors once and calls the same factor with transpose solve. An independent spy confirmed three batch samples cause six legacy factorizations in exact `A,A^T` pairs; independent dense/VJP/finite-difference checks passed. Review exposed a legacy edge case where a finite logit `-744` had positive exponential before normalization but zero supported probability afterwards. The adapter now checks the actual normalized supported entries before any factorization and fails closed; boundary-only disks skip this empty normalization and retain zero factorizations. Final independent verdict: PASS, with `31 passed`. The resumable sweep writes each complete receipt to one flushed/fsynced JSONL in a fresh process and derives one compact CSV. A checker found that an invalid zero-layer failure was vacuously summarized as topology `True`; topology summary now requires positive expected layer/batch counts, nonempty evidence, and exact per-layer cardinality. Final sweep verdict: PASS, `14 passed`; CSV itself is not atomic but is reproducible from the durable JSONL. Element and AI each passed the combined reference/harness/sweep set (`45 passed`) at commit `02019fe`.

Instance-provenance result (2026-09-21T19:29:22Z): Each instance receipt now records ordinary Git `HEAD` plus shape, sum, and L2 summaries of the one prebuilt target object passed to all requested backends. Independent two-process regeneration matched these values and confirmed they are computed before training without entering gradients or training timing. They permit a transparent deterministic cross-host comparison but are not a cryptographic identity; Python `object_identity` is meaningful only among backends in one process, and Git `HEAD` alone does not certify a clean worktree. Formal thresholds were fixed before the main runs from a separate seed-20260921 N=17 pilot: map MSE `1e-4`, image MSE `1e-3`, Adam 40 steps at learning rate `0.03`, and LBFGS five outer steps at learning rate `0.8`; the formal seed is `20260922` and target strength `0.25`.

UTC time: 2026-09-21T19:57:05Z
Route: I — checkerboard instance follow-up
Question: Is the 40-step checkerboard failure a short optimization budget, or a persistent raw-latent optimization/expressivity limitation?
Exact claim/hypothesis: Reusing the same seed-20260922, strength-0.25 target and fixed 256-squared checkerboard pair for 200 Adam steps can distinguish continued convergence from an early plateau without changing the decoder or threshold after observing the result.
Assumptions: the target object is generated once and shared literally between the directed and symmetric runs; learning rate remains `0.03`; every accepted iterate passes the same P1 topology audit; the image MSE threshold remains the predeclared `1e-3`.
What would falsify it: a changed target summary, a decoder failure, any uncertified accepted iterate, or a trace whose solve/time counts do not correspond to actual evaluations.
Smallest decisive test: one N=25, 256-squared, 200-step shared-target receipt containing both GPU backends and the full trace; compare the first 40 evaluations and the final/best objective with the formal 40-step receipt.
Prior work: the formal 40-step run ended at `0.10827` for directed and `0.01124` for symmetric, so neither reached `1e-3`, although symmetric was substantially closer.

UTC time: 2026-09-21T19:56:18Z
Route: I — symmetric CUDA convergence-headroom investigation
Question: Why do two normal-strength N=49, four-layer symmetric CUDA sweep cases reach an apparent float32 CG convergence at iteration 161 yet fail the mandatory final represented true-residual check at a value printed as `1.000e-5`?
Exact claim/hypothesis: The solver currently deactivates a right-hand side at the public threshold and later re-applies the scatter-add operator for the final check. CUDA `index_add_` accumulation order and ordinary float32 reevaluation error can move a boundary-line residual from just below to just above the same threshold. If repeated instrumented evaluations confirm that mechanism, solving to a strictly smaller internal threshold while retaining the requested tolerance for the final acceptance check should provide numerical headroom without weakening the external contract, storing Krylov history, or changing exact zero-right-hand-side behavior.
Assumptions: the reported cases use identical weights, right-hand sides, dtype, seed, and physical GPU; every accepted return remains certified by a freshly recomputed represented residual no larger than the requested tolerance; the reduced conductance matrix remains SPD.
What would falsify it: the exact configurations do not reproduce; repeated operator applications are bitwise stable while the recurrence is genuinely stalled above tolerance; a smaller internal threshold still fails at the same rate; zero right-hand sides cease to return immediately; memory ceases to be constant in iteration count; or the added work is materially disproportionate.
Smallest decisive test: on idle physical GPU 6, repeatedly run only N=49, seed 1701, strength 0.15, four layers, batches 4 and 8 at 256 squared; separately capture the residual at deactivation and multiple immediate true-residual reevaluations, then test one predeclared internal safety factor against the unchanged public `1e-5` final criterion and record iterations/timing/failure rate.
Prior work: the reliable-update CG already replaces candidate and periodic recursive residuals by `b-Ax`, and the N=49 strict-`2e-6` failure established that float32 accuracy has a real floor; this investigation concerns borderline availability at the existing public `1e-5` tolerance, not a stronger accuracy theorem.

UTC time: 2026-09-21T20:03:06Z
Route: I — million-query memory probe
Question: Does the fixed-query symmetric decoder remain practically memory-bounded when one dense deformation contains about one million pixels rather than 256 or 512 squared samples?
Exact claim/hypothesis: At 1024 squared queries, the dense coordinate tensors rather than the control solve should dominate incremental GPU memory, while the implicit CG state should remain independent of its iteration count; one- and four-layer N=25/N=49 cases should either finish with explicit peak allocation/topology receipts or fail closed.
Assumptions: batch one, seed 1701, strength 0.15, float32 CUDA, exact unit-square layers, one warmup and two timed repeats; this is a coordinate-map forward/loss/backward probe, not an image-registration optimizer.
What would falsify it: out-of-memory, topology failure, nonfinite gradients, unexplained memory growth unrelated to layers/control/query tensors, or a receipt that omits a failure.
Smallest decisive test: four fresh-process symmetric configurations `(N,L) in {(25,1),(25,4),(49,1),(49,4)}` with batch one and 1024-squared fixed queries on an idle A6000, retaining the same solver settings as the formal engineering sweep.
Prior work: the complete 256/512 matrix reached roughly 781 MiB peak allocation in its largest successful batch-eight/four-layer cases; no 1024-squared receipt yet exists.

UTC time: 2026-09-21T20:30:07Z
Route: I — explicit-float64 directed conditioning extension
Question: Does the existing explicit float64 directed matrix-free path remain available at N=49 when Gaussian logit strength increases beyond 0.15, and are observed failures in the solver, realized boundary order, or returned-map topology?
Exact claim/hypothesis: With the unchanged float64 BiCGStab tolerance `rtol=1e-10`, `atol=0`, and 500-iteration limit, the 24 prescribed configurations either complete the full coordinate forward/surrogate-loss/backward workload and independent per-layer P1 audit, or retain an explicit first-failure receipt. Higher strength may expose conditioning or near-degenerate target geometry even though normal-strength float64 completed 18/18 cases.
Assumptions: remote source HEAD is `e31586cc9e7012b7d16bb52dce9fad3061f3aa82` without tracked modifications; N=49, R=256, float64 input/solve/output; strength in {0.5,1.0,3.0}, B in {1,4}, L in {1,4}, seeds {1701,1702}; physical AI GPU1 observed idle (11 MiB, 0 percent) before launch, with GPU6 excluded. Each configuration uses one fresh subprocess, one warmup, and one measured repeat to keep the extension bounded. No float64 stationary fallback exists or will be added.
What would falsify it: missing or duplicated configuration receipts; altered tolerance, iteration budget, dtype, or latent distribution; silent failure omission; a reported success with unmet represented residual/finite-gradient/topology checks; or calling these coordinate-surrogate times image-optimization times. Completion at every configuration is an empirical question, not an assumed outcome.
Smallest decisive test: append the three eight-configuration strength blocks using the existing resumable engineering sweep, preserving every exception in one compact JSONL/CSV; summarize exact first-failure categories, native and independently replayed residuals, minimum topology margins, synchronized full-stack timing, and allocator/process-memory scopes. Budget target is about ten minutes; use one measured repeat rather than changing numerical acceptance.
Prior work: the normal-strength float32 N49 directed matrix failed 18/18, while explicit float64 at strength 0.15 completed 18/18 plus one 512-square case with no fallback. The earlier float32 adversarial sweep exposed distinct stationary-budget, boundary-order, and positive-face-screen failures. This extension does not erase those results or imply universal positive-weight availability.

Symmetric CUDA headroom result (2026-09-21T20:34:08Z): Exact unmodified warmup-one/repeat-two reproductions on otherwise idle physical AI GPU 6 gave B=4 `5/5` successes and B=8 `4/5`, with the failure occurring in measurement at iteration 144 and `1.000e-5` when rounded to three significant digits. The diagnostic recurrence then used one forward/backward evaluation per fresh process: B=4 succeeded `4/5` and B=8 `7/10`. Across all 114 reached CG solves, 114 had bitwise-different results when the same represented `A x` was evaluated 32 times by CUDA `index_add_`. In the four failed solves, a right-hand side had been deactivated at no more than `0.9999971` times the requested threshold, while the first final residual was `1.0000158e-5` to `1.0001745e-5`; repeated final checks reached `1.0009400e-5`. This directly localizes the failure to nondeterministic float32 scatter accumulation at a deliberately fail-closed threshold boundary rather than to a fixed nonconvergent SPD system.

Based on the observed sub-`0.1%` upward spread, a `0.99` internal threshold was fixed before confirmation. It does not alter the requested `1e-5` threshold used by the fresh final true-residual acceptance check, and it is applied only to CUDA float32; CPU and float64 paths are unchanged. The instrumented candidate completed B=4 `5/5` and B=8 `10/10` runs, all `120/120` reached CG invocations, with maximum first-final residual `9.9045965e-6` and maximum over 32 immediate rechecks `9.9082818e-6`. The failure-truncated unmodified diagnostic cohort has solve-weighted mean iteration counts `151.54054` (B=4) and `152.35065` (B=8), versus `152.375` and `152.8375` with the candidate; these are per-CG-invocation maximum loop counts, not per-column iteration means. The historical production-patch run then completed the exact warmup-one/repeat-two protocol B=4 `5/5` and B=8 `5/5`; maximum forward/adjoint solver-reported residuals were `9.8937717e-6`/`9.8856681e-6` for B=4 and `9.8989858e-6`/`9.8998644e-6` for B=8. Observed successful end-to-end means changed from `8.2872` to `8.3533` seconds for B=4 and from `15.4844` to `15.7957` seconds for B=8; these single-host sequential means include load/order noise and are not a causal speed estimate. TDD evidence is local RED then GREEN; the complete local symmetric suite is `16 passed, 1 CUDA-unavailable skip`, and physical GPU 6 is `17 passed`. Existing zero-right-hand-side and constant-saved-state tests remain green. Compact evidence, including every failed run and four decisive failure records, is in `raw_results/route1_symmetric_gpu6_reliability.jsonl` and `.csv`; exact phases now separate forward and adjoint residuals, diagnostic phases label all-solve first-final residuals, failure-truncated iteration means are solve weighted, and unmeasured bitwise variability is null rather than zero. The historical patch receipts reported HEAD `02019fe` without dirty-state metadata and remain only as mechanism evidence.

Clean symmetric closure (2026-09-21T21:14:32Z): The correction was committed as `e688abc` and rerun from a detached checkout whose before/after HEAD and empty porcelain status are embedded in every receipt. The original 90-cell normal-strength symmetric product completed `90/90`, including both formerly failing N49/L4/R256 cells; a separate five-process-per-batch cohort completed `10/10`. Across both cohorts all `1,150/1,150` final layer/sample maps were certified and gradients were finite. Full-matrix native primal/adjoint and independent CPU-double primal replay maxima were `9.8962737e-6`/`9.9085782e-6`/`9.9016579e-6`; repeated-cohort maxima were `9.8999753e-6`/`9.9021499e-6`/`9.9027082e-6`. The independent delta checker recomputed the 90-cell identity, 10 repeated processes, topology/residual/timing/memory extrema, solve-weighted historical statistics, null unmeasured fields, clean provenance, and both JSONL-to-CSV projections, then changed the prior CONDITIONAL verdict to **PASS**. This is bounded normal-strength A6000 evidence, not universal convergence, deterministic CUDA arithmetic, or stronger VJP/map accuracy.

Route I independent closure (2026-09-21T22:04:59Z): The real wall-clock guard reported 21,273 elapsed seconds, beyond the required T+5h45 transition boundary. The independent checker completed `04_tutte_checker.md` with a bounded **PASS** after the official-source audit, theorem/code/formula checks, full application delta review, clean symmetric rerun, and preserved negative results. The verdict means: every accepted control layer is a numerically screened P1 map covered by the stated exact Tutte--Floater hypotheses; coordinate composition is a homeomorphism on a refined affine partition; first-order implicit VJPs are implemented and tested; and the required engineering/instance experiments ran. It does not mean exact predicates for all floating inputs, original-mesh P1 validity of a sampled composition, universal Krylov availability, higher-order differentiation, a trained CNN, prescribed Beltrami recovery, or Phase V completion. Route II may begin only from this timestamp/checker closure.

UTC time: 2026-09-21T22:13:40Z
Route: II-A/B/E — MVC canonical subset and exact encode--decode
Question: For which accepted planar P1 Tutte maps does the local mean-value-coordinate encoder define a single canonical positive row representation whose decode is exactly the original map in exact arithmetic?
Exact claim/hypothesis: Let a triangulated topological disk have fixed cyclic one-ring order at each interior vertex, and let an embedding \(Y\) make every interior one-ring a simple star-shaped polygon with \(Y_i\) strictly inside it, with every incident edge nonzero and every consecutive signed angle in \((0,\pi)\). The normalized mean-value weights are then finite and strictly positive, reproduce \(Y_i\), and, with the unchanged boundary \(Y_B\), satisfy \(D(E_{\rm MVC}(Y))=Y\) whenever the reduced positive-Tutte system is nonsingular. Thus \(E_{\rm MVC}\) is injective on this explicitly defined map domain and \(D\) restricted to its image is its inverse; no injectivity of \(D\) on the full positive-weight space is claimed.
Assumptions: certified disk combinatorics and cyclic orders; consistent orientation; nonzero incident lengths; nondegenerate angle sums; finite double arithmetic for the numerical test; the decoder uses precisely the normalized supported weights returned by the encoder and the original boundary.
What would falsify it: an admissible one-ring yields a nonpositive/nonfinite MVC weight or nonzero barycentric residual; a decoded vertex differs beyond a conditioning-predicted floating error; two distinct admissible maps have the same encoded weights and boundary; or an accepted Route-I map violates the stated local hypotheses without being rejected by the MVC domain checker.
Smallest decisive test: analytically check a nonsymmetric convex fan and a degree-four redundant row; then encode/decode all certified Route-I maps at N=11,25,49 in float64, reporting barycentric residual, vertex error, minimum weight, logit spread, one-ring geometry, and system conditioning; preserve concave/non-star-shaped and nearly degenerate counterexamples.
Prior work: Floater mean value coordinates and barycentric-coordinate reproduction; Route-I certified Tutte decoder and returned-map topology screen; Phase-V plan sections 17--21. A targeted primary-source audit is required before theorem attribution.

UTC time: 2026-09-21T22:13:40Z
Route: II-G/H/I — covariance lift, gauge relation, and legal retraction
Question: Is the rowwise covariance formula a differentiable right inverse of the decoder differential, how does it differ from the derivative of MVC canonicalization, and does decoder retraction realize the requested map tangent without ever accepting an Euler-updated map?
Exact claim/hypothesis: At an interior row with positive probabilities \(p_{ij}\), centered neighbor vectors \(r_{ij}=Y_j-Y_i\), and nonsingular \(C_i=\sum_j p_{ij}r_{ij}r_{ij}^{\mathsf T}\), the zero-mean logit increment \(\delta\ell_{ij}=r_{ij}^{\mathsf T}C_i^{-1}(d_i-\sum_jp_{ij}d_j)\) satisfies the linearized barycentric equation. Assembled over all rows with the appropriate boundary tangent, it is one right inverse of the global decoder Jacobian. In general it is not \(dE_{\rm MVC}(Y)[d]\); if both are defined, their difference should lie in the decoder-Jacobian kernel. The retraction \(R_Y(\alpha d)=D(\operatorname{softmax}(E_{\rm MVC}(Y)+\alpha\delta\ell),b_\alpha)\) satisfies \(R_Y(0)=Y\) and \(\partial_\alpha R_Y(0)=d\), subject to the same solver and local-rank hypotheses.
Assumptions: differentiable interior point of the MVC domain; positive represented weights; full-rank two-dimensional covariance at every interior row; decoder Jacobian nonsingular for the fixed boundary problem; boundary tangent represented by the chosen boundary parameterization.
What would falsify it: a finite-difference decoder tangent disagrees with \(d\); the covariance formula is not the weighted minimum-norm row lift under its stated metric; MVC-JVP minus covariance lift has a material decoded tangent; \(R_Y(0)\neq Y\); or a returned accepted state bypasses the decoder/topology screen.
Smallest decisive test: derive the row constraint and Lagrange-multiplier solution on one irregular degree-five star; compare covariance lift, autograd MVC JVP, and an explicit decoder-Jacobian pseudoinverse in float64; then finite-difference the assembled retraction on a small nonsymmetric disk and retain a rank-deficient collinear-star counterexample.
Prior work: Phase-V plan sections 22--24; implicit Tutte VJP/JVP structure from Route I. No equation in the plan is treated as proved until independently rederived and checked.

UTC time: 2026-09-21T22:13:40Z
Route: II-M/N — differentiable MVC layers and fair instance optimization
Question: Can canonical projection \(P_{\rm MVC}=E_{\rm MVC}\circ D\) and covariance-retraction updates be inserted into a PyTorch graph with correct first-order gradients, and do they improve optimization under an identical decoder, target, initialization, and solve/evaluation budget?
Exact claim/hypothesis: A Torch-only MVC encoder using precomputed cyclic one-rings can canonicalize any admitted decoded map, preserve that map after re-decoding within solver/roundoff tolerance, and pass float64 gradcheck away from angle and rank degeneracies. A geometric update layer can map \((Y,d)\) or \((\ell,d)\) to a newly decoded, topology-screened state with an implicit backward. Under the fixed M-Opt-1 supervised-map and M-Opt-2 256-square image protocols, raw/bounded logits, row-softmax, MVC canonical ordinary-gradient, and MVC covariance-retraction variants can be compared without changing target, initialization, loss, optimizer budget, or acceptance rule; superiority is empirical rather than assumed.
Assumptions: fixed control topology/cyclic order; all variants call the same Route-I decoder and dense query table; one multi-RHS forward solve counts once; timing separates canonicalization, solve, interpolation/loss, and backward; failed steps are recorded rather than repaired post hoc.
What would falsify it: canonicalization changes the decoded map materially; gradcheck or directional derivatives fail; saved state grows with an unrolled iterative history; an optimization receipt changes target/budget between variants; an accepted iterate lacks the decoder's P1 certificate; or MVC variants do not improve any declared accuracy/solve/conditioning metric.
Smallest decisive test: double-precision M1/M2 gradcheck on N=11, a fixed supervised N=25 map fit, and the exact Route-I 256-square image pair with predeclared common budgets; report every solve, wall time, best/final loss, map error, induced Beltrami error, minimum determinant/area, logit spread, covariance/system conditioning, and peak memory.
Prior work: Route-I deterministic target and image benchmark, four hard-bijective backends, and mandatory instance receipts; Phase-V plan sections 25--30. No CNN experiment is justified until these instance-level layers are stable.

UTC time: 2026-09-21T22:13:40Z
Route: II-K/L — warm start and exact incremental correction
Question: When weights and/or boundary move, can a warm start or exact residual correction reduce work without changing the represented linear system or hiding global updates behind a local-only benchmark?
Exact claim/hypothesis: For old \(AY=B\) and new \(A'Y'=B'\), the correction \(\Delta=Y'-Y\) obeys exactly \(A'\Delta=(B'-B)-(A'-A)Y\). A Krylov solve initialized at \(Y\), or an exact solve of this correction equation, returns the same \(Y'\) as a cold solve up to the declared residual tolerance. Local row changes may additionally admit a verified low-rank Woodbury update for a direct solver, but global MVC re-encoding must be benchmarked as global and cannot be described as local.
Assumptions: identical represented \(A',B'\), same public residual check, immutable old accepted solution, explicit accounting of factorization/preparation and correction solves, and sufficiently small test updates only where a small-update experiment is claimed.
What would falsify it: the algebraic residual identity fails; warm/correction solutions differ beyond conditioning-scaled tolerance; solve-count or timing excludes necessary preparation; a global canonicalization is benchmarked only through cherry-picked local changes; or topology acceptance differs because one path skips the decoder screen.
Smallest decisive test: one local-row and one all-row perturbation on N=25/49; independently assemble both systems, verify the correction right-hand side, compare cold versus warm iterations/true residual/map error, and include an adverse large update where warm start provides no benefit.
Prior work: classical iterative warm starts and Sherman--Morrison--Woodbury algebra; Route-I direct and matrix-free solvers; Phase-V plan sections 26--27.

Result (2026-09-21T23:22:58Z): The committed M1/M2 benchmark (`b2972ea`) now separates encoder-only, complete `E_MVC o D`, and covariance-retraction forward/loss/backward timing, and records independent re-decode, solver residual, first variation, topology, and allocator memory. Local direct CPU completed all six bounded profiles `N:B=11:8,25:4,49:1` in float32/64. Fresh committed remote checkouts then passed the actual-CUDA contract tests 13/13 on both Turing A40-0 and AI A6000-0 and completed all six formal rows. Completion is not an accuracy verdict. On the A6000, float32 `N=49,B=1` had canonical re-decode maximum vertex error `4.066e-4`, central first-variation relative error `1.999e-1`, and projected-JVP relative error `3.377e-1` at the unchanged matrix-free `1e-5` residual contract. Float64 reduced those quantities to `1.329e-10`, `4.227e-5`, and `9.289e-8`. All returned maps remained certified in these rows. The A40/PyTorch-2.8 process also displayed much larger absolute peak allocation than A6000/PyTorch-2.4 (for `N=49,B=1,float64`, about `1152.65` versus `39.44` MiB); this is an observed runtime/version/device contrast, not yet an attribution. An explicit tighter-float32-tolerance sweep is required before deciding whether float64 is necessary.

Independent protocol finding (2026-09-21T23:22:58Z): A fresh read-only review judged the first O1--O4 runner **FAIL** as a plan-compliant fair comparison even though its fixed-boundary primitives and seven focused tests were mostly correct. Equal outer steps produced unequal global-solve totals; the prebuilt-target validator admitted inconsistent control/dense authorities; failure-path counters counted attempted rather than completed solves; and several provenance/timing labels were incomplete. The adjudicated protocol retains common 40-step trajectories only as secondary evidence and adds an exact primary budget of 83 global solves: O1/O2 use 41 updates (`1+2S`), O3 uses 27 (`2+3S`), and O4 uses 81 (`2+S`). Target setup remains separate. Formal 256-square runs are forbidden until the full validator, actual counters, independent re-decode provenance, timing scopes, failure phases, covariance labels, O3 moment policy, and checker re-review are closed.

UTC time: 2026-09-21T23:45:00Z
Route: II-N — predeclared O1--O4 realistic instance comparison
Question: Under both an exact common global-solve budget and a separately labeled common outer-step budget, do MVC projection or covariance retraction improve fixed-boundary supervised-map and image fitting over raw positive Tutte coordinates?
Exact claim/hypothesis: No method is assumed superior. A shared numeric learning rate is only a robustness slice because logits, bounded raw weights, and vertex coordinates have different units. The primary comparison is the state at exactly 83 completed global solves; the common 40-update state is secondary. Every ranking must retain failures and report objective, map/Beltrami error, topology, conditioning, actual/attempted solves, Krylov work, wall time, and memory.
Assumptions: one CPU-float64 Route-I target authority per task; identical fixed target boundary, uniform decoded initialization, P1 dense table, Adam hyperparameters, loss, and backend settings; target generation excluded and reported separately; every accepted map comes from the decoder and is independently re-decoded/audited.
What would falsify it: any method receives a different target, boundary, initialization, loss, hidden solve, learning-rate selection budget, or acceptance rule; a primary row is compared below/above 83 completed solves; target control and dense tensors disagree; an accepted state bypasses the decoder; failure or timing/memory data are discarded; or conclusions silently switch from shared-rate to tuned-rate evidence.
Smallest decisive test: use pilot seed 20260921 at N=17 and R=128 for both supervised map and medical phantom. Test shared learning rates `{0.003,0.01,0.03}` to an exact 23-solve slice (O1/O2 step11, O3 step7, O4 step21). Among candidates with no failure/topology violation, select the rate minimizing the worst final/initial objective ratio over both tasks and all methods; ties choose the smaller rate. Freeze it before formal seed 20260922, N=25, R=256, strength0.25, 81 requested updates, primary solve83 and secondary step40, thresholds1e-4 map and1e-3 image. CPU float64 is the accuracy authority; GPU float64 is a separately labeled performance replication only after local closure.
Prior work: independently checked fixed-boundary O1--O4 runner at commit 712c525; Route-I target/image convention; Phase-V plan sections5,25,29. Any separately tuned per-method learning rates require the same candidate set and pilot budget and are reported as a distinct cohort, never substituted for the shared-rate primary cohort.

Protocol clarification before any pilot result (2026-09-22T00:17:10Z): The pilot uses target strength `0.25`, the same target-strength convention already frozen for the formal cohort. It requests 21 updates so that all four methods can reach the exact 23-completed-global-solve slice, disables the inapplicable formal step-40 slice, and uses no objective threshold for early stopping. Learning-rate selection is computed only from the exact 23-solve rows; wall time and intermediate trajectories are retained but cannot change the selection rule.

UTC time: 2026-09-22T00:49:41Z
Route: II-N — shared-rate pilot negative result and bounded recovery
Question: Does the failure of the originally preregistered shared learning rates come from the covariance-retraction step scale, and is there a smaller unchanged shared rate that lets all four methods reach the exact 23-solve comparison on both tasks?
Exact claim/hypothesis: The original candidate set has no eligible winner: at image registration, O4 failed before 23 completed solves for each of `0.003`, `0.01`, and `0.03`, whereas all map rows completed. This is a negative result, not permission to select a surviving method or map-only rate. A separately labelled recovery cohort at shared rates `{0.0001,0.0003,0.001}` tests only whether a smaller common scale restores protocol completion without changing O1--O4. If more than one rate is valid, the unchanged minimax final/initial objective-ratio rule over both tasks and all methods selects the rate, with ties resolved toward the smaller value.
Assumptions: seed `20260921`, control side 17, dense resolution 128, target strength `0.25`, 21 requested updates, exact 23-solve slice, CPU float64 direct solver, no early-stopping threshold, and the same generated-once target convention remain unchanged. The recovery cohort is not retroactively part of the original candidate set, and shared numeric rates still compare robustness across coordinate systems with different units rather than proving intrinsic optimizer superiority.
What would falsify it: any recovery run changes the target, method, solver, budget, or failure rule; any method fails or loses topology before its 23-solve row; or the selected rate is chosen from intermediate trajectories rather than the frozen minimax rule.
Smallest decisive test: run the same two tasks at each of the three lower shared rates and retain all six receipts. If none is eligible, do not continue shrinking rates indefinitely; instead treat the shared-rate protocol as failed and separately formulate a geometry-aware O4 trust-region or per-method step-scale experiment.
Prior work: the six original receipts `route2_mvc_instance_pilot_{map,image}_lr{0p003,0p01,0p03}_cpu_b7169ef.json`. At image rate `0.003`, O4 remained certified through step 14 but its maximum canonical covariance condition grew from `2.54` initially to `5924.46`, its minimum area ratio fell to `8.60e-5`, and the next retraction produced unsupported softmax probabilities. Rates `0.01` and `0.03` failed earlier at steps 5 and 3. These observations motivate only the bounded lower-rate test; they do not set its winner.

Recovery result and formal freeze (2026-09-22T00:52:05Z): All six lower-rate receipts completed with zero method, audit, protocol, or topology failures. The preregistered worst final/initial objective ratios over both tasks and all methods were `0.9662170231` at `0.0001`, `0.8984545177` at `0.0003`, and `0.6572357675` at `0.001`. Therefore the shared learning rate for the formal CPU-float64 cohort is frozen at `0.001` before any formal seed-20260922 result is computed. This selected rate is explicitly a recovery-protocol robustness setting, not a winner from the invalid original candidate set and not a per-method optimum.

UTC time: 2026-09-22T00:54:00Z
Route: II-G/H, plan section 22.3 — explicit decoder Moore--Penrose comparison
Question: For one fixed-boundary decoded map and one admissible zero-boundary vertex tangent, how do the Euclidean Moore--Penrose lift of the complete decoder Jacobian, the MVC encoder derivative, and the covariance lift compare after all three are represented in the same supported, arithmetic-zero-row-mean logit gauge?
Exact claim/hypothesis: Let (Q) be a row-block orthonormal basis for supported logit increments whose unweighted supported-row sum is zero, and let (J=\partial_{\theta}D(\ell+Q\theta,b)|_{\theta=0}) include every output vertex coordinate while holding (b) fixed. If (\operatorname{rank}J=2|I|) under a stated singular-value tolerance and (d_B=0), then (QJ^+d) is the unique Euclidean-minimum-norm lift in this gauge. The arithmetic-gauge representatives of (dE_{\rm MVC}(Y)[d]) and (L_Yd) also satisfy (J_Dz=d), but generally differ from (QJ^+d) and from each other by decoder-kernel vectors. The native covariance representative, before arithmetic recentering, remains the probability-weighted-minimum-norm representative derived in Proposition 5.2; recentering it changes that metric norm and is performed only to make the three-way gauge comparison unambiguous.
Assumptions: CPU float64 direct decoder; one nonsymmetric variable-degree disk with at least one barycentrically redundant row; canonical MVC base logits; strictly positive supported probabilities; fixed boundary and exactly zero boundary tangent; unsupported padded slots fixed at zero; an orthonormal Helmert basis on each supported row; SVD rank tolerance (\tau=64\epsilon\max(m,n)\sigma_{\max}), recorded rather than inferred from a default; and a central finite-difference check of the assembled full Jacobian.
What would falsify it: any unsupported slot or row-shift coordinate enters the Jacobian; the supplied boundary tangent is nonzero; the SVD rank is below (2|I|); the autodiff and finite-difference Jacobians materially disagree; any lift has a material decoded-tangent residual; the purported Moore--Penrose lift has larger Euclidean norm than another feasible zero-row-mean lift; or a pairwise lift difference has material image under the same full decoder Jacobian.
Smallest decisive test: on the existing two-interior-vertex variable-degree disk, explicitly assemble (Q), the full (2|V|\)-by-(\sum_i(\deg i-1)) decoder Jacobian, its thresholded SVD pseudoinverse, the MVC JVP, and both native and arithmetic-gauge covariance lifts. Report rank/tolerance/singular values, boundary and support residuals, decoded tangent residuals, Euclidean and probability-weighted norms, all pairwise differences, all pairwise kernel residuals, and an independent finite-difference Jacobian error in one compact JSON receipt.
Prior work: Phase-V plan sections 22--23; the rowwise weighted variational proof in `05_mvc_theory.md` and `06_mvc_optimization.md`; the existing two-lift test in `test_phase5_mvc_lift_comparison.py`; and the supported-space weighted-pseudoinverse check in `test_mvc_retraction.py`. Neither earlier test constructs the complete common-gauge Euclidean Moore--Penrose comparison requested here.

UTC time: 2026-09-22T00:59:22Z
Route: II-O4 follow-up variant -- geometry-aware latent trust backtracking
Question: Is the formal image failure of baseline O4 at step 18 caused by taking the full covariance-lifted logit increment outside the decoder's strictly-positive, certified Tutte domain, and can a separately named fail-closed variant cross that state by shrinking only this latent increment?
Exact claim/hypothesis: Keep the accepted canonical logits, vertex-space Adam moments, covariance lift, fixed boundary, decoder, and topology screen unchanged. For the follow-up variant only, try increments `alpha * delta_logits` in the fixed sequence `alpha = 1, beta, ..., beta^(K-1)`. Every attempted decoder call increments `primal_attempts`; every successfully completed decoder call increments both `primal_solves` and the global-solve budget, even when its geometry audit rejects that trial. Accept the first trial whose independent P1 audit retains the existing global-injectivity certificate and whose minimum area ratio is at least a fixed fraction `rho` of the last accepted state's minimum area ratio. A failed decode, failed audit, or insufficient area margin causes geometric backtracking, never post-hoc vertex repair. Exhaustion fails closed and preserves the last accepted state. Baseline `O4_covariance_retraction` and its defaults/receipts remain unchanged; the new method is reported as `O4_covariance_retraction_trust` with per-step trial count and accepted scale.
Assumptions: fixed-boundary structured-disk problems; CPU float64 direct decoder for decisive tests; `0 < beta < 1`, positive finite `rho <= 1`, positive integer trial limit; the full lifted increment is finite before backtracking; and each successful trial is auditable by the same `compute_p1_map_metrics` certificate used elsewhere. This is a discrete fixed-mesh safeguard, not a continuum quasiconformal theorem and not an equal-global-solve comparison to baseline after rejected trials.
What would falsify it: the baseline behavior or counters change when the variant is not selected; any attempted/completed trial solve is omitted from accounting; an accepted trial fails the unchanged topology certificate or violates the declared relative area margin; the variant silently accepts `alpha=0`, mutates the accepted state after exhaustion, or repairs a folded map; the N=17 failure cannot be crossed within the preregistered bounded sequence; or the N=25 formal failure persists despite available smaller certified trials.
Smallest decisive test: first inject a deterministic decoder that rejects the full step but accepts a half step and assert exact attempts/completions, accepted scale, and unchanged baseline behavior. Then rerun the original N=17 image case at learning rate `0.003` and the N=25/R=256/seed=20260922 image case at learning rate `0.001`, recording trial scales, extra solves, topology, area margin, objective, and failure without overwriting any original receipt.
Prior work: original pilot failures at image learning rates `0.003/0.01/0.03`; recovery success at N=17 and shared rate `0.001`; formal N=25 image receipt `route2_mvc_instance_formal_image_cpu_b7169ef.json`, where baseline O4 preserves a certified step-17 state with covariance condition `181.68` and minimum area ratio `0.0025158`, then fails its step-18 decoder attempt. Classical trust-region/backtracking logic motivates scaling a proposed increment; it does not justify weakening Tutte positivity or topology predicates.

Trust counterexample (2026-09-22T01:11:13Z): The bounded rule behaved correctly but did not solve the optimization pathology. On the original N=17/R=128 image case at rate `0.003`, it crossed the baseline step-15 failure and completed 21 certified steps, but required 69 trial attempts, 66 completed trial solves, 45 extra completed solves, and a minimum accepted scale of `2^-9`; the final minimum area ratio was only `2.706e-4`. On the formal N=25/R=256/seed-20260922 image case at rate `0.001`, it delayed the baseline failure from step 18 to step 32 but exhausted all 12 trials. Its last accepted step 31 was still certified, yet used scale `2^-11`, had minimum area ratio `3.224e-5`, covariance condition `1.321e4`, and objective `0.00458555`; 69 completed solves were extra. Thus a relative per-step area floor prevents one catastrophic jump but does not prevent cumulative convergence toward the boundary of the discrete-homeomorphism set. Increasing the trial limit would mainly buy vanishing updates and more solves, not evidence of a fast stable layer. The variant is retained as a precise negative-control mechanism, not substituted for baseline O4 or called a fix. It is not exact-budget evidence: accepted states jump from 80 to 86 completed solves, so exact 83 is absent; the last accepted state has 97 solves and the terminal counter reaches 102 only after failure trials. The N=17 trace ends at 68 solves. The receipts encode these distinctions explicitly and never substitute an outer-step or first-above-budget row for exact 83.

UTC time: 2026-09-22T01:11:13Z
Route: II-O4 follow-up -- per-method step-scale sensitivity after trust counterexample
Question: Does baseline O4's formal image failure primarily reflect a shared numeric learning rate that is too large in vertex-coordinate units, rather than a decoder-specific numerical accident?
Exact claim/hypothesis: Run baseline `O4_covariance_retraction` alone, with no trust trials or topology changes, at the preregistered O4-specific rates `{0.0003, 0.0001, 0.00003}`. These runs are a separately labelled sensitivity cohort and cannot replace the shared-rate O1--O4 comparison. If a smaller rate completes all 81 steps with every accepted P1 state certified, it establishes only step-scale sensitivity; it does not establish optimizer superiority, equal-solve fairness, or a solved fast-layer problem.
Assumptions: the formal N=25/R=256/seed-20260922 medical-phantom target, target strength `0.25`, fixed target boundary, CPU float64 direct decoder, 81 requested updates, identical generated-once target authority, unchanged covariance limit and audit, and no early stopping. The positive formal map baseline at shared rate `0.001` and the negative CPU/A6000 image baselines remain separate immutable evidence.
What would falsify it: any target/method/backend/topology predicate changes between rates; trust backtracking is enabled; failed rates are omitted; a run is called successful without 81 accepted certified steps; or a surviving smaller rate is retroactively used in the primary equal-numeric-rate comparison.
Smallest decisive test: retain all three N=25 image receipts and compare completion, first failure, final/initial objective ratio, minimum area ratio, maximum canonical covariance condition, and actual primal attempts/solves. Stop after this bounded set rather than shrinking indefinitely.
Prior work: the shared-rate recovery cohort, baseline formal CPU/A6000 step-18 failures, the positive 81-step formal map result at rate `0.001`, and the bounded trust counterexample immediately above.

Per-method sensitivity result (2026-09-22T01:19:34Z): All three preregistered rates were retained. Baseline O4 at `0.0003` failed its step-53 decoder after 52 certified updates; its final/initial objective ratio was `0.411183`, minimum area ratio `3.825e-6`, maximum canonical covariance condition `1.506e5`, and primal attempts/completions were `55/54`. Rates `0.0001` and `0.00003` both completed all 81 steps and exact 83 solves with every iterate certified. Their respective final/initial objective ratios were `0.669337` and `0.888633`, minimum area ratios `0.339922` and `0.549375`, and maximum covariance conditions `3.4353` and `3.0435`. This bounded result supports strong O4 step-scale sensitivity: `0.0001` is the best tested completion/optimization compromise, but it is not inserted into the shared-rate primary cohort. A post-change formal-map regression at baseline rate `0.001` reproduced the original objective `4.8074104943e-7`, final minimum area ratio `0.4442842504`, final covariance condition `4.0730594`, final-state numeric summaries, and `83/83` primal attempts/completions exactly. Thus the opt-in trust variant did not alter baseline O4's positive map behavior.

Plan-section-22.3 result (2026-09-22T01:06:11Z): The explicit CPU-float64 diagnostic assembled the full 12-output by 6-coordinate fixed-boundary decoder Jacobian after deleting unsupported slots and imposing an orthonormal arithmetic-zero-row-mean Helmert gauge. Its boundary rows were exactly zero. Under the preregistered (6.9720e-14) SVD tolerance, the singular values were (0.4088450, 0.3689694, 0.1942412, 0.1501054, 1.8298e-17, 1.6853e-17), hence rank four and common-gauge kernel dimension two. A central finite-difference reassembly differed by (6.925e-11) in relative Frobenius norm. The Moore--Penrose, MVC-derivative, and arithmetic-recentered covariance lifts had Euclidean norms (0.2435816, 0.2601148, 0.2437569), respectively, while every analytic decoded-tangent residual was below (2.1e-17). Their pairwise Euclidean distances were (0.0912564, 0.00924179, 0.0827460), but all pairwise decoder-kernel residuals were below (3.1e-17). The native covariance representative was kept separate: its probability-weighted row-mean residual was (3.47e-18), its unweighted row-sum maximum was (0.0229488), and its weighted norm (0.1231707) increased to (0.1234326) after arithmetic recentering. A second Jacobian assembled directly on all eight supported raw-logit coordinates produced the weighted pseudoinverse (W^{-1/2}(J W^{-1/2})^+ d), which differed from the native covariance formula by (1.30e-16) and had decoded residual (2.64e-17). Thus the Euclidean common-gauge Moore--Penrose and native probability-weighted covariance minimum-norm statements are both numerically verified under their stated hypotheses but are not the same claim. Focused TDD evidence is `3 passed`; the CLI also passes in a fresh Windows process with all three MKL/OMP thread overrides removed, using a real NumPy-LAPACK bootstrap rather than the unsafe duplicate-runtime override. This is a tiny differential closure only, not a realistic-resolution benchmark or Route-II verdict. Independent checker review is still pending.

Formal shared-rate result (2026-09-22T01:57:25Z): The original pilot had no admissible learning rate because image O4 failed for every preregistered rate. The bounded recovery froze the shared rate at `0.001` before formal evaluation. Clean detached-worktree CPU-float64 receipts at commit `b7169ef` (`dirty=false`) then ran seed `20260922`, control side 25, resolution 256, strength `0.25`, 81 updates, exact 83-solve primary slices and step-40 secondary slices. On supervised map fitting, all methods completed and the exact-83 objectives were `7.00551e-5`, `4.96621e-5`, `6.76226e-5`, and `4.80741e-7` for O1--O4; the corresponding objective ratios were `0.140328`, `0.0994786`, `0.135455`, and `0.000962976`, with minimum area ratios `0.454326`, `0.468556`, `0.461943`, and `0.444284`. O4 reached the `1e-4` threshold at 23 solves, O2 at 23, O1 at 43, and O3 at 56. On the medical phantom, O1--O3 retained certified exact-83 states with objectives `0.00181334`, `0.00121600`, and `0.00175008`; O2 and O3 reached the `1e-3` threshold later at 101 and 152 solves, while O1 did not reach it in 81 updates. Baseline O4 instead failed closed on the attempted step 18 because the returned P1 map had a nonpositive face. Its last accepted step 17 remained certified with objective `0.00433907`, minimum area ratio `0.00251579`, and covariance condition `181.682`; exact-83 and step-40 rows are therefore absent rather than imputed. Dirty first-pass CPU receipts reproduce every objective trajectory bit-for-bit but are not provenance authorities. Approximate process-lifetime high-water memory was 297 MiB and is not attributable per method. This is a robustness comparison at one shared numeric rate across parameterizations with different units, not an intrinsic optimizer-efficiency ranking.

UTC time: 2026-09-22T01:57:25Z
Route: II-N — residual-conditioned independent GPU replay audit
Question: When an A6000 float64 matrix-free re-decode differs from the accepted map by approximately `8.4e-10`, is this a genuine state mismatch or a converged iterative approximation that the former direct-solver-scale absolute tolerance falsely rejects?
Exact claim/hypothesis: For the represented reduced system \(AX=B\), a CPU-float64 direct replay is the independent authority. Accepted and independently re-decoded states must each satisfy the represented residual contract, lie within a sufficient \(\lVert A^{-1}\rVert_2\)-weighted residual bound from that authority, preserve the fixed boundary, and independently pass the unchanged global P1 topology certificate. A matrix-free run must also report converged true residuals. Exact dense singular values may certify \(\lVert A^{-1}\rVert_2\) only when the number of interior rows does not exceed an explicit audit limit; above that limit the audit must skip the \(O(I^2)\)-storage, \(O(I^3)\)-work SVD and fail closed.
Assumptions: the final logits and boundary are replayed without optimization; the reduced matrix is nonsingular; CPU float64 direct solve is an audit authority rather than a scalable neural-layer path; per-coordinate relative residuals use the solver's two-right-hand-side convention; the joint Frobenius scaled-residual indicator is diagnostic and is not asserted to be the exact minimum multi-right-hand-side backward error.
What would falsify it: accepted or replayed residual above its declared limit; failure of either sufficient forward-error inequality; a nonconverged Krylov report; boundary disagreement; any flipped/overlapping P1 map; dense SVD above the configured limit; or a receipt whose Git commit and cleanliness cannot reproduce the audit implementation.
Smallest decisive test: retain the old false-negative A6000 receipt, add direct and iterative corruption tests, reproduce the unchanged formal O4 map trace under the residual-conditioned contract, test an above-limit case with `numpy.linalg.svd` replaced by a forbidden sentinel, then commit and rerun from a clean remote worktree before accepting the receipt as evidence.
Prior work: the original independent re-decode used a fixed `4096*eps` absolute agreement tolerance. The first A6000 O4 map run completed with zero folds but was rejected at `8.398446e-10`; its BiCGStab relative residual was `8.829264e-11`.

Audit correction status (2026-09-22T01:57:25Z): The mathematical checker accepted the represented-system assembly, residual formulas, exact small-system \(\kappa_2\), sufficient forward-error bounds, direct/iterative tolerance split, corruption failures, and three-way boundary/topology checks, but returned bounded FAIL on evidence. The first correction unconditionally ran a full dense SVD and did not disclose its scaling: for interior sizes 529 and 2209, one float64 matrix grows from about 2.135 MiB to 37.229 MiB, while cubic-work scaling is about 72.8 times before workspace and platform effects. The audit now routes the configured dense limit into the independent check, records the exact method, bytes and asymptotic costs below the limit, and returns a structured fail-closed scale-limit result before matrix construction or SVD above it. The ambiguous `normwise_backward_error` field is renamed `joint_scaled_residual_indicator`. Focused local evidence after the correction is `16 passed, 1 CUDA-unavailable skip`; independent code re-review and a clean post-commit A6000 receipt are still required. The prior dirty A6000 run establishes the numerical cause but is not final provenance evidence.

Clean A6000 replay result (2026-09-22T02:08:30Z): Commit `74b452c` was pushed, checked out as a detached clean AI-host worktree, and kept clean by writing outputs outside the checkout. The actual-CUDA harness suite passed `17/17`. Eight fresh single-method float64 A6000 processes then reproduced the formal N25/R256 map and image cohort with `commit=74b452c`, `dirty=false`, strict JSON, and the same seed, target, shared rate, solve slices, and thresholds as the CPU authority. Map O1--O4 all completed; exact-83 objectives were `7.0055138e-5`, `4.9662126e-5`, `6.7622546e-5`, and `4.8073904e-7`, agreeing with CPU to ordinary iterative-solver error. Image O1--O3 completed with exact-83 objectives `0.0018133391`, `0.0012159964`, and `0.0017500772`; baseline O4 independently repeated the true attempted-step-18 geometry failure and preserved its certified step-17 state (`0.0043390717`). Every final accepted state, including failed O4's preserved state, passed the residual/condition, fixed-boundary, and three-way P1 topology audit; method/audit/protocol failure counts were `0/0/0` except the intentionally preserved image O4 `1/0/2`. Final exact \(\kappa_2\) values ranged from `134.78` to `213.76`, while maximum iterative-to-direct replay discrepancies ranged from `6.29e-10` to `1.27e-9` and remained below sufficient a-posteriori bounds. CUDA peak allocated memory in fresh processes was about `16.5--25.9 MiB`; this includes optimization and final replay audit and excludes process/framework memory. Thus the old O4 map rejection is closed as a fixed-tolerance false negative, while the O4 image failure remains a genuine decoder/topology-domain failure. Independent receipt-level review is pending; this paragraph is not a Route-II PASS declaration.

Local regression status (2026-09-22T02:08:30Z): Because this Windows Anaconda stack can abort when PyTorch and NumPy/SciPy Intel OpenMP runtimes are initialized in the wrong order, validation is intentionally split across fresh processes with NumPy LAPACK bootstrapped before Torch rather than using `KMP_DUPLICATE_LIB_OK`. The non-harness Route-II collection passed `85` tests with one local-CUDA skip; the independently loaded benchmark collection passed `16` with one local-CUDA skip, for `101 passed, 2 skipped` in total. The remote Linux A6000 benchmark collection adds `17/17` actual-CUDA passes.

Independent clean-receipt verdict (2026-09-22T02:14:30Z): Bounded PASS for all eight `74b452c_clean` A6000 receipts. The checker independently recomputed every residual, forward, pairwise, boundary, and three-map topology predicate; verified exact solve-slice indices and missing rows; and confirmed the remote checkout remained clean. Within each task, all four target identities agree; versus the clean CPU authority, maximum trace-objective differences are `9.10e-11` for map and `2.15e-9` for image, with identical threshold steps and counters. The O4 map's old/new 82-row non-timing trace and final state are identical, so only the corrected audit verdict changed. The image O4 retains 17 accepted steps, `20/19` primal attempts/completions, the step-18 `retraction_decode` failure, and a certified preserved final state. This closes the clean-provenance blocker for the instance receipts but is not yet the whole Route-II checker verdict.

Route-II closure (2026-09-22T03:06:41Z): After the real five-hour boundary, the independent checker issued bounded PASS at frozen HEAD `1646570`; the report and eight claim-level records were committed as `4a01311`. This authorizes Route III only. It does not turn the failed O4 image cohort, inaccurate tested float32 large-mesh gradients, runtime-specific A40 memory peak, missing CNN, or absent arbitrary-prescribed-Beltrami result into success.

UTC time: 2026-09-22T03:11:21Z
Route: III-A/B — finite-direction limitation and primary-source boundary
Question: Can a fixed finite set of planar edge directions with nonnegative scalar conductances represent every two-dimensional symmetric positive-semidefinite tensor exactly, and which compatible-discretization results actually bear on the hard-bijective decoder objective?
Exact claim/hypothesis: For fixed nonzero directions \(e_1,\ldots,e_m\), the cone \(\mathcal C=\{\sum_k c_ke_ke_k^{\mathsf T}:c_k\geq0\}\) is finitely generated and polyhedral, whereas \(\mathbb S_+^2\) has a continuum of extreme rank-one rays and is not polyhedral. Hence no fixed finite direction set covers all SPD orientations exactly. This obstruction applies to scalar positive-direction decompositions, not adaptive directions, changing planar connectivity, or a full/block Hodge star.
Assumptions: directions and connectivity are fixed independently of the target tensor; coefficients are scalar and nonnegative; equality is tensor equality rather than only agreement on a finite test set; degenerate zero directions are removed.
What would falsify it: a valid finite fixed-direction positive decomposition of every rank-one ray; an error in the extreme-ray/polyhedral argument; or prior art showing that the proposed object is not a fixed scalar-direction cone.
Smallest decisive test: give a self-contained extreme-ray proof, then sample the declared Beltrami radius/angle grid and solve exact NNLS projections on each candidate planar stencil, retaining worst orientations and zero-error cases. A targeted primary-source table must state what each DEC/Whitney/RT/FEEC/rectangle-tiling/neural work preserves and does not guarantee.
Prior work: Phase-V plan sections 33--34; Phase-III anisotropic-Hodge notes; classical finite-element cone/DMP conditions. Literature claims require direct primary-source verification.

UTC time: 2026-09-22T03:11:21Z
Route: III-C/D — full anisotropic Whitney/Hodge reference
Question: Can one fixed-topology reference discretization retain the complete piecewise-constant tensor energy and differentiable solve while measurably improving flux/conjugacy structure over an ordinary sampled construction, without mislabelling that reference hard-bijective?
Exact claim/hypothesis: On an oriented triangular mesh and facewise SPD tensors \(A_T\), local P1/Whitney edge-form integration yields an assembled symmetric operator \(L_A=B_0^{\mathsf T}H_AB_0\) whose Dirichlet energy equals \(\sum_T |T|\nabla u_T^{\mathsf T}A_T\nabla u_T\). After Dirichlet elimination it is SPD when the mesh is connected to the boundary. The potential solve and tensor/Dirichlet VJPs are differentiable, but off-diagonal signs and a global homeomorphism theorem are separate questions. A compatible flux representation should reduce a precisely defined conservation/stream-conjugacy residual relative to the declared ordinary baseline.
Assumptions: nondegenerate consistently oriented triangles, uniformly SPD face tensors, fixed incidence, exact Dirichlet elimination, compatible sign conventions, and no claim that SPD alone implies an M-matrix or injectivity.
What would falsify it: one-triangle energy mismatch, loss of symmetry/positive energy, incidence-sign inconsistency, nonzero interior balance for a solved manufactured problem beyond tolerance, incorrect VJP finite differences, or no measurable improvement in the preregistered conjugacy diagnostic.
Smallest decisive test: hand-check one triangle and a two-triangle patch; compare assembled energy to direct gradients; verify solve/tensor/boundary directional derivatives; then run a manufactured anisotropic field on increasing structured meshes and compare energy, flux balance, stream integration, and \(\|\nabla v-JA\nabla u\|\) to the ordinary P1/legacy reference.
Prior work: existing `src/qcopt/forward/anisotropic_hodge.py` is reconnaissance and must be audited rather than trusted; Phase-V plan sections 35--36 and 41--42.

UTC time: 2026-09-22T03:11:21Z
Route: III-E--K — positive planar projection and hard-bijective hybrid
Question: What accuracy frontier results when an arbitrary Beltrami tensor is projected to strictly positive conductances on fixed planar graphs, and can that projection drive a differentiable hard-bijective Tutte layer at 256 squared resolution?
Exact claim/hypothesis: For each explicitly embedded noncrossing planar candidate graph, a positive conductance parameterization \(c_e=\varepsilon+\operatorname{softplus}(s_e)\) preserves the symmetric Laplacian sign structure. With the verified convex rectangle boundary and applicable Tutte/Floater hypotheses, its decoded control map is a P1 homeomorphism. Standard, center-split, and one additional planar refinement will exhibit a nonzero worst-case tensor/operator approximation frontier at large anisotropy; a full-Hodge teacher may be more accurate but has no automatic topology guarantee.
Assumptions: every added edge belongs to an explicitly verified planar triangulation/cell complex; boundary cycle and dividing-edge hypotheses are checked; tensor fitting, operator fitting, and map/Beltrami fitting are reported separately; positivity is never inferred from an unconstrained least-squares coefficient.
What would falsify it: crossing edges, nonpositive fitted conductances, a topology failure in an accepted positive decoder output, a false claim of exact arbitrary-SPD coverage, autograd disagreement, or a 256-squared result that bypasses the decoder or compares different targets.
Smallest decisive test: derive local direction matrices and NNLS/softplus fits on tiny cells; sweep \(|\mu|\in\{0,0.2,0.4,0.6,0.8,0.9\}\) over a fixed angle grid on three planar constructions; verify the edge-gradient identity \(- (B_0\lambda)_e(B_0x)_e\) by finite differences; then compare the surviving positive student, Route-I baseline, and full-Hodge teacher on one shared N25/R256 map and image problem, with topology, \(\mu\), energy, timing, memory, and solve iterations.
Prior work: Phase-V plan sections 37--43; Route-I symmetric positive-conductance layer; existing Phase-III anisotropic-Hodge and rectangle-conductivity code. No post-hoc fold repair is permitted.
