# Phase III independent checks

This file records only claim, independent checker finding, and fix. A claim is
not closed merely because the builder's derivation or implementation passes its
own tests.

| claim_id | claim | checker finding | fix/status |
|---|---|---|---|
| C1 | Unit-grid electrical rectangle has modulus/energy/right flux/area (n_y/(n_x-1)). | Independent parallel-row derivation and 2x2, 3x2, 3x3 calculations agree; old 0.75 omitted one strip. | PASS for the restricted rectangular row-path-independent construction; weighted API documented as restricted. |
| C2 | Four-direction anisotropic decomposition is unique. | Rank-three coefficient map has null vector ((-1,-1,1,1)); exact signed family is one-parameter, while nonnegative feasibility is (|b|\le t\le\min(a,c)). | PASS after API/documentation correction. |
| C3 | Local transformed nonobtuse condition is globally necessary. | Isotropic 120°/30° shared edge gives local (+0.288675), (-0.866025), assembled (-0.577350). | PASS as a sufficient-only condition; use assembled edge sums for (M)-matrix claims. |
| C4 | Directed Tutte layer unconditionally guarantees a homeomorphism for its current boundary input. | The theorem requires a simple weakly convex/strict-corner boundary, positive directed weights, and 3-connected-to-boundary. The implementation now validates weak convexity/orientation/repeated vertices, but still does not check graph 3-connectivity. | REVISE: retain graph certificate as an explicit caller precondition. |
| C5 | Rectangle modulus latent is differentiable. | Independent finite-difference check at (m=-0.3): analytic gradient (1.651961931851348), central difference (1.651961931692370), absolute error (1.6\times10^{-10}). | PASS for tensor-valued width/height or modulus helper; float defaults are constants. |
| C6 | Directed Tutte is universal for arbitrary Beltrami coefficients. | Local-star positivity proof applies only to an already valid straight-line PL embedding with positive areas and boundary-reachable support. | REVISE scope to valid-embedding surjectivity; arbitrary-(\mu) expressivity remains open. |
| C7 | SPD anisotropic mixed solve has monotone free-side traces. | Explicit 3x3 vertex/8-face case with (|\mu|=0.8) gives left middle-row increment (-0.10057537), despite SPD face tensors. | COUNTEREXAMPLE for fixed facewise-(P_1) complementary solve; continuum canonical-map monotonicity remains separate. |
| C8 | Positive scalar conductances on the tested planar stencil force ordered free-side traces. | Exhaustive \(3^8=6561\) scalar assignments \(\{1,2,10\}^8\) on the 2x2-cell right-triangle stencil had no negative increment; minimum was 0.0909090909. | RESTRICTED PASS only for this M-matrix stencil family; no arbitrary-planar-graph theorem claimed. |
| CQ1-CQ2 | Continuum mixed Beltrami/conductivity theorem has the stated sign, same-(A) stream problem, and reciprocal energy. | Independent checker found no sign/orientation/algebra error. Required precision fixes: image is open rectangle interior; closure maps to closed rectangle; boundary implications are weak trace identities; insert (operatorname{div}(A\nabla v)=0); fresh audit additionally requires explicit Jordan wording, Carathéodory closure extension, a.e. arc-trace constants, and weak flux pairing. | PASS after document revisions; checker reports `tmp/phase3_continuum_checker.md` and `tmp/phase3_continuum_checker_reaudit.md`. |
| CQ1-P1-sampling | A continuous canonical QC homeomorphism automatically yields a fold-free P1 interpolation. | Independent checker validated the Taylor/singular-value bound only under a conforming polygonal disk, certified C2 Hessian and minimum-singular-value bounds, and a simple noncollapsed polygonal boundary trace. A curved boundary cannot be called the image of the straight sampled chords without an isoparametric or separate boundary argument. | REVISE: retain as a conditional sufficient sampling lemma; no implication follows from merely \(\|\mu\|_{L^\infty}<1\). |
| CQ8-continuum | Canonical continuum stream map has ordered marked-side traces. | Conditional on CQ1's canonical QC closure theorem, marked boundary arcs map homeomorphically to rectangle sides, so their coordinate traces are strictly monotone. Ellipticity alone is insufficient; the fixed-P1 negative trace example remains separate. | CONDITIONAL PASS for the canonical continuum map; OPEN/NOT CLAIMED for arbitrary independent discrete solves. |
| CQ10 | Directed Tutte can fit arbitrary QC/PL targets with exact universal expressivity. | Sine-bump targets on a 16x16-cell mesh fit with map RMSE 1.27e-3 to 4.12e-3, but facewise mu RMSE grows 1.95e-2 to 8.91e-2 and the determinant margin approaches 3.75e-6. | REVISE: empirical conditional expressivity only; no universal exact claim. |
| CQ11-CQ12 | Learnable modulus plus directed Tutte is a practical hard-bijective neural layer at realistic resolution. | Independent Tutte runs at 129x129 and 257x257 are finite and positive (257x257: 4.0842 s forward, 1.8012 s backward, RSS +121.4 MB). The MBM implicit CPU VJP is also finite (257x257: 14.1279 s forward, 12.1254 s backward, RSS +247.25 MB), but has 0.02352 conjugacy residual; graph 3-connectivity remains an explicit precondition. | CONDITIONAL: Tutte has lower measured prototype cost, but neither is a certified universal production solver. |
| CODE-SPD | `discrete_conjugacy` accepts only valid face tensors for the stated theorem. | Independent code checker found that determinant-positive alone admits negative-definite `-I`; the validator was corrected to require positive diagonal entries and determinant-one for the conjugacy residual. | PASS after focused regression tests. |
| CQ4-stream | A structure-preserving flux can be integrated as a discrete stream coordinate rather than reconstructed by an independent complementary solve. | The new edge-integrated prototype was independently tested on direct affine sign cases and the layered compatible P1 map: face predictions agree, cycle residual is below 1e-12, and reconstructed values equal the manufactured stream up to gauge. A random incompatible P1 potential with identity tensors is rejected by edge/cycle diagnostics. Boundary singleton edges necessarily contribute zero to face-mismatch, and tree-edge cycle residuals vanish by construction. | PASS as a compatibility diagnostic/prototype; not a general dual Hodge star, boundary solver, or arbitrary-μ neural layer. |
| CODE-BOUNDARY | Directed Tutte boundary preflight rejects all invalid target cycles needed for the theorem. | Independent checker found a boundary-only empty reduction and a self-intersecting star polygon. The layer now handles zero interior rows, checks device equality, and rejects repeated/negative-turn/self-intersecting boundaries. | PASS for these preflights; graph 3-connectivity remains a caller precondition. |
| CODE-AREA | Electrical tiling-area check is independent of the reference target. | Independent checker identified `target_area=modulus` as circular; the result field was removed and tests compare independently reconstructed area to right flux. | PASS after API correction. |
| CODE-METRICS | Benchmark determinant and memory columns have one consistent meaning. | Independent checker found raw triangle area and normalized Jacobian values were mixed and provenance was absent. CSV now records determinant definition, hardware class, seed, and RSS-delta semantics in notes. | PASS as a reporting correction; RSS is a process-level proxy, not allocator peak. |
| C6-ADAPTIVE | Eigen directions, metric pullback, or a full SPD Hodge block automatically solve the anisotropic route. | Eigen/pullback algebra is valid locally, but face-dependent metrics require weighted edge sums; a generic 2x2 Hodge block lacks specified basis/DOFs/assembly and gives no M-matrix/topology guarantee. The current P1 module is only a compatibility prototype. | REVISE: keep adaptive/metric/block routes open research, not implemented or proved. |
| SUPPORT-Beurling | A bounded-domain Beurling solve can use a periodic FFT without boundary qualification. | On a 128x128 compactly supported field, periodic vs pad2 differed 5.19% centrally and 27.5% in the outer annulus; periodic vs pad8 differed 5.53% and 29.5%, while pad4/pad8 central values differed 2.03e-4. | PASS as a caution: FFT is a torus/periodized operator; zero-padding is an approximation, and nonuniform meshes need quadrature/NUFFT/treecode. |
| SUPPORT-BHF | Legacy BHF multilevel evidence establishes a production-ready universal solver. | Independent checker matched the receipts: a separate 512x512-cell GPU-native single-step assembly took 1,044.19 s (1,066.53 s end-to-end), had zero flips, and a separate 32x32 NumPy Duffy parity run had relative L2 error 1.66e-7. The receipts omit GPU driver/software/seed/commit metadata; older validation identifies the device class as an AI RTX A6000. No bounded-domain principal-value theorem, arbitrary-mesh homeomorphism certificate, or production multistep benchmark is closed in Phase III. | SUPPORTING ONLY: retain the promising multilevel direction, but do not promote it to a certified candidate or call the failed large run a theorem counterexample. |
| CQ12-Tutte-unstructured | Structured-grid positivity evidence transfers automatically to a general mesh. | Independent Delaunay stress with 4,056 vertices and 7,854 faces had zero flips/intersections and finite VJPs at logit spreads 1 and 3, but the minimum signed-area ratio fell from 3.465e-3 to 5.547e-11. | REVISE: unstructured finite stress passes conditionally, but high logit spread is nearly singular and no arbitrary-mesh theorem is established. |
| CQ12-MBM-VJP | The MBM candidate has no differentiable backward prototype, or a finite VJP benchmark is only self-consistent. | `mbm_lbs_torch_implicit` was independently timed in isolated processes at 65/129/257/513 vertices (0.78013/3.17149/14.06027/69.09319 s forward; 0.77766/3.00769/12.22195/48.66864 s backward; RSS deltas +18.309/+59.016/+245.375/+1230.804 MB). A separate 7x7 directional finite-difference row (seed 3, epsilon 1e-6) gave relative gradient error 9.37e-10. After the metric-label fix, direct implicit-output conjugacy/min-det values at all four resolutions agree with the independent reference solve to below 1.2e-13; the independent induced-face calculation gives all-face Beltrami RMSE 2.1288e-3/1.0675e-3/5.3513e-4/2.6822e-4, while max error remains about 1e-2. | PASS for existence of a CPU VJP prototype and empirical P1 refinement trend; REVISE candidate status because exact discrete conjugacy, GPU implementation, and hard topology certificate remain unresolved. |
| CQ12-fair-control | MBM and directed Tutte can be compared on exactly the same nontrivial target and resolution. | For constant real (a=0.3), both fresh-process runners recovered the affine target at 129 and 257 vertices with map RMSE below (4.3\times10^{-13}), normalized face-area ratio one, finite gradients, and the receipts' forward/backward/RSS values (MBM 2.985/2.860 s and 48.57 MB at 129; Tutte 6.155/0.428 s and 21.52 MB; MBM 13.778/12.570 s and 193.92 MB at 257; Tutte 25.667/1.683 s and 119.43 MB). No independent global injectivity audit was run for this control. | PASS as a matched affine sanity control only; it does not establish general-μ accuracy, topology margin, or production complexity. |
| CQ12-fill | MBM memory is dominated only by the assembled sparse stiffness matrix. | Independent SuperLU/COLAMD assembly at 129/257/513 vertices found free-matrix nonzeros 113,659/456,699/1,830,907, while one primary (L+U) factor had 1,776,480/9,612,312/53,221,022 nonzeros and one complementary factor had 1,725,390/9,580,596/53,887,458; structural fill ratios rose to 29.07--29.43 at 513. | REVISE: direct-factor fill is a major memory warning; these are structural nonzero counts for one ordering, not bytes, peak RSS, or an asymptotic theorem, and the production VJP retains four factorization closures. |
| CQ12-highres | The two surviving neural-layer prototypes remain practical at 513x513 vertices. | Independent run: MBM output/gradient finite, 69.093 s forward, 48.669 s backward, RSS delta +1,230.8 MB, normalized minimum face determinant 0.50538, residual 0.02355. Directed Tutte at 512x512 cells (263,169 vertices) had finite output/gradient and zero flips/intersections at logit spreads 1 and 3, but minimum signed-area ratios 1.328e-3 and 2.5495e-12; the combined passes took 117.990 and 118.219 s, and RSS delta was not measured by that script. | CONDITIONAL numerical pass only; MBM memory is a major scaling bottleneck and high-spread Tutte is near singular. No production or universal complexity claim. |
| CQ7-wide-stencil | Enlarging a positive edge-direction stencil preserves topology while improving anisotropic tensor coverage. | Independent checker reproduced the structural counts, residuals, determinants, and 1,641 flips from the checked-in artifact. One-ring support had fit residual mean/p95 0.129011/0.795089 and no flips but minimum raw determinant (2.4053\times10^{-12}); two-ring support reduced residual mean/p95 to 0.00290868/(7.44\times10^{-12}) but produced 1,641 flips and minimum determinant (-4.9312\times10^{-4}). The residual is an absolute pre-clipping coefficient norm over vertices with at least three supports, including boundary vertices; artifact timings are machine-specific. | COUNTEREVIDENCE for a naive wide-stencil decoder; an adaptive planar connectivity and global monotonicity theorem remain open. |
| CQ7-edge-formula | The metric-Delaunay / weighted-cotangent inequality predicts the assembled shared-edge P1 stiffness sign. | Independent direct-gradient audit on 20,000 random two-triangle configurations found maximum formula error 3.73e-11 and zero sign mismatches for determinant-one Beltrami tensors and for general SPD weighted tests. It found 2,352 local-obtuse-but-edge-nonpositive cases; dropping determinant weights in the general-SPD control misclassified 325/20,000, while determinant-one Beltrami tensors correctly make those weights equal. | PASS for the local edge identity and its precise hypotheses; this remains only an assembled-sign certificate, not a global M-matrix/homeomorphism theorem. |
| FINAL-MATH-R1 | Section 02 claimed a unique facewise Beltrami coefficient without a nondegeneracy hypothesis. | If Df_h=0, both complex derivatives vanish and the ratio is not identifiable; positive face determinant implies f_z != 0. | FIXED: Section 02 now states the nondegenerate/positive-determinant assumption explicitly. |
| FINAL-MATH-R2 | Section 04 could read K_ij <= 0 as a universal necessary condition for every DMP proof. | The sign/connectivity conditions are a standard sufficient Z-matrix/M-matrix certificate; another monotonicity proof need not use that certificate. | FIXED: wording now says sufficient certificate and disclaims universal necessity. |
| FINAL-MATH-R3 | The P1 sampling lemma did not identify compatible norms or certified m_T,M_T. | The Taylor perturbation bound is valid only with one fixed subordinate matrix/vector norm and certified lower/upper bounds, not point samples. | FIXED: Section 01 now states the norm and certification requirement. |
| FINAL-MATH-R4 | The incidence/Hodge feasibility equations wrote B_1 q = 0 as primal flux conservation. | With B_0 vertex-to-edge and B_1 edge-to-face, B_1 B_0 = 0 is gradient curl-freeness; Kirchhoff conservation is B_0^T q = 0, while dual stream exactness is dual B_1 Rq = 0 or primal B_1 delta = 0 for delta = Rq. | FIXED: Section 02 now separates primal conservation, dual exactness, and the implemented primal-edge diagnostic. |
| FINAL-CODE-R1 | The implicit MBM layer accepted undersized grids, malformed tensors, or tensors on a different device. | Independent code review found that the solver's assumptions were not enforced at the public entry point. | FIXED: `mbm_lbs_torch_implicit` now requires a rank-two real/imaginary tensor, at least 3x3 vertices, and matching devices; focused validation tests pass. |
| FINAL-CODE-R2 | The MBM benchmark's conjugacy and determinant columns were direct metrics of the differentiable output. | The first receipt computed those columns from a separate reference solve, so it could not support that interpretation. | FIXED: the benchmark now evaluates `implicit_conjugacy_residual` and `implicit_min_face_determinant` directly on the implicit output and reports reference values separately; the four v2 receipts agree below 1.2e-13. |
| FINAL-CODE-R3 | The structure-preserving stream prototype was part of the public forward API and the cost comparison described its scope accurately. | The stream symbols were not exported and the comparison heading overstated production feasibility. | FIXED: `DiscreteStreamResult` and `integrate_stream_from_face_flux` are exported; the document now says “prototype cost comparison.” |
| FINAL-CODE-R4 | Fill and fair-control receipts carried enough provenance and topology semantics for their claims. | Fill metadata omitted the CPU/software environment, while fair-control `flip_count=0` was inferred rather than independently audited. | FIXED: fill receipts record CPU/Python/NumPy/SciPy/OMP; fair-control CSV rows use `NA` for unperformed global injectivity audits and state that limitation explicitly. |
| FINAL-CODE-SCOPE | The Phase III code closes a universal arbitrary-\(\mu\), hard-bijective, GPU-scale production layer. | Final code review confirms finite CPU VJP and matched controls only; arbitrary graph connectivity, exact discrete conjugacy, GPU execution, and a global topology certificate remain unclosed. | BLOCKED CLAIM: do not call either prototype universal, production-ready, or certified for arbitrary \(\mu\). |

## Phase V Route I checks

| Claim ID | Claim checked | Independent finding and correction | Verdict |
|---|---|---|---|
| P5-R1-DIRECTED-FALLBACK | CUDA float32 BiCGStab can fall back to stationary Tutte iteration without weakening fail-closed semantics. | The checker proved the exact-arithmetic spectral-radius condition only under row substochasticity and boundary reachability, and found no uniform float32 or `20*max_iter` rate guarantee. A second checker found a finite `3e38` RHS whose represented norm overflowed, making `inf > inf` false and returning an unconverged zero solution. Nonfinite norm/tolerance guards and a mandatory converged-report check were added; the red regression, dense/VJP/finite-difference probes, and A40 overflow rejection now pass. Mixed-precision replay at N=49 is `1.001176e-5`, so it is not reported as strictly below `1e-5`. | PASS for bounded fail-closed correctness; convergence availability and speed remain empirical. |
| P5-R1-SYMMETRIC-CG | Candidate/periodic true-residual replacement yields a usable constant-memory float32 symmetric layer. | Independent dense-SPD, VJP, finite-difference, batch, 32-step restart, and N=49 CPU-double checks pass. The strict `2e-6` N=49 stress still fails at `6.609e-6`; the `1e-5` default is an explicit tradeoff. An inactive zero RHS initially triggered a full true-residual matvec every iteration; intersecting reliable updates with the active mask closed that inefficiency without changing the solution. | CONDITIONAL: bounded numerical logic passes; multi-seed conditioning and stable timing remain open. |
| P5-R1-HARNESS | The engineering harness reports actual work, memory, residual, and topology semantics without conflating them. | Independent review confirmed logical primal/adjoint solve counts, topology fields, 256/512 query support, synchronization, and structured failures. Fallback iteration counts exclude the failed primary attempt and are explicitly labelled non-total; GPU peak allocation excludes independent CPU audit and replay. The reference/legacy backend is explicitly unimplemented rather than silently relabelled as improved direct. | PASS for reporting semantics; the planned remote matrix is still pending. |
