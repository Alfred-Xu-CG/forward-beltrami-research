# Phase VII 阶段报告：稠密固定网格 P1 同胚的正向可微生成

> 本报告是21小时研究窗口的技术总结。研究窗口始于2026-09-23 17:52:45 UTC；本轮结束时应再核对最终实验与提交。报告把**数学安全、表示存在性、图像可辨识性、实测速度/显存**分开陈述。没有任何一项单独等于“通用图像配准已解决”。

## 一、问题与明确结论

区域为单位正方形 \(\Omega=[0,1]^2\)，固定规则三角网 \(\mathcal T_N\) 有 \(N^2\) 个**控制顶点**和 \(2(N-1)^2\) 个原三角面。网络从图像对或别的输入产生多尺度有限实数 latent \(z\)，正向解码器输出每个控制顶点像 \(Y_i=D_N(z)_i\)。在每个原三角面上，用三个顶点像定义唯一仿射映射 \(f_Y\)；这才叫一张**固定原网格 P1 映射**。512²图像查询分辨率与 \(N\) 独立，不得拿图像查询数冒充控制顶点数。主设置逐点固定整个正方形边界。

对于取向一致的面 \(T=(i,j,k)\)，定义
\[
J_T(Y)=\frac{\det(Y_j-Y_i,Y_k-Y_i)}{\det(x_j-x_i,x_k-x_i)}.
\]
本轮要求所有原面 \(J_T>0\)，并保持边界恒等。对这张拓扑圆盘，在所述边界一一对应和连续P1条件下，局部非退化正向与全局拓扑度数给出整体同胚；不能把“没有负面”在任意边界条件下滥用成全局单射。[Lipman 的定理](https://arxiv.org/abs/1310.0955)提供一般 simplicial map 的边界/度数框架。本轮针对有限精度还在最终输出上逐原面做保守符号筛选；失败样本返回单位图，这使实际输出仍为同胚，但筛选分支在接受边界不可微、回退时 latent 梯度为零。

本轮**已经达到**：F1交错单顶点、F2交错局部patch及混合F2粗种子＋F1细级均是不用求全局大线性系统的前向机制，在规定有限latent和精确算术下保持同一原网格P1拓扑；两种独立机制在4097²真实控制网格上完成目标复现与所有latent的VJP，实际float32顶点也通过逐面证书；混合模型在1025²及4097²做过多步图像损失反传和未见种子评价。[逐颜色激活重算与局部编译](82_compiled_color_kernel_full_training_pareto.md)使4097²四视图batch1完整训练的热态步时从传统约.76秒降至约.37秒、allocated峰值从约16.07GB降至约6.78GB，不借助CPU暂存；300步实际image-only训练及128例留出评价保持同任务精度。**尚未达到**：单幅自然图像的通用、准确、低显存真值形变推断；所有有限latent上的全域光滑梯度；在消费级物理显存上大batch 4097²训练；已训练小网络对一般同胚的统一逼近。4097²多视图合成数据的高精度不能代替这些结论。

## 二、三种前向机制到底怎样生成一张固定P1地图

### F1：四颜色单顶点安全更新

设当前顶点表 \(Y\) 已是同胚，边界不动。内点 \(p=Y_i\) 邻接六个原三角面；把其余两个顶点记为按取向排列的相对边 \((a_k,b_k)\)，于是面积分子 \(A_k=\det(b_k-a_k,p-a_k)>0\)。给每个内点两个实数latent \(z_i\)，提出原始位移 \(r_i=\alpha h\tanh z_i\)（代码常取 \(\alpha=2\)）。定义对第 \(k\) 面的不利面积损失
\[
\ell_k=[-\det(b_k-a_k,r_i)]_+,
\quad m_k=\min\{\gamma A_k,[A_k-\beta h^2]_+\},
\quad s_i=\min\bigl\{1,\min_{k:\ell_k>0}m_k/\ell_k\bigr\}.
\]
\(0<\gamma<1\)，\(\beta h^2\) 是可选的绝对面积底线；实现对分母再加保守浮点guard。实际位移 \(s_ir_i\) 使每个受影响面的新面积 \(A_k+\det(b_k-a_k,s_ir_i)\ge A_k-m_k>0\)。若当前面已低于指定绝对floor，截断的 \([\cdot]_+\) 只禁止进一步不利运动，不能凭空把它修回floor。将内点按行列奇偶分四组；同一原面在每组最多包含一个可动顶点，因此组内可并行，四组顺序进行。后续组重新读取最新相邻面积；故每一步都是一张固定 \(\mathcal T_N\) 的P1图。面积比例是**当前地图的局部几何函数**，不是对训练损失方向做一次后处理线搜索。[实现与梯度测试](../../src/qcopt/neural_bijection/dense/colored_vertex_relaxation.py)、[精确/浮点保证](22_numeric_certified_joint_layer.md)。

从粗层加密到细层时，每个新顶点首先取**旧P1函数在该点的精确值**，所以连续映射完全不变；随后在更细的原网格上运行F1。这个“精确加密＋安全细更新”与“把两张P1图作连续函数复合再仅取目标网格顶点”不同：后者通常已不再是目标固定网格的一张P1函数，普通重采样也不自动保单射。

### F2：交错patch内的联合更新

将规则网格分为小的、互不交叠的patch，固定每块的当前边界顶点，同一块内多个顶点同时提议移动。一个面在共同缩放 \(t\) 下的有向双面积是**精确二次式** \(A_T(t)=A_T(0)+tL_T+t^2Q_T\)，其中 \(L_T\) 是边位移与旧边的交叉项，\(Q_T\) 是两条边位移的叉积。取不利上界 \(C_T=(-L_T)_++(-Q_T)_+\)，对整块所有受影响面计算允许尺度，使 \(C_T\) 不超过当前正面积的安全份额及绝对floor余量。相邻patch同一pass不共享自由内点；随后把patch平铺偏移半个块宽，四个交错pass覆盖此前固定的seam内点。其精确算术不变量与F1同样是原网格逐面正向，但F2允许相邻顶点联合运动，能在特定强形变下减少被单点可行域截断的粗层轮数。[公式、充分条件与1025²强样本](26_f2_uniform_isotopy_approximation.md)。

### 混合模型与图像提示的职责边界

实验中常用17²粗种子上的F2，随后各dyadic级别的F1。宽度16的卷积编码器从图像/坐标预测粗种子和早期细级latent；到513²、1025²、2049²、4097²时，局部光度提示可在当前地图上重新观察图像残差，再提出下一次latent。对 \(C\) 幅观测同一真实形变的纹理通道，当前地图为 \(g\)，通道 \(c\) 的残差 \(r_c(x)=I_{F,c}(x)-I_{M,c}(g(x))\)、移动图梯度 \(v_c(x)=\nabla I_{M,c}(g(x))\)。在顶点周围窗口 \(W_x\) 定义
\[
G_x=\rho I_2+\frac1C\sum_{c=1}^C\langle v_cv_c^\top\rangle_{W_x},\qquad
b_x=\frac1C\sum_{c=1}^C\langle v_cr_c\rangle_{W_x},\qquad
d_x=G_x^{-1}b_x.
\]
\(\rho>0\) 使每个**独立2×2**矩阵正定。这只是光度位移近似；把 \(d_x\) 变为有界logit后仍必须经过F1安全器。全层没有一个随 \(N^2\) 增长的全局矩阵逆。上式和编码器、图像取样、F1/F2均可用自动微分求VJP；对已接受且不在分支切换点的样本，训练损失能反传到网络参数。理想独立残差噪声下的偏差/协方差公式，以及它为何不能直接用于本实验的噪声梯度，见[严格局部统计推导](76_multiview_local_estimator_noise_bias_formulation.md)。[局部提示实现](../../src/qcopt/neural_bijection/dense/photometric_hint.py)、[1025²训练](57_high128_trainable_feedback_layer.md)、[4097²多视图](68_multiview_observability_4097_p1.md)。

当前图像模型的细级latent主要由这个局部2×2提示及少量可学习增益生成，**不是**一个任意指定33.5百万细级标量的通用逆编码器。因而下一节对所有满足界的目标构造出的teacher latent，并不自动属于该小网络在给定图像下的可学输出集合；其有效表达能力必须由独立形变/纹理的训练与留出实验评估。

## 三、逼近理论：证明了什么，未证明什么

先考虑保持边界恒等的空间 \(C^{1,1}\) 同伦 \(F_t\)，\(F_0=\mathrm{id}, F_1=F\)，且所有 \(t,x\) 有 \(\det DF_t\ge m>0\)、\(\|DF_t\|\le L\)、\(\operatorname{Lip}(DF_t)\le K\)、\(\|\partial_tF_t\|\le M\)。[F1定理](23_uniform_isotopy_multilevel_approximation.md)与[F2定理](26_f2_uniform_isotopy_approximation.md)给出：对**不含浮点分母guard的理想实数算子**，存在只依赖统一界的足够细但**固定**seed尺度和有限seed周期数，使每次dyadic加密后仅一个F1颜色周期或一个F2交错patch周期，就能用有限teacher latent**精确得到 \(I_hF\) 的顶点表**；误差 \(\|I_hF-F\|_\infty\le Kh^2\)。证明的关键不是“最后目标面正向”这一句，而是每一小时间步/新顶点残差都低于局部一次到位面积预算，故安全器不截断，归纳逐级达到目标。每一级仅线性扫其网格，网格总顶点数形成几何级数，所以这种**已知teacher latent条件下**的前向工作与存储为 \(O(V_h)\)，不需要大系统求解。实际float32/float64代码的正guard只会缩小更新、保留拓扑，却在极薄目标上可能阻止精确teacher到达；相应文档给出guard不活动的附加面积余量条件，不能无条件把实数可达性赋给固定dtype。这里的时间速度、Jacobian余量和二阶界若趋于无穷/零，seed和周期数并不统一。

对任意边界固定平面同胚，先缩放共轭得到内部紧支撑的近似图，再用相对边界、任意 \(C^0\)-小的曲面光滑化定理得到内部紧支撑微分同胚，随后接上上述固定规则网格F1/F2构造，可以证明**理想实数算子跨所有足够大但有限的架构规模之并**在全体边界固定同胚中 \(C^0\) 稠密。这里取**不设统一正面积底线**的配置 `minimum_jacobian=None`，还须将实际代码的正guard理想化为0；实验里的固定0.05底线或固定dtype guard都不能未经证明用于任意小Jacobian目标。精确量词、角点规避及来源见[定性稠密性证明](30_all_boundary_homeomorphisms_c0_density.md)与[Hatcher 曲面定理](https://ems.press/content/serial-article-files/52184)。这不说明一个固定网络、固定小seed、固定轮数或低维latent能覆盖全部同胚，也不给逆编码的算法复杂度，更不保证仅凭图像能识别目标latent。数学定理是**表示存在性**；4097²实际teacher试验是另一层数值证据。

## 四、实验协议、指标和主要结果

除非特别说明，512²表示图像像素与P1查询点；257²、1025²、4097²表示控制顶点网格。训练图像由独立解析目标变形或本研究定义的合成目标产生，不从待测解码器自身采样；目标地图只在地图监督诊断或事后评价使用，`image-only`训练不看真地图。query-map RMSE为 \(\sqrt{(BQ)^{-1}\sum_{b,q}\|\hat f_b(q)-F_b(q)\|_2^2}\)；image MSE为所有样本/通道/像素的预测移动图采样与固定图差平方平均；mode RMSE是在指定正弦基函数上的二维位移投影系数误差。最小 \(J\) 扫**全部原面**；“接受/回退”由实际输出坐标的保守浮点证书决定。时间为CUDA同步后的热态中位并标明包括哪些环节；显存为PyTorch `max_memory_allocated`，不是显卡总占用或reserved，若数据集常驻GPU会单列。

| 证据对象 | 控制顶点 / 原面 | 输入与训练 | 精度、拓扑、成本 | 范围与解释 |
|---|---:|---|---|---|
| [F1与F2解析teacher](74_two_distinct_forward_mechanisms_4097_teacher_vjp.md) | 4097² / 33,554,432 | 独立 \(F_{.08}\) 的已知teacher latent；不含图像CNN | F1/F2顶点RMSE均3.44e-10，最大坐标误差1.49e-8，最小 \(J=.5660\)；F1全部latent forward+VJP .559s/12.561GB，F2 1.135s/19.941GB，同一GPU7 | 证明该目标在两种机制的大网格实际可达且可反传；**不是图像推断能力**。 |
| [三机制同任务](49_same_task_three_forward_pyramids_and_ood.md) | 1025² / 2,097,152 | 512²合成图像，257²训练后迁移；新128例high32 | F1 map RMSE .003258，F2 .002818，混合 .002912；三者全原面正向零回退；完整训练步各 .144/.253/.223s，但精度及dtype不同 | F1最快不等于最准，F2更准但显存更大；不能当严格同精度Pareto。 |
| [单图像high128局部反馈](57_high128_trainable_feedback_layer.md) | 1025² / 2,097,152 | 32训练例、500步image-only，另对细修正500步；128例探索评价、[新种子确认](58_confirmation_and_texture_visibility.md) | 只训粗编码器/增益的map RMSE约4.9e-4；细局部修正使指定128周期模态误差约减半，但sparse spots换纹理时map RMSE约.0082；零回退 | 网络已可训练且细级参与，但单图纹理泛化不足。 |
| [4097²实际image-only迁移](53_mesh_free_queries_and_4097_image_training.md) | 4097² / 33,554,432 | high32，旧1025²编码器迁移，全参数100步与仅新head100步对照 | 全参数新128例map RMSE .001245，训练步1.089s、22.854GB、零回退 | 已有全参数大网格训练；这里只是100步high32迁移，并非high128多视图结果。 |
| [4097²四独立纹理通道](68_multiview_observability_4097_p1.md) | 4097² / 33,554,432 | 第一通道旧CNN，四通道共用同一真实形变；局部2×2提示按通道平均；新128例 | 无噪声map RMSE 1.523e-5、128周期模态4.592e-6；四通道重复第一幅则回到单通道map RMSE 4.826e-4；全部128接受 | 信息来自独立图像梯度，不是重复通道数量；**不是单图像自然配准成功**。 |
| [4097²完整编码器再训练](75_full_encoder_4097_training_single_vs_four_views.md) | 4097² / 33,554,432 | 32图像训练例、300步、batch1，编码器＋各级增益全VJP；新128例 | C1 map RMSE 4.905e-4→5.580e-4，C4 1.497e-5→1.490e-5；C4训练步.762s/15.934GB；训练/新例零回退 | 证明全链可训练，但单通道退化、四通道收益微小；非一般图像几何恢复。 |
| [四视图对称粗latent融合](85_permutation_invariant_coarse_latent_fusion.md) | 4097² / 33,554,432 | 同32训练地图、300步image-only、batch1；四视图逐坐标去极值平均；新128例＋独立新seed128例外观确认 | 标准四视图map 1.43e-5；一幅spots＋三幅standard新seed map 3.21e-5，对照首通道模型.00436；热态训练.369s/8.075GB allocated，完整热态推理.0887s；均零回退 | 修复特定“坏首通道”结构脆弱性；未改善\(\sigma=.001\)加性噪声，重复图像和自然图像仍有缺口。 |
| [通道数与噪声](70_multiview_channel_count_noise_tradeoff.md) | 4097² / 33,554,432 | \(C=1,2,4,8,16\)，新128例；固定/移动独立高斯噪声 \(\sigma=.001\) | 无噪声C8/C16 map RMSE 6.28e-6/5.39e-6；含噪C4/C8/C16为1.73e-4/8.94e-5/5.66e-5；均拓扑接受 | 多视图增加可观测性但噪声仍显著；采集/显存成本随通道增大。 |
| [降采样光度提示](73_sparse_image_hint_dense_p1_negative_result.md) | 4097² / 33,554,432 | 仅把最细提示采到2049²再插值，原拓扑层仍稠密 | full四通道无噪声map 1.51e-5；2049²提示4.64e-5；VJP .756→.704s | 小速度收益换来明显高频失真；不能拿提示插值替代细级可观测性。 |

4097²[干净单视图细级增益训练](65_trained_4097_extra_feedback_gains.md)及[空间修正阴性](66_trained_4097_spatial_correction_negative_result.md)还表明：图像MSE可以明显下降，而地图RMSE几乎不变；直接地图监督虽改善几何，却不能被算作image-only成功。[含噪四视图再训练](72_noisy_multiview_training_does_not_restore_geometry.md)也只略改善光度与特定模态，留出整体地图误差略变差。**报告任何“精度”必须指明是哪一种指标。**

4097²时仅一份float32二维顶点表就有\(4097^2\times2\times4\approx134.3\) MB；最细级若显式存每个内点的二维latent，又有\(2(4095)^2=33{,}538{,}050\)个标量、约134.2 MB，**不是低维全局代码**。反传还需保存多级地图、各颜色相邻面面积、索引/临时张量、光度提示和网络激活，所以训练峰值远大于顶点表大小。渐近工作/存储\(O(V)\)并不意味着小常数或能装进8GB卡。下面的逐颜色checkpoint在VJP时重算局部前向以减少保存张量；编译融合中间kernel；CPU暂存把显存压力转移到主机内存/PCIe。三者改变实现成本，不改变前述实数拓扑与逼近命题。

4097²四视图完整训练的[细层激活重计算实测](77_4097_multiview_checkpoint_memory_tradeoff.md)把batch1训练allocated峰值从15.934GB降到13.992GB，训练步中位从.761s升至.852s；20步权重轨迹只差浮点末位。[全部saved tensor转存CPU](78_4097_saved_tensor_cpu_offload_extreme_memory_tradeoff.md)进一步把训练CUDA **allocated**峰值降到5.859GB，但步时升至4.333s，运行中主机RSS约29.9GiB；单例reserved峰值约8.66GB，不能据此说已适配8GB物理GPU。[动态图索引＋仅暂存大张量](79_generated_indices_and_selective_offload_4097.md)把同协议完整训练allocated峰值降到5.512GB、reserved峰值8.452GB、单进程RSS历史峰值约26.9GiB，却把训练中位步时从.761s增至3.611s；20步训练和16例测试均通过最终原面证书，参数轨迹只差浮点末位。动态索引使初始化显著更轻，却不降低直接反传的峰值；叠加**整个**细层重算也没有带来可加的收益。更细的[逐颜色重算](80_per_color_checkpoint_vjp_memory.md)则有显著不同的Pareto：**不**暂存CPU时完整训练.877s/7.885GB allocated/10.815GB reserved，约为直接方案一半allocated、约15%增时；再选择性暂存时为2.107s/4.575GB allocated/8.198GB reserved、主机RSS约15.16GiB。后者在48GB卡的8GiB **allocator模拟上限**下运行20步，但**未在8GB或12GB物理设备实测**，不可等同容量保证。[batch扩展](81_batch_scaling_4097_color_checkpoint.md)表明逐颜色重算还在4097²真实控制网格上完成batch2与batch4的20步全参数训练，batch2相比直接反传allocated峰值28.817→14.488GB，batch4实测27.696GB allocated/40.049GB reserved；各训练输出均通过原面证书，但batch4只在48GB卡测试。

最新[局部颜色算子编译实测](82_compiled_color_kernel_full_training_pareto.md)将同协议batch1的20步热态中位进一步降到.367s、allocated6.778GB、reserved9.030GB；编译但不重算为.345s/8.997GB/10.897GB。300步完整训练全部拓扑接受，独立128例map RMSE \(1.490054\times10^{-5}\)，与未编译同训练检查点接近；编译＋逐颜色重算的batch4也完成20步/80例，热态1.063s/24.727GB allocated/34.880GB reserved。**首步编译约十几到数十秒**，不是无条件加速；选择性CPU暂存可降到4.022GB allocated/6.965GB reserved，但步时1.602s且主机RSS约15GiB。后者在48GB卡的7GiB **allocator模拟上限**通过20步，仍非物理小卡验证。

为单独核查编译造成的数值差异，[4097²极端latent压力测试](83_compiled_color_extreme_latent_stress.md)又在幅度5和20下各采样32张独立地图：eager和compiled均有32/32通过全部原面证书，最小实际归一化面积约0.0494；两版顶点坐标不是逐位相同，最大差分别为\(5.36\times10^{-6}\)和\(6.26\times10^{-6}\)。这是有限样本稳健性证据，不是浮点编译版的无条件拓扑定理；正式输出继续依赖逐面数值证书。

同一4097²细级算子的**全latent VJP**也在幅度1和5下各16个新随机cotangent案例比较过：eager/compiled都16/16通过前向证书、梯度有限；相对L2梯度差的中位约0.17%/0.20%，最大约0.36%/0.71%，个别坐标绝对差最高约.0036。故不能把编译版与eager版称为逐位梯度等价；训练结果接近只证明当前任务上的经验稳定性，活动面切换仍值得后续分析。[完整定义与原始数据](83_compiled_color_extreme_latent_stress.md)。

## 五、为什么拓扑安全不等于从一幅图像找回真值

[精确固定P1反例](71_single_image_exact_p1_nonidentifiability.md)取非恒定移动图 \(I_m(x,y)=x\)。在任意奇数 \(N\) 的同一固定网格上，\(F_0=(x,y)\) 与 \(F_1=(x,y+I_h[x(1-x)y(1-y)])\) 都固定边界；\(F_1\) 的所有面 \(J_T\ge3/4\)，中心顶点与恒等图却相差 \(1/16\)。两张图逐点满足 \(I_m\circ F_0=I_m\circ F_1=x\)。因此任意只读这一图像对的算法，在没有额外先验/landmark/其他通道时不可能无条件辨认哪个是真地图。多纹理观测在局部梯度跨方向时改善可辨识性，但[噪声试验](70_multiview_channel_count_noise_tradeoff.md)说明它仍非无代价解决。这个定理**不否定生成层本身**；应把“安全表达给定latent”与“观测足以推断latent”列为不同论文命题。

[新128例四视图通道顺序实验](84_4097_trained_multiview_channel_order_ood.md)进一步揭示**可观测性与编码器架构不是同一回事**：把第一幅标准纹理替成稀疏spots、保留其余三幅独立标准纹理时，已训练4097²模型map RMSE从标准四视图的\(1.50\times10^{-5}\)恶化到.00455；不改变四幅观测，只将spots移到末通道，让仅看首通道的粗CNN改读标准纹理后，误差回到\(3.33\times10^{-5}\)。两次都128/128通过原面证书。三种标准纹理下的新目标形变族仍约\(1.36\text{--}1.50\times10^{-5}\)，但这不代表跨自然图像泛化；复制同一spots为四通道也不提供独立信息，map RMSE达.00830。

针对这个具体瓶颈，[四视图对称粗latent融合](85_permutation_invariant_coarse_latent_fusion.md)让共享CNN分别编码四幅图，对每个latent坐标排序后平均中间两个值，细层安全解码器保持不变。从原方案同一初始权重与32张训练地图独立跑300步完整image-only训练后，在新128例standard/首幅spots＋另三幅独立standard的map RMSE分别为\(1.43\times10^{-5}\)/\(3.20\times10^{-5}\)，对照原首通道方案的\(1.50\times10^{-5}\)/.00455。热态训练步时.369s对.366s、PyTorch allocated峰值8.075GB对7.955GB（都含128例评价数据常驻），300步及留出评价均通过原面证书。若四幅只是同一standard图像的重复，新方案仍有\(4.91\times10^{-4}\)地图RMSE；这个有限合成证据说明输入融合可解除一类人为首通道脆弱性，不代表解决任意弱纹理或自然图像配准。

在相同16例常驻数据、batch1、4097²完整推理链上，去掉各进程第一次编译/特殊化后5次中位，首通道方案约.0854秒、对称融合约.0887秒；范围含编码器、拓扑层、证书、512²查询与图像重采样，后者热态约慢3.8%。[完整设置和逐次计时](85_permutation_invariant_coarse_latent_fusion.md)；这比只报局部F1内核时间更接近layer实际前向代价。

同一新种子下，两种未训练过的高频形变族`high128_tri`与`high128_tiles`在对称融合模型上的map RMSE分别\(1.320\times10^{-5}\)、\(1.295\times10^{-5}\)，均128/128通过证书；旧首通道模型为\(1.385\times10^{-5}\)、\(1.361\times10^{-5}\)。这仅排除这两个合成形变族上的明显性能牺牲，不能外推到一般高频变形。

同一四视图评价集加入固定/移动图各通道独立\(\sigma=.001\)高斯噪声后，首通道与对称融合模型的map RMSE分别\(1.71138\times10^{-4}\)、\(1.71245\times10^{-4}\)，没有可称的抗噪改进；两者各128/128拓扑接受。[同噪声配对日志](85_permutation_invariant_coarse_latent_fusion.md)。

由于上述spots集曾用于结构选择，另用**设计定稿后**的新seed20270823做128例同设置确认：原首通道/新对称融合的地图RMSE分别.004363/.00003208，各128/128通过原面证书；[逐项原始记录及协议](85_permutation_invariant_coarse_latent_fusion.md)。这强化了指定“一坏三好”纹理组合下的改善，但仍是合成分布内的第二种随机实例，而非真实图像验证。

## 六、与已有工作和下一步的科学边界

[原始文献核查](06_prior_art_boundary.md)已列出Tutte正权系统、TutteNet多因子复合、CorticalFlow连续流、SITReg多尺度可逆组合、Generative Escher Meshes等先例。**多尺度、正向、可微或神经同胚各自都不是首次出现。**本研究目前较可防守的区别是固定规则2D原网格的单张P1输出、局部前向安全原语、实现相关的统一光滑同伦类逼近命题，以及百万至一千六百万真实控制顶点的全VJP实测；没有完成覆盖2024–2026所有论文和代码的系统检索，不能声称领域排他性首创。正对称conductance/Tutte或MVC路线在[Phase VI报告](../research_phase6/REPORT.md)中有作为全局求解型比较，Phase VII没有把它误包装为正向新路线，也没有用不收敛近似解冒充拓扑保证。

计划中的S路线在本轮被保留为**既有参考基线**，未新增与F1/F2同一4097²目标、同一GPU和相同训练预算的正导纳完整实验；Phase VI现有1025²数字不能组成对本轮4097²前向层的公平速度/精度排名。Woodbury/分块精确解的低秩适用范围已有[结构化求解分析](../research_phase6/03_conductance_structure.md)，但本轮没有验证一个可让大部分密集细网格导纳变化保持低更新秩的新参数化，故不把它称为已经实现的细网格加速。下一阶段若比较S，须固定控制分辨率、目标分布、输出证书、图像训练预算及显存统计，并分别报告求解和隐式VJP。

最值得继续的是：①以已验证的一坏三好通道改进为起点，研究**对称且置信度感知的多尺度观测/先验模型**，同时保持干净几何精度、含噪稳健性和高频细节；先做公平的真值不可辨识测试，不能把四幅重复图误作独立证据；②把F1/F2的已证明teacher可达性转为可学习、高带宽、跨纹理的latent编码，同时记录训练样本数与模型自由度；③以已见效的**逐颜色重算**为基线，研究算子融合/局部VJP以降低反传激活和重算成本，而不是主要转存PCIe；并在物理小显卡检验batch扩展、allocated/reserved和实际可用容量，在相同地图精度和同一浮点拓扑筛选下比较；④更严格的目标类复杂度上界与先例全文核查。若只降低图像MSE、只加控制点、或只把局部提示稀疏插值，当前证据均不足以解决核心瓶颈。
